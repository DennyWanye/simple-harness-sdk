# F-P32-2 / P32-A03 — Worker action candidate context (bounded fix)

Historical native realpublish needed a hand-written candidate schema in the Mission goal. The first two model-written `actions/*.json` files were refused by `rule_check` for missing or extra fields. The Worker request currently carries the Task contract, but not the declared operation contract.

## Plan / oracle (before implementation)

1. Project the exact candidate wire fields from `action_commits.CANDIDATE_FIELDS` / `validate_candidate`: `connector`, `operation`, `target`, `params`, `reason`; all five required, no extras. The `params` object is model-authored. For `file_publish.publish`, describe only `artifact_path` as model-authored; `artifact_id`, `content_hash`, `size`, `storage_uri` are assigned by `bind_artifact_params` from the accepted Result, even though the connector's execution `OperationSpec.required_params` includes three of those fields. Do not copy any connector root, ledger path, storage URI, secret, or free-form connector metadata.
2. On the Worker path only, require a declared `actions/*.json` Task output and project operation(s) from the Mission action charter admitted by the actual deployment's `action_decision` over registered connector descriptors. Narrow to the Task's action criteria when it has any; a Task may otherwise declare its candidate by the action output while its action criterion remains at Mission level. Cap the projection at eight operations/output paths and refuse an oversized context. Do not synthesize descriptors for an unknown connector/operation. The contract names the charter target and approval level as information, never permission; the original `check_candidate`, approval, allowed-actions and canonical Artifact binding remain the gates.
3. Put the projection in a small helper, pass it at the existing Worker context assembly callsite, and seal it with the existing context hash. Contract schema identity is `action-candidate-context-v1`; unchanged role instructions keep their historical `worker-v3` prompt version, while the added request field changes `context_version` / input hash. Existing ordinary and frozen intent bytes/config stay unchanged.
4. Add focused oracles for actual dispatch-to-Provider USER input with the exact five fields and deployed publish params; an ordinary Task and disabled deployment must have no extra field; a schema-following candidate must pass the original rule checker, while altered scope/undeployed operation remains refused. No publish call in these tests.

Parent owns running the tests and native/paid verification against its immutable snapshot. The child ran no test, build or provider call and made no commit; parent execution follows below.

Parent review corrections before execution: schema belongs to executable Task attempts, including Manager-selected connector/explorer role variants, not just literal worker. Removedarbitrary8-operation/output cap; existing context/request budget remains authority. IndependentSol/mediumreview caught a second rolefilter incontextbuilder afterparentfixedcallsite; fixedboth, added actualManager-set connectorrolewirecontrol and nine-operationcontrol. Original generic ContextRejected handling retained. Tests stillpending while frozenpaidP34runownsrunner.

Parent first integration:48PASS8FAIL72.90s. Five action-wire fixtures failed because envelope_step missed required claims and the role-change fixture used nonexistent Mission.graph_version. Parent corrected claims=[] and final_report graph_version. Original request, candidate and scope assertions remain unchanged; successor pending. No publish executed.

## Parent verified source checkpoint - 2026-09-14 CST

After first48PASS8FAIL72.90s, bounded repairs passed72/72 in13.07s (runner13.51), including22 LC2 controls, actual action-wire context, protocol metadata, known/unknown billed-failure accounting and no resampling/tool execution. Adjacent budget identity/recovery/context/profile plus LC2/action56PASS13.64s (runner14.09). Host with pinned tokenizer45PASS0.52s (runner1.10), zero skip. SDK mypy120filesPASS; new LC2 formatting/imports and protocol detail typing repaired by parent. Native source acceptance and final cumulative audit remain pending. Evidence labels: g-lc2-p32-observation-v2, g-lc2-adjacent-v1, g-host-lc2-profile-v1 under SDK .local-test-evidence/2026-09-12/p33-g. Historical no-dispatch/no-default-evidence limit remains explicit. No packaging/installer/release/push.
