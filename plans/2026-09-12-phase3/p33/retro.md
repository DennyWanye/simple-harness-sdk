# P3.3 接续复盘

2026-09-12：定向测试通过后，全量发现了旧结构断言与两条本机问题；用原源码对照避免误归因，再保留全部反例修复。代价是首次全量未用 -x 多跑了失败后的测试；后续早停并保留完整最终全绿运行。凭据应检查主 .env 的专用 provider 字段，不能只看通用 APIKEY 或旧机证据路径就判断缺失。

B：来源保护必须覆盖工具写入、结果收集、新工作区继承及发布 handoff；只验证初次派发会漏掉撤销后旧树的文件/目录拓扑。ACTIVE 树不能被重建而擦除篡改证据。审批拒绝无需旧 head 仍有效，但必须保持原始绑定不可伪造。独立审查拦住了这些问题；完整回归 867/8/0。不重跑源码未变的昂贵门。
