# P3.3 接续复盘

2026-09-12：定向测试通过后，全量发现了旧结构断言与两条本机问题；用原源码对照避免误归因，再保留全部反例修复。代价是首次全量未用 -x 多跑了失败后的测试；后续早停并保留完整最终全绿运行。凭据应检查主 .env 的专用 provider 字段，不能只看通用 APIKEY 或旧机证据路径就判断缺失。

B：来源保护必须覆盖工具写入、结果收集、新工作区继承及发布 handoff；只验证初次派发会漏掉撤销后旧树的文件/目录拓扑。ACTIVE 树不能被重建而擦除篡改证据。审批拒绝无需旧 head 仍有效，但必须保持原始绑定不可伪造。独立审查拦住了这些问题；完整回归 867/8/0。不重跑源码未变的昂贵门。

C：结构准则与内容准则必须拆分，不能用空 claim 的假 assessment 覆盖文件/action 检查。原始输入长度合法不代表系统包装后仍可持久化，最长引用必须走真实 accept 与重开。升级结果接受契约时，旧 caller-PASS fixture 应补真实前置并保留原 oracle；故障注入需先确认数据库真的变了。

C 完整回归发现 record/fail 新历史保护误改旧 code 接口，两个独立复核者都未提前发现。保留旧失败用例，收窄 doc 范围后完整 979/8/0；后续跨领域公共函数变更必须显式检查 code 分支入口行为，不能只比 grade 函数体。

D：先红绿验证接收资格和实际人工恢复有效；测试故障注入必须先确认冻结字段真的改变，不能用不写config的CAS方法推断产品行为。独立评审提前发现物理产物与复用额度漏洞；E需区分历史receipt和新的消费时效，不扩写无关schema。

E：新人工写侧guard必须配旧任务恢复反例，否则会形成接受拒绝循环。只覆盖首次新请求漏掉已决定的历史review。来源失效是当前消费派生状态，应同时排除Knowledge与会再次暴露其正文的历史摘要，且不改历史等级或receipt。

F：版本提升漏改current API快照导致5个真新增失败，必须与两个runtime版本同时更新，不能事后说成基线。可选tiktoken缺包遮住15失败并让整个12测试模块跳过，环境依赖必须显式记录。baseline runner第二轮复用XML名覆盖首轮XML；已保留首轮完整log/JSON、撤回旧XML可用性并改唯一label，后续每轮证据路径必须独立。
