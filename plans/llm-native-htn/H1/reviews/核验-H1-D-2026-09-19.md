# 独立核验报告：切片 H1-D（请求包新协议增量）

**片名：** H1-D `h1-d-planner-package-v4`（planner-package-hierarchical-v5）
**基准：** `b13e757b74aff9987eebf796ad1b01a3e27b4808`
**核验员立场：** 只看代码与证据；不 commit、不读密钥文件、未运行 `git stash/checkout/reset/rebase/push`。所有变异前把原文件复制到 `/tmp`，变异后恢复并以 `git status --short` 确认干净。
**规格来源（只读引用）：** `simpleharness-llm-native-htn-execution-plan-v2.zh-CN.md`、`LLM-native-HTN计划V2-裁定补遗-2026-09-18.zh-CN.md`（含「追加裁定 2026-09-19 06:30」）、`H1-V2对照源码冲突检查-2026-09-18.zh-CN.md`。

> 还原说明：本报告在若干轮之间曾多次从核验副本中缺失（该目录一度只剩 `核验-H1-A1-2026-09-18.md`）。核验员按同一会话原文**原样还原**此前各轮，并在同一路径续写，未新建报告文件。

---

# 第 1 轮（受检 HEAD `d0f81ba548dceeab954296a7e31e41c2dba05ae4`）

## 结论

边界干净、旧协议逐字节不变、五字段 / v5 标签 / `output_contract` / `enabled_decision_types` / 三哈希函数均与规格一致；但 `visible_refs` 的 task / obligation 哈希来源不符 §5.1。

**判定：修后可合（P0 无；P1 一条）。**

## 逐项

1. **越界：通过。** 仅 `planner_package.py` + 1 个测试文件 + `journal-D.md`。
2. **旧协议字节不变：通过。** baseline 独立加载比对规范 JSON sha256：stub `801b8e39…`、`p23c-pkg` `a9aa2e7e…`、`p23c-pkg2`、`p23c-shared` 全部一致，键集合未移动。
3. **五字段 / v5 / output_contract / enabled_decision_types：通过。** §12 H1 列 7 个 `D/A/X` 类型，脚本差集为空。
4. **visible_refs：发现 P1-1。** task 输出 `fbc4fcfd…`，库内 `task_semantics.content_hash` 为 `814531fc…`；obligation 输出 `66efa7b8…`，`content_hash_of(obligation_json)` 为 `ed0798d5…`（代码对 `{kind,id,semantic_revision}` 求哈希）。
5. **三哈希函数：通过**（独立实现 3 组一致；key 序无关、数组序参与）。
6. **变异：** 指定 8 个全 KILLED；附加 E1–E5 全部 SURVIVED（暴露 P1/P2）。
7. **ruff / 测试：** `All checks passed!`；本片 `46 passed in 0.45s`；相关四文件 `319 passed in 9.49s`。

## 问题

- **P1-1（必修）：** task / obligation 哈希来源不符 §5.1，无测试钉死。
- **P2-1：** 非正 revision 抬为 1、字符串 revision 被 `int()` 收下。
- **P2-2：** `resolution_ref` 被标成 `acceptance`。
- **P2-3：** observation 缺 `content_hash` 时被派生。
- **P2-4（非问题）：** `method_instance` 缺枚举时降级为 method 引用，归属 H1-A。

## 变异表（第 1 轮）

指定 8 个（fact→observation 改写、去重、排序、截断、旧路径加字段、subject_key 随机、enabled 多放 `REQUEST_EVIDENCE`、哈希未排序 JSON）**全部 KILLED**；附加 E1–E5 **全部 SURVIVED**。

```text
$ uv run --offline ruff check <两文件>
All checks passed!
$ PYTHONPATH=src uv run --offline pytest .../test_planning_decision_package_v4.py -p no:cacheprovider -q
46 passed in 0.45s
$ PYTHONPATH=src uv run --offline pytest <相关四文件> -p no:cacheprovider -q
319 passed in 9.49s
```

---

# 复核 2（受检 HEAD `4e5a3264f3b7212858262b610676804b6c0f8de9`；只看 `d0f81ba..4e5a326`）

## 结论

第 1 轮 P1-1 与 P2-1/P2-2/P2-3 修复方向均落实，E1–E5 全部 KILLED，旧协议字节不变。但修复引入新 P1：`visible_refs` 与 `visible_refs_omitted` 输入不同源。

**判定：修后可合（P0 无；P1 一条）。**

## 步骤 1：改动与越界

```text
 plans/llm-native-htn/H1/journal-D.md               |  80 ++++++
 .../planning/htn/planner_package.py                | 224 +++++++++++------
 .../test_planning_decision_package_v4.py           | 268 +++++++++++++++++++--
```

白名单外路径为空，**边界通过**。实现：删 `_ref_hash`；`_one_ref` 只收权威哈希（`isinstance(int)`、`>=1`，缺则跳过）；新增旁路 `authoritative_refs`；`_network_authorities` 从 `network.task_bindings` 取 `binding.content_hash()`；`_task_ref` 只按 `(kind,id)` 查权威表；`_accepted_ref` 区分 acceptance/resolution；`_read_set_ref` 缺哈希即跳过。

## 步骤 2：第 1 轮 5 个存活变异重跑（全部 KILLED）

| 变异 | 结果 | 尾行 |
|---|---|---|
| E1 task 权威哈希加盐 | KILLED | `3 failed, 55 passed in 0.57s` |
| E2 obligation 固定哈希 | KILLED | `2 failed, 56 passed in 0.58s` |
| E3 非正 revision 抬 1 | KILLED | `1 failed, 57 passed in 0.55s` |
| E4 `resolution_ref` 发 acceptance | KILLED | `1 failed, 57 passed in 0.55s` |
| E5 字符串/浮点 revision 被接受 | KILLED | `1 failed, 57 passed in 0.54s` |

## 步骤 3：新增变异

KILLED：N1、N2、N4、N5、N6、F1b。SURVIVED：N3、N7、N8、N9、F1、F2。

## 逐条问题（复核 2）

- **P0：无。**
- **P1-2（必修）：** `visible_refs_omitted` 与 `visible_refs` 取自不同输入包，截断时少报（`planner_package.py:932-942`）。复现：132 方法 + 11 task → `visible_refs` 128、真实丢弃 15，字段报 `4`；最小例（128 方法 + 5 task）字段报 `0` 而实际丢 5。
- **P2-5：** 权威表 kind 区分未钉死（N3）。
- **P2-6：** 截断边界与旁路顺序无断言（N7/N8）。
- **P2-7：** resolution 与 acceptance 优先级无断言（N9）。
- **P2-8：** 新增模型可见顶层字段 `authoritative_refs` 不在 §38「只加五项」清单内。
- **P2-4（沿用，非问题）：** `method_instance` 归属 H1-A。

```text
$ uv run --offline ruff check <两文件>
All checks passed!
$ PYTHONPATH=src uv run --offline pytest .../test_planning_decision_package_v4.py -p no:cacheprovider -q
58 passed in 0.52s
$ PYTHONPATH=src uv run --offline pytest <相关四文件> -p no:cacheprovider -q
331 passed in 9.52s
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3070 passed, 2 skipped in 126.53s (0:02:06)
```

---

# 复核 3（受检 HEAD `881471ccf6605c969e005b1c5d1f83cde227baf9`；只看 `4e5a326..881471c`）

## 结论

复核 2 的 P1-2 与 P2-5/P2-6/P2-7 均已修复；复核 2 记录的 6 个存活变异（N3/N7/N8/N9/F1/F2）全部 KILLED；E1–E5 复跑仍 KILLED；旧协议字节不变。新发现 P1-3（omitted 度量口径未钉死）。

**判定：修后可合（P0 无；P1 一条）。**

## 步骤 1：改动与越界

```text
 plans/llm-native-htn/H1/journal-D.md               | 114 +++++++++++
 .../planning/htn/planner_package.py                |  37 +++-
 .../test_planning_decision_package_v4.py           | 223 +++++++++++++++++++++
```

白名单外为空，**边界通过**。实现：`omitted = len(_sorted_unique_refs(scoped)) - len(refs)`；新增 `_authority_sort_key` 并对旁路行排序（修 P2-6）。

## 步骤 2：复核 2 的 6 个存活变异重跑（全部 KILLED）

| 变异 | 结果 | 尾行 |
|---|---|---|
| N3 索引忽略 kind | KILLED | `1 failed, 65 passed in 0.61s` |
| N7 omitted 用未带旁路的包 | KILLED | `2 failed, 64 passed in 0.57s` |
| N8 旁路行序反转 | KILLED | `1 failed, 65 passed in 0.56s` |
| N9 resolution 优先 | KILLED | `1 failed, 65 passed in 0.57s` |
| F1 omitted 恒 0 | KILLED | `2 failed, 64 passed in 1.36s` |
| F2 只留第一个 binding | KILLED | `2 failed, 64 passed in 0.57s` |

## 步骤 3：新增变异

SURVIVED：C（omitted 计重复）、I（`max(0, raw-refs)`）、D/E/N8b（排序键弱化）。KILLED：B、G、H、J、L。A/K 为等价变异。

## 逐条问题（复核 3）

- **P0：无。**
- **P1-3（必修）：** `visible_refs_omitted`「只计唯一引用」口径无测试钉死。生产路径 `method_library` 与 `applicability` 由同一 registry/open goal 集合生成，同一 method_ref 会重复出现；实测 132 方法库 + 11 条 applicability 重复时唯一引用 143、真实丢弃 15，变异报 26。
- **P2-9：** `_authority_sort_key` 的 hash/kind 分量可弱化而不被发现（D/E/N8b）。
- **P2-10：** 畸形旁路行兜底分支无断言（F）。
- **P2-8（沿用，仍未裁定）：** `authoritative_refs` 字段问题。

```text
$ uv run --offline ruff check <两文件>
All checks passed!
$ PYTHONPATH=src uv run --offline pytest .../test_planning_decision_package_v4.py -p no:cacheprovider -q
66 passed in 0.55s
$ PYTHONPATH=src uv run --offline pytest <相关四文件> -p no:cacheprovider -q
339 passed in 9.61s
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3078 passed, 2 skipped in 133.70s (0:02:13)
```

---

# 复核 4（受检 HEAD `0d9d7fb9868a09ca82607a9cd0981462098f5322`；只看 `881471c..0d9d7fb`）

## 结论

复核 3 的 P1-3 与 P2-10 已修复并钉死；P2-8 按「追加裁定 2026-09-19 06:30」落实（`authoritative_refs` 改当收集器可选入参，`visible_refs_omitted` 离开包体，新协议包顶层**恰好只加 §38 五项**）。但重跑暴露**覆盖回归**：E1（task 权威哈希被改写）在复核 2/3 曾 KILLED，如今 SURVIVED。

**判定：修后可合（P0 无；P1 一条）。**

## 步骤 1：改动与越界

```text
 plans/llm-native-htn/H1/journal-D.md               | 108 ++++--
 .../planning/htn/planner_package.py                | 109 +++---
 .../test_planning_decision_package_v4.py           | 376 ++++++++++++++++-----
```

白名单外为空，**边界通过**。实现：`_authority_index` 改收参数；收集器与 `visible_refs_omitted` 全部新增 `authoritative_refs=()` 关键字参数；`_decision_fields` 只输出 §38 五项。

**§38 恰好五项（实测）：** `added keys = ['decision_limits','planning_protocol','planning_subjects','previous_feedback','visible_refs']`，`moved keys` 为空。**sealed 文本不含** `authoritative_refs` / `visible_refs_omitted`。P2-8 关闭。

## 步骤 2：复核 3 记录的存活变异重跑

I、C、F 全部 KILLED；D / E / N8b SURVIVED（现有输入下等价，属测试缺口）。

## 步骤 3：新增变异

KILLED：P1、P2、P3、P4、P6、P7、P8、T1、T3、T4、T5、E2、E3、E4、E5、N3、N7、N9、F1、F2。
**SURVIVED：P5 / N8（构建器不排序）、T2（`_ref_sort_key` 丢 kind）、E1（task 权威哈希加盐，回归）。** A、K 为等价变异。

## 逐条问题（复核 4）

- **P0：无。**
- **P1-4（覆盖回归）：** 测试总是传调用方 `authoritative_refs`，而 `_authority_index` 对同 `(kind,id)` 后写覆盖，调用方行盖过构建器 `_network_authorities` 的行；**E1 回归为 SURVIVED（73 passed）**。注入 E1 后生产路径（`event_handler` 不传该入参）task 哈希 `62b03a07…` ≠ `binding.content_hash()`，而测试路径仍显示正确。
- **P1-5：** `_ref_sort_key` 的 kind 分量未真正钉死（T2 SURVIVED；现有用例 id 序恰好与 kind 序一致）。
- **P1-6：** `_authority_sort_key` 并列分量与构建器排序无判别性断言（D/E/N8b/P5/N8 SURVIVED）。
- **P2-8：** 已关闭。等价变异 A、K 不计为缺口。

```text
$ uv run --offline ruff check <两文件>
All checks passed!
$ PYTHONPATH=src uv run --offline pytest .../test_planning_decision_package_v4.py -p no:cacheprovider -q
73 passed in 0.55s
$ PYTHONPATH=src uv run --offline pytest <相关四文件> -p no:cacheprovider -q
346 passed in 9.19s
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3085 passed, 2 skipped in 132.84s (0:02:12)
```

---

# 复核 5（受检 HEAD `75be3dfd736262e3293eacec701121cf58a1e945`；只看 `0d9d7fb..75be3df`）

## 结论

复核 4 记录的 4 个存活点（E1 / T2 / D·E·N8b / P5·N8）全部 KILLED，P1-4 / P1-5 / P1-6 均被针对性用例钉死；旧协议字节不变、§38 五字段约束仍成立。新增变异又发现 P1-7（合并去重键 kind 维度）与 P1-8（`_network_authorities` 的 revision 取值）两个测试缺口。

**判定：修后可合（P0 无；P1 两条）。**

## 步骤 1：改动与越界

```text
 plans/llm-native-htn/H1/journal-D.md               |  73 ++++++++++++
 .../planning/htn/planner_package.py                |  54 ++++++++-
 .../test_planning_decision_package_v4.py           | 124 +++++++++++++++++++--
```

白名单外为空，**边界通过**。实现：新增 `_merge_authorities`（构建器行在前、调用方行只补缺、同键去重取首见、调用方行先排序）；`hierarchical_planner_package` 改用该合并；测试改用交叉 id、新增生产路径与重复键用例。

## 步骤 2：复核 4 的存活变异重跑（全部 KILLED）

| 变异 | 结果 | 尾行 |
|---|---|---|
| E1 构建器 task 哈希加盐 | KILLED | `4 failed, 72 passed in 0.62s` |
| T2 `_ref_sort_key` 丢 kind | KILLED | `1 failed, 75 passed in 0.61s` |
| D / D′ 丢 hash（两处排序键） | KILLED | `1 failed, 75 passed in 0.61s` / `0.60s` |
| E′ 只留 hash | KILLED | `1 failed, 75 passed in 0.60s` |
| N8b 调用方行仅按 hash 排序 | KILLED | `1 failed, 75 passed in 0.60s` |
| P5 / N8 构建器不排序 | KILLED | `1 failed, 75 passed in 0.62s` |
| X 调用方行覆盖构建器行 | KILLED | `3 failed, 73 passed in 0.61s` |

## 步骤 3：新增变异

KILLED：A1、A3、A6、A8、A9、A10、B1–B7（共 13 个）。
**SURVIVED：A2（去重键忽略 kind）、A7（revision 写死 1）。** A4、A5 为等价变异。

## 逐条问题（复核 5）

- **P0：无。**
- **P1-7：** `_merge_authorities` 去重键的 kind 维度未钉死（A2 SURVIVED）。改为 `key=("", id)` 后 76 条全过；task 与 obligation 同 id 时 obligation 引用被静默丢弃（实测变异后 `refs` 只剩 `('task','shared')`）。
- **P1-8：** `_network_authorities` 的 `semantic_revision` 取值未钉死（A7 SURVIVED）。现有世界 binding revision 恒为 1；真实系统产出 `rev>1`（`plan_commits.py:1507`），写死会破坏 §17 字节匹配（实测构造 `rev=3` 输出 `1`）。
- **P2-13：** A4/A5 为等价变异（畸形行透传与否被 `_authority_index` 跳过，不可观测）。
- **P2-4 / P2-8（沿用）：** H1-A 归属 / 已关闭。

```text
$ uv run --offline ruff check <两文件>
All checks passed!
$ PYTHONPATH=src uv run --offline pytest .../test_planning_decision_package_v4.py -p no:cacheprovider -q
76 passed in 0.60s
$ PYTHONPATH=src uv run --offline pytest <相关四文件> -p no:cacheprovider -q
349 passed in 9.49s
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3088 passed, 2 skipped in 131.01s (0:02:11)
```

---

# 复核 6（任务书「第 2 轮复核」：`75be3df` → `a5acea1`，同一核验员会话续做，2026-09-19）

**上一轮核验副本 HEAD：** `75be3dfd736262e3293eacec701121cf58a1e945`
**当前核验副本 HEAD：** `a5acea1d5751f38bc21ddde711724ee9f0efc4a9`
**本轮只看：** `75be3df..a5acea1` 之间的改动（`2967b43` test + `85129c7` / `a5acea1` journal）。
**命名说明：** 任务书写「追加到原报告的『## 复核 2』小节」，但本报告此前各轮已占用 `## 复核 2 / 3 / 4 / 5`；为不覆盖既有历史，本轮在同一文件内续写为「复核 6」。

## 复核 6 结论

复核 5 的 **P1-7 / P1-8 已被针对性用例钉死**（对应变异 A2 / A7 本轮 **KILLED**）；本轮改动为**纯测试 + journal，源码零改动**（`git diff 75be3df a5acea1 -- src/...` 为空），故此前各轮的实现不变量全部沿用。旧协议字节不变、§38 恰好五项、sealed 不含旁路、§5.1 生产路径 task 哈希= `binding.content_hash()`、三哈希函数一致、全套通过。

本轮新增变异又发现 **2 个新的存活点（测试缺口 → P1）**：`_network_authorities` 的 `semantic_revision` 若取自**别的来源**（如 `plan_revision + contract_revision`，C4）不被发现；`_ref_sort_key` 的 revision 分量若用 `str()` 而非 `int()`（C7）会让两位数的 revision 按字典序排在前面。另有 3 个等价变异（C3 / C6 / C8）。

**判定：P0 无；P1 两条（新）→ 修后可合。**

## 复核 6 步骤 1：本轮改了什么、是否越界

```text
 plans/llm-native-htn/H1/journal-D.md               |  83 ++++++++++++++
 .../test_planning_decision_package_v4.py           | 127 ++++++++++++++++++++-
```

`git diff --name-only 75be3df a5acea1` 仅这两个文件；**`src/agent_orchestrator/planning/htn/planner_package.py` 零改动**（与 `75be3df` 逐字节相同）。**边界通过。**

测试新增（`2967b43`）：`test_a_caller_obligation_sharing_a_task_id_is_not_dropped`（P1-7，合并侧）、`test_a_task_ref_revision_comes_from_the_binding_even_past_one`（P1-8，rev=3）、自审用例 `test_the_refs_are_sorted_by_id_ahead_of_revision_and_hash` / `..._revision_ahead_of_hash` / `test_refs_differing_only_in_revision_are_not_collapsed` / `..._only_in_id_are_not_collapsed`；`_WideBinding` 增加 `revision` 参数。

## 复核 6 步骤 2：复核 5 记录的存活变异逐个重跑（全部 KILLED）

| 变异（对应复核 5） | 本轮结果 | 尾行 |
|---|---|---|
| A2 `_merge_authorities` 去重键忽略 kind（`("", id)`） | **KILLED** | `1 failed, 81 passed in 0.61s` |
| A7 `_network_authorities` 的 revision 写死为 `1` | **KILLED** | `1 failed, 81 passed in 0.60s` |

杀死它们的分别是 `test_a_caller_obligation_sharing_a_task_id_is_not_dropped`、`test_a_task_ref_revision_comes_from_the_binding_even_past_one`。**复核 5 的 P1-7 / P1-8 确认修复。**

同时复跑 journal §12.4 列出的自审变异（本轮新增用例的对应项），全部 KILLED：

| 变异 | 结果 | 尾行 |
|---|---|---|
| R_drop_kind（`_ref_sort_key` 丢 kind） | KILLED | `1 failed, 81 passed in 0.61s` |
| R_drop_id（丢 id） | KILLED | `2 failed, 80 passed in 0.62s` |
| R_drop_rev（丢 revision） | KILLED | `2 failed, 80 passed in 0.61s` |
| R_drop_hash（丢 hash） | KILLED | `1 failed, 81 passed in 0.61s` |
| D_dedup_drop_id / D_dedup_drop_rev / D_dedup_drop_hash | KILLED | `1 failed, 81 passed`（各） |
| M_drop_kind（合并键丢 kind） | KILLED | `1 failed, 81 passed in 0.64s` |
| M_drop_id（合并键丢 id） | KILLED | `2 failed, 80 passed in 0.61s` |

## 复核 6 步骤 3：本轮新增变异

| # | 变异 | 结果 | 说明 |
|---|---|---|---|
| C1 | 合并键改为 `(kind, id, hash)` | KILLED | `2 failed, 80 passed in 0.61s` |
| C2 | `_network_authorities` 改用 `binding.contract_hash` | KILLED | `4 failed, 78 passed in 0.64s` |
| C3 | 跳过 `contract_revision == 0` 的 binding | SURVIVED | **等价**（rev 0 的 ref 本就被 `_one_ref` 丢弃） |
| **C4** | **`semantic_revision = plan_revision + contract_revision`** | **SURVIVED** | `82 passed in 0.58s`，见 P1-9 |
| C5 | 调用方行对 obligation 也覆盖构建器行 | KILLED | `3 failed, 79 passed in 0.64s` |
| C6 | 合并丢弃构建器侧 obligation 行 | SURVIVED | **等价**（`_network_authorities` 只产出 task 行） |
| **C7** | **`_ref_sort_key` 用 `str(revision)` 而非 `int`** | **SURVIVED** | `82 passed in 0.60s`，见 P1-10 |
| C8 | 去重改为 last-wins | SURVIVED | **等价**（同四元组重复时首末同值） |

每个变异前 `cp planner_package.py /tmp/pp_r6_*_backup.py`，变异后跑本片测试，恢复后 `git status --short` 仅剩本报告文件（干净；未 commit）。

## 复核 6 逐条问题

### P0 阻塞

无。

### P1 必修（按任务书「存活变异 = 测试缺口（P1）」口径）

**P1-9：`_network_authorities` 的 `semantic_revision` 来源未被判别性钉死（C4 SURVIVED）**

- **位置：** `src/agent_orchestrator/planning/htn/planner_package.py:952`（`"semantic_revision": int(binding.contract_revision)`）。
- **现象：** 把该值改为 `int(network.plan_revision) + int(binding.contract_revision)` 后 82 条全过。现有 fixture 世界的 `plan_revision` 恰好为 **0**（实测），故该表达式与 `contract_revision` 数值相同，无法区分。
- **可达性：** 生产世界 `plan_revision > 0`（提交后即非 0），此时该变异会把 ref 的 `semantic_revision` 写成 `plan_revision + binding_revision`，与 `task_semantics.binding_revision` 不一致。
- **可分辨性（实测，变异注入后恢复，构造 `plan_revision=5`）：**
  ```text
  C4 mutant row        : [{'kind':'task','id':'task-000','semantic_revision':6, ...}]
  C4 expected (binding): 1        # 应为 1，变异给出 6
  ```
- **测试缺口证据：** C4 **SURVIVED（82 passed）**——复核 5 新增的 `test_a_task_ref_revision_comes_from_the_binding_even_past_one` 已把 `contract_revision` 抬到 3，但没动 `plan_revision`（仍为 0），故只钉死了「不是常量 1」，未钉死「来源是 binding」。
- **修复方向：** 在 revision 判别用例里同时把 `network.plan_revision` 设为非 0（或让 task/obligation 的 plan_revision 与 binding revision 交叉），断言 ref 的 `semantic_revision == int(binding.contract_revision)`。

**P1-10：`_ref_sort_key` 的 revision 分量类型未被钉死（C7 SURVIVED）**

- **位置：** `src/agent_orchestrator/planning/htn/planner_package.py:783`（`int(ref["semantic_revision"])`）。
- **现象：** 改为 `str(ref["semantic_revision"])` 后 82 条全过。现有 revision 判别用例只用到 1 与 2（同一数量级），`str` 与 `int` 排序结果相同。
- **可分辨性（实测，变异注入后恢复，构造 revision 2 与 10）：**
  ```text
  C7 mutant order: [10, 2]     # 字典序把 "10" 排在 "2" 前
  C7 expected    : [2, 10]
  ```
  包内 `visible_refs` 数组顺序参与 canonical hash（§15），故两位数字 revision 下排序会与规格不符。
- **测试缺口证据：** C7 **SURVIVED（82 passed）**——`test_the_refs_are_sorted_by_revision_ahead_of_hash` 用 v1/v2，`test_a_task_ref_revision_comes_from_the_binding_even_past_one` 只断言单条 ref 的数值，均不覆盖跨数量级的排序。
- **修复方向：** 补一条 revision 跨越 9/10 的排序断言（例如同 `(kind,id)` 下 v2 与 v10 必须升序为 `[2, 10]`）。

### P2 建议

**P2-14（记录以免误报）：C3 / C6 / C8 为等价变异。**
- C3（跳过 rev 0 的 binding）：`_one_ref` 对 `semantic_revision < 1` 本就返回 `None`，跳过与否不可观测；
- C6（合并丢弃构建器侧 obligation 行）：`_network_authorities` 只产出 `kind=task` 行（实测 `builder kinds = ['task']`），无 obligation 可丢；
- C8（去重 last-wins）：现有输入中的重复项都是**完全相同的四元组**，首末同值。
三者不构成测试缺口。

**P2-4（沿用，非问题）：** `method_instance` 缺枚举成员时降级为 method 引用，归属 H1-A。

**P2-8（沿用，已关闭）：** 06:30 裁定「请求包只加 §38 五项」仍成立。

## 复核 6：其它已核对项

| 项 | 本轮结果 |
|---|---|
| 边界（`75be3df..a5acea1`） | 仅测试文件 + journal；**源码零改动**，**通过** |
| 旧协议字节不变 | baseline vs HEAD：stub `801b8e39…`、`p23c-pkg` `a9aa2e7e…`、`p23c-pkg2`、`p23c-shared` 全部一致，键集合未移动，**通过** |
| 新协议顶层键 = 旧键 + 恰好 §38 五项 | 实测 `added keys` 为五项、`moved keys` 为空，**通过** |
| sealed 文本不含 `authoritative_refs` / `visible_refs_omitted` | 实测均 `False`，**通过** |
| §5.1 生产路径 task 哈希 | 实发包 task 哈希 == `binding.content_hash()`；伪造调用方行（`rev=99`、`f…`）无法覆盖构建器行，**通过** |
| 合并语义 | 构建器行在前、同键胜出、调用方只补缺；4 行 24 种排列输出顺序唯一，**通过** |
| 三哈希函数独立实现比对 3 组 | 逐组一致；key 序无关、数组序参与，**通过** |
| ruff | `All checks passed!` |

## 复核 6 命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/planning/htn/planner_package.py tests/orchestrator/full_target/test_planning_decision_package_v4.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_package_v4.py -p no:cacheprovider -q
82 passed in 1.27s

$ PYTHONPATH=src uv run --offline pytest     tests/orchestrator/full_target/test_planning_decision_package_v4.py     tests/orchestrator/full_target/test_planning_decision_contract.py     tests/orchestrator/full_target/test_htn_end_to_end.py     tests/orchestrator/full_target/test_root_review_repair_library.py     -p no:cacheprovider -q
355 passed in 9.38s

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3094 passed, 2 skipped in 129.91s (0:02:09)
```

（`2 skipped` 为既有条件跳过：`test_panda_backend.py` 无 `SH_PANDA_PARSER`、`test_real_provider_hierarchical_smoke.py` 需 `--run-real-provider`。）

## 复核 6：恢复与工作树状态

- 所有变异均以 `/tmp` 副本注入并恢复；恢复后 `git status --short` 仅剩本报告文件，`git diff HEAD` 对 `src/`、`tests/`、`journal-D.md` 为空。
- 未 commit、未 merge、未 push。

---

> 第 1–5 轮（及此前各轮复核）的历史结论：修后可合。最终结论见文末 VERDICT。

---

> 第 1–6 轮（及此前各轮复核）的历史结论：修后可合。最终结论见文末 VERDICT。

---

# 复核 7（任务书「第 2 轮复核」：`a5acea1` → `b77d41c`，同一核验员会话续做，2026-09-19）

**上一轮核验副本 HEAD：** `a5acea1d5751f38bc21ddde711724ee9f0efc4a9`
**当前核验副本 HEAD：** `b77d41ce40d4799fbd2ac806cc44932167f70ec9`
**本轮只看：** `a5acea1..b77d41c` 之间的改动（`b32c9c5` test + `b77d41c` journal）。
**命名说明：** 任务书写「追加到原报告的『## 复核 2』小节」，但本报告已占用 `## 复核 2 / 3 / 4 / 5 / 6`；为不覆盖既有历史，本轮在同一文件内续写为「复核 7」。

## 复核 7 结论

复核 6 的 **P1-9 / P1-10 已被针对性用例钉死**（对应变异 C4 / C7 及其两个同类 sibling 本轮 **KILLED**）。本轮改动为**纯测试 + journal，源码零改动**（`git diff a5acea1 b77d41c -- src/` 为空），此前各轮的实现不变量全部沿用：旧协议字节不变、§38 恰好五项、sealed 不含旁路、§5.1 生产路径 task 哈希 = `binding.content_hash()`、合并语义与调用方行序无关、三哈希函数一致、全套通过。

本轮新增 8 个变异，其中 6 个 KILLED；2 个存活（D2 / D5），经逐一验证**均为等价变异**（不是缺口）。故本轮**无新增 P1/P2 缺口**。

**判定：P0 无；P1 无（前几轮 P1 已全部关闭）→ 可合。**

## 复核 7 步骤 1：本轮改了什么、是否越界

```text
 plans/llm-native-htn/H1/journal-D.md               | 75 ++++++++++++++++++++++
 .../test_planning_decision_package_v4.py           | 73 ++++++++++++++++++++++
```

`git diff --name-only a5acea1 b77d41c` 仅这两个文件；**`src/agent_orchestrator/planning/htn/planner_package.py` 零改动**（与 `a5acea1` 逐字节相同）。**边界通过。**

测试新增（`b32c9c5`）：

- `test_the_refs_are_sorted_by_revision_numerically_not_as_strings`（P1-10 主项：v2 vs v10 必须升序 `[2, 10]`）；
- `test_a_task_ref_revision_is_the_binding_not_the_plan_revision`（P1-9：`plan_revision=5`、binding rev=1 时 ref 必须仍读 1）；
- `test_caller_authority_revisions_sort_numerically_not_as_strings`（P1-10 sibling：`_authority_sort_key` 的 revision 也须数值比较，rev 2 vs 10 应取 2）。

## 复核 7 步骤 2：复核 6 记录的存活变异逐个重跑（全部 KILLED）

| 变异（对应复核 6） | 本轮结果 | 尾行 |
|---|---|---|
| C4 `semantic_revision = plan_revision + contract_revision` | **KILLED** | `1 failed, 84 passed in 0.63s` |
| C4b `semantic_revision = plan_revision`（同类更激进写法） | **KILLED** | `10 failed, 75 passed in 0.67s` |
| C7 `_ref_sort_key` 用 `str(revision)` | **KILLED** | `1 failed, 84 passed in 0.62s` |
| C7b `_authority_sort_key` 用 `str(revision)`（sibling） | **KILLED** | `1 failed, 84 passed in 0.62s` |

杀死它们的分别是 `test_a_task_ref_revision_is_the_binding_not_the_plan_revision`、`test_the_refs_are_sorted_by_revision_numerically_not_as_strings` 与 `test_caller_authority_revisions_sort_numerically_not_as_strings`。**复核 6 的 P1-9 / P1-10 确认修复。**

复核 6 记录为等价的 C3 / C6 / C8 复跑仍 SURVIVED（`85 passed`），与复核 6 判定一致。

## 复核 7 步骤 3：本轮新增变异

| # | 变异 | 结果 | 说明 |
|---|---|---|---|
| D1 | `_ref_sort_key` 改为 id 先于 kind | KILLED | `1 failed, 84 passed in 0.60s` |
| **D2** | `_authority_sort_key` 改为 id 先于 kind | SURVIVED | `85 passed in 0.59s`，**等价**（见 P2-15） |
| D3 | `_merge_authorities` 构建器重复行改为 last-wins | KILLED | `3 failed, 82 passed in 0.66s` |
| D4 | 调用方行可覆盖构建器行 | KILLED | `4 failed, 81 passed in 0.64s` |
| **D5** | `_network_authorities` 按 id 去重 | SURVIVED | `85 passed in 0.59s`，**等价**（见 P2-16） |
| D6 | `_sorted_unique_refs` 改为降序排序 | KILLED | `9 failed, 76 passed in 0.68s` |
| D7 | 去掉 `MAX_VISIBLE_REFS` 截断 | KILLED | `3 failed, 82 passed in 0.63s` |
| D8 | 调用方行改为降序排序 | KILLED | `2 failed, 83 passed in 0.62s` |

每个变异前 `cp planner_package.py /tmp/pp_r7_*_backup.py`，变异后跑本片测试，恢复后 `git status --short` 仅剩本报告文件（干净；未 commit）。

## 复核 7 逐条问题

### P0 阻塞

无。

### P1 必修

无。复核 1–6 记录的 P1-1…P1-10 已全部关闭（各自被对应轮次的新用例钉死，且本轮复跑的 C4/C7 再次确认）。

### P2 建议（等价变异备案，非缺口）

**P2-15：D2（`_authority_sort_key` 改为 id 先于 kind）为等价变异。**
该键只决定 `_merge_authorities` 内部两行排序与重复 `(kind,id)` 的胜者。能产生并列的只有 **kind 与 id 都相等**的行，此时交换 kind/id 次序不影响比较结果；而 `visible_refs` 的最终顺序由 `_ref_sort_key` 重新排序。实测 3 行权威行的 6 种排列，发射出的 ref 顺序唯一：`{(('obligation','a'),('task','a'),('task','z'))}`。故不构成测试缺口。

**P2-16：D5（`_network_authorities` 按 id 去重）为等价变异。**
`TaskNetworkSnapshot.__post_init__` 对重复 `task_id` 的 binding 直接抛 `ContractError`（`graph/task_network.py:394`），故此函数输出天然按 `(kind,id)` 唯一；即便出现重复，也是**同一 binding 的同一四元组**，去重与否不可观测。故不构成测试缺口。

**P2-4（沿用，非问题）：** `method_instance` 缺枚举成员时降级为 method 引用，归属 H1-A。
**P2-8（沿用，已关闭）：** 06:30 裁定「请求包只加 §38 五项」仍成立。

## 复核 7：其它已核对项

| 项 | 本轮结果 |
|---|---|
| 边界（`a5acea1..b77d41c`） | 仅测试文件 + journal；**源码零改动**，**通过** |
| 旧协议字节不变 | baseline vs HEAD：stub `801b8e39…`、`p23c-pkg` `a9aa2e7e…`、`p23c-pkg2`、`p23c-shared` 全部一致，键集合未移动，**通过** |
| 新协议顶层键 = 旧键 + 恰好 §38 五项 | 实测 `added keys` 为五项、`moved keys` 为空，**通过** |
| sealed 文本不含 `authoritative_refs` / `visible_refs_omitted` | 实测均 `False`，**通过** |
| §5.1 生产路径 task 哈希 | 实发包 task 哈希 == `binding.content_hash()`；伪造调用方行（`rev=99`、`f…`）无法覆盖构建器行，**通过** |
| 合并语义与序无关性 | 构建器行在前、同键胜出、调用方只补缺；4 行 24 种排列输出顺序唯一，**通过** |
| 排序 revision 数值化 | `[2, 10]`（非 `[10, 2]`），**通过** |
| 三哈希函数独立实现比对 3 组 | 逐组一致；key 序无关、数组序参与，**通过** |
| ruff | `All checks passed!` |

## 复核 7 命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/planning/htn/planner_package.py tests/orchestrator/full_target/test_planning_decision_package_v4.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_package_v4.py -p no:cacheprovider -q
85 passed in 1.36s

$ PYTHONPATH=src uv run --offline pytest \
    tests/orchestrator/full_target/test_planning_decision_package_v4.py \
    tests/orchestrator/full_target/test_planning_decision_contract.py \
    tests/orchestrator/full_target/test_htn_end_to_end.py \
    tests/orchestrator/full_target/test_root_review_repair_library.py \
    -p no:cacheprovider -q
358 passed in 9.78s

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3097 passed, 2 skipped in 132.56s (0:02:12)
```

（`2 skipped` 为既有条件跳过：`test_panda_backend.py` 无 `SH_PANDA_PARSER`、`test_real_provider_hierarchical_smoke.py` 需 `--run-real-provider`。）

## 复核 7：恢复与工作树状态

- 所有变异均以 `/tmp` 副本注入并恢复；恢复后 `git status --short` 仅剩本报告文件，`git diff HEAD` 对 `src/`、`tests/`、`journal-D.md` 为空。
- 未 commit、未 merge、未 push。

---

VERDICT: 可合
