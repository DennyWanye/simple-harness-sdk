# 核验报告：切片 H1-E（提示词 v8、配对、冻结指纹）

- 核验日期：2026-09-19
- 核验对象：`58baef4`（`feat(h1-e): planner-hierarchical-v8 prompt, package-4 pairing and frozen digests`）
- 基准：`b13e757`（`docs(h1-a1): archive independent verification ...`）
- SDK 版本：`HIERARCHICAL_PLANNER_PACKAGE_VERSION` 仍为 3；`PLANNING_DECISION_PACKAGE_VERSION` 为 4
- 源码定位核对：`PYTHONPATH=src uv run --offline python -c "import agent_orchestrator; ..."` 解析到
  `<worktree>/src/agent_orchestrator/__init__.py`，即当前目录内，未串到别处。
- 权威规格（冲突时后者优先）：
  - V2 `…/v1.4/simpleharness-llm-native-htn-execution-plan-v2.zh-CN.md`
  - 补遗 `…/v1.4/LLM-native-HTN计划V2-裁定补遗-2026-09-18.zh-CN.md`
  - 源码定位参考 `…/v1.4/H1-V2对照源码冲突检查-2026-09-18.zh-CN.md`（§1 符号对照表、§5.1 引用来源表）
- 完整 diff 见 §2；本片新增/改动清单：`journal-E.md`、`prompt-v8.md`、`role_templates.py`、
  `test_output_port_claims.py`、`test_planning_decision_prompt_v8.py`、`test_root_review_user_goal.py`。

## 结论

**VERDICT: 可合。**

逐项核验全部通过：旧模板 sha256 逐一相等、`HIERARCHICAL_PLANNER_PACKAGE_VERSION` 仍为 3、
包 1–3 映射逐字节不变；v8 覆盖 V2 §41 十二要点、§32 禁止字段零差集、示例 JSON 字段名不越界、
未诱导 H1 未启用类型；配对正反例（v8↔包4 真、包4↔v7 假、包3↔v8 假）全部成立；既有测试改动
仅追加；`prompt-v8.md` 与代码文本逐字一致；6 个规定变异全部被杀死，另加 15 个自发变异亦全部被
杀死，无存活变异（无 P1）。相关测试 62 passed / 0 failed，`ruff` All checks passed，工作树干净。

仅 2 条 P2 观察（不阻塞合并），见 §5。

---

## 1. 核验方法

全部结论由脚本或测试产出，代理只负责汇总。脚本位于 `/tmp/h1e-verify/`（可丢弃，不随片提交）：

- `all_checks.py`：A–G 七组确定性检查（旧模板 sha256、§41 要点、§32 差集、示例字段差集、
  逐字比对、包 1–3 映射、未启用类型诱导）。
- `run_mutations.py` / `probe*.py`：变异核验；每个变异前把原文件复制到 `/tmp/h1e-verify/orig/`，
  变异后跑相关测试，随后用副本恢复并以 `git status --short` 确认干净。
- `check_digests.py`、`check_v8_doc.py`、`check_forbidden_fields.py`、`check_example_union.py`、
  `check_pkg_mapping.py` 为分解脚本。

关键命令：

```
PYTHONPATH=src uv run --offline pytest \
  tests/orchestrator/full_target/test_planning_decision_prompt_v8.py \
  tests/orchestrator/full_target/test_output_port_claims.py \
  tests/orchestrator/full_target/test_root_review_user_goal.py -q -p no:cacheprovider
```

---

## 2. 完整 diff

基准 `b13e757` → 对象 `58baef4`：

```
 plans/llm-native-htn/H1/journal-E.md               | 183 +++++++++++++++
 plans/llm-native-htn/H1/prompt-v8.md               | 108 +++++++++
 src/agent_orchestrator/runtime/role_templates.py   |  99 ++++++++
 .../full_target/test_output_port_claims.py         |  15 ++
 .../full_target/test_planning_decision_prompt_v8.py| 249 +++++++++++++++++++++
 .../full_target/test_root_review_user_goal.py      |  11 +
 6 files changed, 665 insertions(+)
```

- `role_templates.py`：`git diff --numstat = 99 0`，即**纯追加，无删除行**。新增
  `PLANNER_HIERARCHICAL_V8`（:694）、`PLANNING_DECISION_PACKAGE_VERSION = 4`（:790）、
  包 4 映射（:818）、`hierarchical_planner_pairing_is_valid`（:831）、模式级集合与 `__all__` 追加。
- 两个既有测试文件：`15 0`、`11 0`，**仅追加**（见 §6）。
- 三个新文件：`journal-E.md`（日志）、`prompt-v8.md`（提示词全文 + 设计说明）、
  `test_planning_decision_prompt_v8.py`（10 用例）。

---

## 3. 逐项核验

### 3.1 越界与旧文本不变（任务 1）

脚本 `all_checks.py [A]/[F]`：

- 对 `plans/llm-native-htn/H0/prompt-digests.json` 的 `prompt_shas` 全部 29 个旧模板重算
  sha256：`checked=29 mismatches=0 -> ALL EQUAL`。
- `HIERARCHICAL_PLANNER_PACKAGE_VERSION = 3`（未变）；`PLANNING_DECISION_PACKAGE_VERSION = 4`（新增）。
- 包 1–3 映射：把基线 `b13e757` 与本对象在该区段做 diff，**删除行 = []**，新增行仅为
  package 4 的四行（三行注释 + 一行映射）。即 1→{v1,v2}、2→{v3,v4}、3→{v5,v6,v7} 逐字节不变。

### 3.2 v8 对 V2 §41 十二要点（任务 2）

`all_checks.py [B]`：从规格 §41 文本抽取 12 条要点，逐条在 `PLANNER_HIERARCHICAL_V8.instructions`
里匹配关键短语，全部命中（`十二条全部命中: True`）。要点与提示词原句：

- [x] 1. hierarchical Planner，只提出 Decision — 原句：「你是编排系统在**层次模式（hierarchical）**下的 Planner … 一轮回复里**只提出一个决定**」
- [x] 2. 只输出一个 `<planning_decision>` — 原句：「只输出一个 `<planning_decision>…</planning_decision>` 块，块内是一个 JSON 对象」
- [x] 3. 类型只能从 package enabled_decision_types — 原句：「decision_type 只能取请求包 planning_protocol.**enabled_decision_types** 里列出的值」
- [x] 4. subject_key 只照抄 — 原句：「**subject_key 照抄**请求包里给你的 subject_key，不要改写、不要自己编」
- [x] 5. refs 只从 visible_refs 完整照抄 — 原句：「每条引用都必须从请求包的 **visible_refs** 里完整照抄**四元组**」
- [x] 6. 禁止系统字段 — 原句：「**禁止系统字段**：以下字段由系统绑定 … 只要出现就会被整块拒绝」
- [x] 7. 不能 REQUEST_EVIDENCE，证明不了就 BLOCKED/NO_CHANGE — 原句：「**REQUEST_EVIDENCE**（以及 REQUEST_HUMAN、PROPOSE_METHOD）不在本阶段 enabled_decision_types 里，你不要写 … 就改成 **DECLARE_BLOCKED** … 或 … **NO_CHANGE**」
- [x] 8. rejected_refinements 用一个 REPAIR/REPLACE_METHOD — 原句：「若输入里 **rejected_refinements** 非空 … 用一个 **REPAIR** 决定表达修复，payload.repair_kind = "**REPLACE_METHOD**" … 不要拆成两个顶层决定」
- [x] 9. sharing 用 BIND_EXISTING_GOAL — 原句：「**BIND_EXISTING_GOAL**：把一个已有目标共享/复用到某个方法槽位」
- [x] 10. no method 用 DECLARE_BLOCKED，让系统决定 synthesis — 原句：「**DECLARE_BLOCKED**：当你找不到任何可用方法 … 系统据此决定是否进入方法**合成**轮」
- [x] 11. 不输出内部 CoT — 原句：「不要在回复里写出内部**思维链**（CoT）：只给最终决定与理由」
- [x] 12. block 外禁止文字 — 原句：「**块外不要输出任何文字**，不要写解释、标题或 Markdown 代码围栏」

### 3.3 禁止字段与 §32 求差集（任务 2）

`all_checks.py [C]`：从 V2 §32 代码块抽取 23 个禁止字段，从提示词「禁止系统字段」段抽取 token，
`spec - prompt = []`、`prompt - spec（含下划线） = []`，**零差集**。

§32 清单：`mission_id tenant_id principal principal_id scope scope_id manager_epoch budget_account
budget_grant_revision registry_status opened_by authorization_ref grant_ref provenance authored_by
dispatch_generation plan_revision expected_plan_revision operation_id acceptance_id approval_id
decision_id request_id`（23 项，提示词全列）。

### 3.4 示例 JSON 字段名与 §13、§24–30 求差集（任务 2）

`all_checks.py [D]`：示例为 `REFINE` 的一行 JSON。字段名与规格并集比较：

- `示例中不在规格并集的字段 = []`（无越界字段）。
- 规格并集中未出现在示例的字段（`blockers/code/.../wait_for` 等 18 项）属其它决定类型的 payload，
  示例只承诺覆盖 §13 骨架 + §24 的 REFINE payload，故属正常。

另单独比对：示例核心字段集合 == §13 骨架字段集合（10 项逐一相等）；示例 `payload` 键集合 == §24
`payload` 键集合（`{method_ref, bindings}`）。示例的 `method_ref` 可被已合入的
`contracts.planning_decisions.PlanningRefV1.from_json` 接受并 round-trip 相等。

### 3.5 未诱导 H1 未启用类型（任务 2）

`all_checks.py [G]`：`REQUEST_EVIDENCE / REQUEST_HUMAN / PROPOSE_METHOD` 各只出现 **1 次**，且都
位于同一句「本阶段不能请求取证：…不在本阶段 enabled_decision_types 里，你不要写」内——是**禁止**语境，
不是诱导。另确认提示词不含旧标签/旧类型词：
`<plan_revision_proposal>=0`、`<method_proposal>=0`、`SELECT_METHOD=0`、`RETIRE_METHOD_USE=0`、
`REUSE_RESULT=0`、`no_applicable_method=0`、`<task_graph_proposal>=0`。

### 3.6 配对（任务 3）

由 `test_planning_decision_prompt_v8.py::test_v8_pairs_only_with_package_four_and_package_four_refuses_v7`
及 `::test_package_four_is_new_and_the_default_package_stays_three` 断言（测试全绿）：

- `hierarchical_planner_pairing_is_valid("planner-hierarchical-v8", 4) is True`（v8↔包 4 正例）
- `hierarchical_planner_pairing_is_valid("planner-hierarchical-v7", 4) is False`（包 4 配 v7 **被拒**）
- `hierarchical_planner_pairing_is_valid("planner-hierarchical-v8", 3) is False`（包 3 配 v8 **被拒**）
- `HIERARCHICAL_PLANNER_VERSIONS_BY_PACKAGE[4] == frozenset({"planner-hierarchical-v8"})`
- 默认 `HIERARCHICAL_PLANNER_PACKAGE_VERSION == 3`，`hierarchical_planner_versions()` 仍返回
  `{v5,v6,v7}`（既有 `test_htn_end_to_end.py:1229` 的相等断言仍通过）。

说明：把配对接到 `_hierarchical_planner_template` 的实际派发属于 H1-H 接线片，不在 H1-E 范围；
本片交付的是可测的配对判定函数与映射，符合 §9「旧 1–3 映射逐字节不动 + 新增包 4」。

### 3.7 既有测试改动仅追加（任务 4）

`git diff --numstat b13e757 58baef4`：`test_output_port_claims.py 15 0`、
`test_root_review_user_goal.py 11 0`——新增 0 删除，**只追加**。

- `test_output_port_claims.py`：`FROZEN_PROMPT_DIGESTS` 追加 `PLANNER_HIERARCHICAL_V1`（v1）、
  `PLANNER_HIERARCHICAL`（v2）、`PLANNER_HIERARCHICAL_V8` 三行（补齐 H0 前两处缺口 + 登记 v8）。
- `test_root_review_user_goal.py`：新增 `FROZEN_ROOT_REVIEWER_V3` 字面量并在既有用例内追加一条断言
  （补齐 H0 第三处缺口）。

### 3.8 prompt-v8.md 与代码逐字一致（任务 5）

`all_checks.py [E]` / `check_v8_doc.py`：从 `prompt-v8.md` 的第一个 ```text 块抽取文本，与
`PLANNER_HIERARCHICAL_V8.instructions` 比较，`doc==code: True`。代码文本 sha256 =
`dba73c4f583256ff36147d59a936aef48f3d1392797cebeba468142417e85ada`，与 `FROZEN_PROMPT_DIGESTS`
登记值一致。

### 3.9 冻结指纹收口（任务 2/5）

- H0 记录的三处缺口全部关闭（`test_the_three_h0_freeze_gaps_are_now_closed` 通过）：
  v1、v2 进 `FROZEN_PROMPT_DIGESTS`；`ROOT_REVIEWER` 现文本有 `FROZEN_ROOT_REVIEWER_V3` 字面量。
- v8 自身进冻结表（`test_v8_itself_is_in_the_frozen_table_with_its_digest` 通过）。

---

## 4. 变异表（任务 6）

规则：变异前把原文件复制到 `/tmp/h1e-verify/orig/`；变异后相关测试**至少 1 条失败**即 KILLED；
随后用副本恢复并以 `git status --short` 确认干净。相关测试三文件（v8 提示词 10 用例 +
`test_output_port_claims` 44 用例 + `test_root_review_user_goal` 8 用例）。

### 4.1 任务规定的 6 个变异

| # | 变异 | 结果 | 失败数 |
|---|---|---|---|
| M1 | 删掉提示词中「只输出一个 `<planning_decision>` 块」句 | KILLED | 4 failed, 58 passed |
| M2 | 删禁止字段清单中的一项（`mission_id`） | KILLED | 4 failed, 58 passed |
| M3 | 把包 4 映射加上 v7 | KILLED | 2 failed, 60 passed |
| M4 | 改动一个旧模板（v7）的一个字符 | KILLED | 2 failed, 60 passed |
| M5 | 移除 v8 的冻结登记（`FROZEN_PROMPT_DIGESTS["PLANNER_HIERARCHICAL_V8"]`） | KILLED | 1 failed, 60 passed |
| M6 | 把默认包版本改成 4（`HIERARCHICAL_PLANNER_PACKAGE_VERSION = 4`） | KILLED | 1 failed, 61 passed |

M4 失败证据（原样）：

```
E           AssertionError: planner-hierarchical-v7 changed its bytes; a shipped prompt is never edited in place
E           assert '8292c620b7ef...7bd8e842ea461' == '5b87b9624fbf...427676cb50f15'
FAILED tests/orchestrator/full_target/test_planning_decision_prompt_v8.py::test_every_existing_prompt_still_matches_the_h0_recorded_digest
FAILED tests/orchestrator/full_target/test_output_port_claims.py::test_a_shipped_prompt_keeps_its_bytes[PLANNER_HIERARCHICAL_V7]
```

### 4.2 追加自发变异（用于寻找存活变异 = P1 测试缺口）

| # | 变异 | 结果 | 失败数 |
|---|---|---|---|
| M7 | 把要点 7 由「不能请求取证」翻成「可以/应该请求取证」 | KILLED | 3 failed, 59 passed |
| M8 | 去掉 subject_key「不要改写/新编」禁令 | KILLED | 3 failed, 59 passed |
| M9 | 删掉禁止字段的「整块拒绝」后果 | KILLED | 3 failed, 59 passed |
| M10 | 把 REPAIR 反拆句改成「可以拆两个顶层决定」 | KILLED | 3 failed, 59 passed |
| M11 | 删掉「块外不要输出任何文字」 | KILLED | 4 failed, 58 passed |
| M12 | 示例 `decision_type` 改成非 H1 类型 `PROPOSE_METHOD` | KILLED | 3 failed, 59 passed |
| M13 | 示例字段 `method_ref` 改名 `ref` | KILLED | 4 failed, 58 passed |
| S1 | 删要点 7 的 BLOCKED/NO_CHANGE 退路 | KILLED | 3 failed, 59 passed |
| S4 | 删要点 1 的「只提出一个决定」 | KILLED | 4 failed, 58 passed |
| S5 | 删「只给最终决定与理由」 | KILLED | 3 failed, 59 passed |
| P1 | 要点 7 极性翻转但保留全部断言短语 | KILLED | 3 failed, 59 passed |
| P3 | 示例 `decision_type` 改成 H1 内另一类型 `DECLARE_BLOCKED` | KILLED | 3 failed, 59 passed |
| P4 | 删 BIND_EXISTING_GOAL 的 mode/goal_ref/resolution_ref 说明 | KILLED | 3 failed, 59 passed |
| X1 | 只改 `prompt-v8.md`（代码不动） | KILLED | 1 failed, 61 passed |
| X2 | 冻结表 v8 摘要改一个字符 | KILLED | 2 failed, 60 passed |
| X3 | 整体删除包 4 映射 | KILLED | 2 failed, 60 passed |
| X4 | 取消注册 v8（删 `register_template(PLANNER_HIERARCHICAL_V8)`） | KILLED | 1 failed, 61 passed |

**存活变异 = 0**，无 P1。

---

## 5. 问题清单

### P0

无。

### P1

无。

### P2（观察项，不阻塞合并）

1. **BIND_EXISTING_GOAL 的 payload 说明不完整。** `role_templates.py:731-732` 的 v8 提示词只写
   「payload 里给出 mode 与被复用的 goal_ref / resolution_ref」，未点名 §27 载荷的
   `consumer_method_instance_ref` 与 `step` 两个必填键。§41 要点 9 只要求「sharing 用
   BIND_EXISTING_GOAL」，名义上已满足；且完整的 `additionalProperties:false` schema 将随 H1-D/F
   下发给模型，故不构成本片缺陷。但本片是当前唯一落地的 v8 提示词，建议在 H1-F/H1-D 落地时确认
   模型能从请求包/schema 得到这两个键名，或后续 v9 补齐描述。同类轻微省略还有 REPAIR 未点
   `bindings`、DECLARE_BLOCKED 未点 `resumable_if`。
2. **注释前向引用未实现的字符串标签。** `role_templates.py:786` 的注释称包 4 的包内字符串标签
   为 `planner-package-hierarchical-v5`，但该常量在本片未新增（属 H1-D）。纯注释，不影响 wire 与
   测试；若 H1-D 命名有变，此注释需同步。同理 §41 的 pair 串
   `planner-hierarchical-v8 <-> package4 <-> planning-decision-v1` 未逐字出现在提示词正文里
   （§41 是「必含段落」清单，pair 行是对外说明而非提示词段落），核验判为不构成缺项。

---

## 6. 命令尾行原文（任务 7）

相关测试（三文件，62 用例）：

```
PYTHONPATH=src uv run --offline pytest \
  tests/orchestrator/full_target/test_planning_decision_prompt_v8.py \
  tests/orchestrator/full_target/test_output_port_claims.py \
  tests/orchestrator/full_target/test_root_review_user_goal.py -q -p no:cacheprovider
..............................................................           [100%]
62 passed in 0.84s
```

全量 `full_target` 回归（本片相关面）：

```
PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -q -p no:cacheprovider
3025 passed, 2 skipped in 135.90s (0:02:15)
```

`ruff`（本片 4 个改动源码/测试文件）：

```
PYTHONPATH=src uv run --offline ruff check \
  src/agent_orchestrator/runtime/role_templates.py \
  tests/orchestrator/full_target/test_planning_decision_prompt_v8.py \
  tests/orchestrator/full_target/test_output_port_claims.py \
  tests/orchestrator/full_target/test_root_review_user_goal.py
All checks passed!
```

工作树：

```
git status --short
(空 —— 干净)
```

---

## 7. 结果

- 变更文件：`plans/llm-native-htn/H1/journal-E.md`、`plans/llm-native-htn/H1/prompt-v8.md`、
  `src/agent_orchestrator/runtime/role_templates.py`、
  `tests/orchestrator/full_target/test_planning_decision_prompt_v8.py`、
  `tests/orchestrator/full_target/test_output_port_claims.py`、
  `tests/orchestrator/full_target/test_root_review_user_goal.py`（后两者仅追加）。
- 测试：相关 62 passed / 0 failed；`full_target` 3025 passed / 2 skipped / 0 failed；`ruff` 通过。
- 变异：规定 6 个 + 自发 11 个全部 KILLED，存活 0。
- 问题：P0 = 0，P1 = 0，P2 = 2（观察项）。


---

## 编排者附记（2026-09-19 06:25）

流水线首次全量检查 RED：旧模式回归 559/560，唯一失败 `tests/orchestrator/step02/test_live_provider_progress.py::test_live_worker_progress_prevents_false_stall_without_new_attempt`（asyncio 超时）。当时本机同时有三条流水线在跑测试，属计时类测试在高负载下超时；该测试与本片改动（提示词文本/版本配对/指纹登记）无关。单独重跑：本分支 3 次、主干 1 次，全部 `1 passed`。首次报告留档为 `../gates/h1-e-gate-full-first-run.json`；合入主干后在主干上重跑完整检查，报告为 `../gates/h1-e-merged-main-gate-full.json`。
