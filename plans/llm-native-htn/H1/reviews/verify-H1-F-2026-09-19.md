# 独立核验报告：切片 H1-F（准入检查）

**核验对象：** 切片 H1-F，基准 `0d89307`，被核验 HEAD `5804dc3`
（`202907b` 红测试 → `8f861ca` 实现 → `5804dc3` journal 收尾）。
**工作目录：** `/Users/taiwan/PROJECTS/SimplaHarness/simple-harness-sdk-h1f-verify-h1-f`
（分离副本；`agent_orchestrator.__file__` = 本目录 `src/agent_orchestrator/__init__.py`，已确认）。
**核验员立场：** 只看代码与证据；不 commit、不读密钥文件、未运行 `git stash/checkout/reset/rebase/push`。所有变异前把原文件复制到 `/tmp/h1f-verify/orig/`，变异后用副本恢复并以 `git status --short` 确认干净。
**权威规格（冲突时后者优先）：** V2 `simpleharness-llm-native-htn-execution-plan-v2.zh-CN.md`（第 12、16–19、24–34、39、40、43 节）与补遗 `LLM-native-HTN计划V2-裁定补遗-2026-09-18.zh-CN.md`（含 2026-09-19 06:30 追加裁定：引用以请求记录保存的清单为准，不从包体重算）。源码定位参考 `H1-V2对照源码冲突检查-2026-09-18.zh-CN.md`。可执行类型以 `H1_DECISION_ENABLEMENT` 与 V2 第 12 节为准。

> 第 1 轮（受检 HEAD `5804dc3`）。

---

## 结论

边界干净（相对 `0d89307` 只新增 3 个白名单文件）、模块 import 图无存储/事件/编译/提交、§33/§32/§12 差集为空、§43 所列阶段相对顺序保持、6 组双阶段只报先到、31 个 H1-F 夹具全过、引用四元组与大小写/类型错均拒、反馈不泄漏 §32 字段名或内部 id。规定 12 个变异中 11 个被杀死。

**但有 3 条 P1、无 P0：** 空启用集 / 空 bindings 会把本应返回的拒绝打成 `ContractError`；「空清单放行」与「方法身份只比 version 不比 hash」两个可分辨变异在现有 132 条测试上存活。

**判定：修后可合（P0 无；P1 三条）。**

---

## 1. 逐条问题（P0/P1/P2）

### P1-1 — 空集合拼进 `expected`/`observed` 时准入抛 `ContractError`，不返回 `PlanningFeedbackV1`

- 位置：
  - `src/agent_orchestrator/planning/decision_admission.py:797`（阶段闸门 `expected=", ".join(sorted(enabled))`）
  - 同文件 `:1037`（参数检查 `observed=", ".join(sorted(bindings))`）
  - 契约层 `contracts/planning_decisions.py:521` → `semantic_base.text` 拒空白。
- 事实：`PlanningProblemDetailV1.expected` / `observed` 是 `str | None`，空串不是合法值。启用集为空、或方法必填参数存在但 `bindings={}` 时，准入已经决定拒绝，却在构造问题对象时崩掉。
- 复现：

```text
# 启用集为空（刁钻输入 6）
PYTHONPATH=src uv run --offline python -c "
from tests helper: admit(refine, enabled_decision_types=frozenset())
"
ContractError: problem.expected must not be blank
# 期望：DECISION_NOT_ENABLED_IN_PHASE

# 空 bindings + 方法要求 target（现实模型可输出）
refine.json 的 payload.bindings = {}
ContractError: problem.observed must not be blank
# 期望：PARAMETER_INVALID
```

- 为何是 P1 而非 P0：拒绝逻辑本身走的是对的码；崩在反馈构造。H1 包的启用集正常非空，现有 `parameter-invalid` 夹具仍带 `target` 键所以测不到。须把空串改成 `None`（§40 允许）或非空占位，并补两条用例。

### P1-2 — 变异「空 visible_refs 直接放行」存活（测试缺口）

- 位置：`decision_admission.py:765-775`（`_check_visible_refs`）；测试从未构造空清单。
- 事实：原实现对 REFINE + `visible_refs=()` 返回 `REF_OUTSIDE_CONTEXT`。把检查改成「清单为空则 return」后，同一输入变成 `AdmittedPlanningDecision`，targeted 仍 `132 passed`。
- 复现：

```text
# 原版
REFINE + visible_refs=() -> REF_OUTSIDE_CONTEXT
# M11：if not visible: return
REFINE + visible_refs=() -> AdmittedPlanningDecision
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py -q -p no:cacheprovider
132 passed in 0.20s
```

- 行为可分辨，不是等价变异。须补「清单为空且决定引用了四元组 → `REF_OUTSIDE_CONTEXT`」（NO_CHANGE 无引用被放行是对的，须区分）。

### P1-3 — 变异「方法身份只比 version、不比 content_hash」存活（测试缺口）

- 位置：`decision_admission.py:996-1008`（`_check_one_method` 的 version+hash 联合比较）。
- 事实：原实现对「version 相同、hash 不同」返回 `METHOD_STALE`。改成只比 `version` 后同一输入被准入，targeted 仍全绿。`method-stale.json` 只把 `semantic_revision` 改成 1（与库内 2 不同），从未单独钉 hash。引用阶段的「hash 改一个字符」走的是 `REF_OUTSIDE_CONTEXT`，到不了方法身份阶段。
- 复现：

```text
# 原版
library (id=code.fix-by-patch, version=2, hash=HASH_B) vs 决定 (version=2, hash=HASH_A)
-> METHOD_STALE
# M13：if view.version != method_ref.semantic_revision
-> AdmittedPlanningDecision
$ ... pytest test_planning_decision_admission.py -q -p no:cacheprovider
132 passed in 0.21s
```

- §24 方法身份是 version **与** hash。须补「版本对、哈希不对 → `METHOD_STALE`」，且 `expected`/`observed` 应能区分 hash（现状两边都写 version，见 P2-4）。

### P2-1 — 上下文类错误的 JSON 指针在决定 JSON 上解析不到

`PACKAGE_HASH_MISMATCH` 用 `/package_hash`，`REQUEST_BINDING_STALE` 用 `/request_binding/plan`，对 `decision.to_json()` 做 RFC 6901 解析均 `MISSING`。`INTERNAL_CONTRACT_ERROR` 的 `field_path=None`。§40 允许 `field_path` 为 `None`；这三类错误不在模型 JSON 里，用真名 `plan_revision` 又会触 §32。其余 25 条夹具指针均可解析。不阻塞。

### P2-2 — 实现在 §43 列表之外插入了预算阶段与结构前置阶段

§43 原文顺序：request identity → package/prompt binding → plan revision → subject → visible refs → phase enable → payload checks → method/… → operation gate → typed admitted command。

实现在 phase enable 与 payload 之间插入 `_check_budget_and_bound`，在 operation gate 之后插入 `_check_plan_shape`。所列阶段的相对顺序未乱；这两个码属于 §33，双阶段探测里「预算耗尽 + 修复不允许」报的是先到的 `BUDGET_INSUFFICIENT`。属扩展而非颠倒，备案。

### P2-3 — `ADMISSION_CODE_CASES` 有一个不存在的用例名

`"DECISION_NOT_ENABLED_IN_PHASE": "test_a_decode_only_type_is_phase_refused"`，仓库里没有这个函数（真名是 `test_every_decode_only_valid_fixture_is_phase_refused`）。因夹具 `decision-not-enabled-in-phase` 已覆盖，元测试 `code in fixture_codes or def test_name` 仍绿。journal 表本身指向的两个真函数都断言了该码。

### P2-4 — `METHOD_STALE` 的 expected/observed 只回写 version

`:1005-1006` `expected=str(view.version)` / `observed=str(method_ref.semantic_revision)`。哈希失配时两边都是 `"2"`，模型看不到差在哪。随 P1-3 一起修。

### P2-5 — `goal_type_ref` 不按类型注册表校验；BIND 的消费实例不校验是否活动

补遗 §一：「`goal_type_ref` 不进 `visible_refs`，按类型注册表校验」。`AdmissionContext` 无类型注册表字段，伪造 `goal_type_ref` 仍被准入。§43 / 实施任务最小集未列此项，编译期也可能接住，备案。`consumer_method_instance_ref` 只走可见引用，不要求是本对象上的活动实例；§27 未强制。自身目标 SHARE_ACTIVE 亦被准入（规格未禁）。

### P2-6 — journal 第二处 INTERNAL 用例不断言该码

`test_a_caller_that_passes_no_usable_context_raises_rather_than_half_returns` 只 `pytest.raises(ContractError)`，不出现 `INTERNAL_CONTRACT_ERROR`。对照表另一条 `test_a_non_envelope_decision_is_an_internal_contract_error` 有断言。journal 提交列表也比 `git log 0d89307..HEAD` 短（少了夹具遍历与 leak 测试那几笔），属文档漂移。

---

## 2. 逐项核验

### 2.1 越界：通过

相对基准 `0d89307`：

```text
A	plans/llm-native-htn/H1/journal-F.md
A	src/agent_orchestrator/planning/decision_admission.py
A	tests/orchestrator/full_target/test_planning_decision_admission.py
```

白名单外路径为空。未改 `contracts/planning_decisions.py`、`schemas/`、`invalid/*.expect.json`、`hierarchical_dispatch.py`。未新增枚举/字段名/拒绝码。`_new_mode` 仍为 26。

### 2.2 纯函数性：通过

AST 递归 import 图（`agent_orchestrator.*`）：

```text
decision_admission
  -> contracts.evidence_state
  -> contracts.htn
  -> contracts.models
  -> contracts.planning_decisions
  -> contracts.semantic_base
  -> contracts.state_machines   (htn 间接)
forbidden_hits []
```

无 `storage/`、无事件、无 compile/commit。同一输入两次 `PlanningFeedbackV1.to_json()` 逐字节相同（`byte_identical True n 525`）；两次准入 `AdmittedPlanningDecision` 相等且 `canonical_hash` 为 §15 哈希。

### 2.3 检查顺序与双阶段：通过（插入阶段见 P2-2）

核验员自构 6 组「同时违反两阶段」，全部只报先到：

| # | 同时违反 | 只报 | 指针 |
|---|---|---|---|
| D1 | 包哈希 + 修订号 | `PACKAGE_HASH_MISMATCH` | `/package_hash` |
| D2 | 修订号 + 未知 subject | `REQUEST_BINDING_STALE` | `/request_binding/plan` |
| D3 | 未知 subject + 引用越界 | `SUBJECT_NOT_IN_REQUEST` | `/subject_key` |
| D4 | 未启用类型 + 空方法库 | `DECISION_NOT_ENABLED_IN_PHASE` | `/decision_type` |
| D5 | 修复实例不活动 + UNKNOWN 操作 | `METHOD_RETIRED` | `/payload/rejected_method_instance` |
| D6 | 方法不存在 + 顺序环 | `METHOD_NOT_FOUND` | `/payload/method_ref` |

`dual_all_ok True`。

### 2.4 拒绝码差集与 journal 对照表：通过（差集见 §3）

§33 抽取 36 码与 `PlanningDecisionRejectionCode` 空差集。准入模块 `REJECTION.*` 引用 28 码，与 `ADMISSION_CODE_CASES` 一致；其余 8 码属 codec/契约层（H1-A2b / H1-C），journal 已标明归属。journal 28 行「拒绝码 → 用例」中，列出的测试函数全部存在，除 P2-6 外都在函数体里断言了该码；夹具名均可对上 `invalid/*.expect.json`。

### 2.5 `invalid/` 中 `checked_in=H1-F` 的样例：通过

脚本遍历 31 个（28 admission + 3 codec 描述符）。`test_every_h1f_fixture_yields_its_expected_code` 参数化全绿（含在 132 passed 内）。清单靠扫描得出，未手抄。

### 2.6 引用逐字节匹配：通过

独立构造（均拒）：

| 变异 | 结果 |
|---|---|
| kind `method`→`task` | admission `REF_OUTSIDE_CONTEXT` `/payload/method_ref` |
| id 末尾加 `X` | 同上 |
| revision 2→3 | 同上 |
| hash → HASH_B | 同上 |
| kind `METHOD` / `Method` | `from_json` `ContractError`（枚举大小写敏感） |
| revision `"1"`（字符串） | `from_json` `ContractError: semantic_revision must be an integer` |
| hash 大写 | `from_json` `ContractError: must be a lowercase SHA-256 hex digest` |

补遗 06:30：模块不 import `planner_package`，核对只用 `AdmissionContext.visible_refs`。

### 2.7 反馈对象安全性：通过（指针见 P2-1）

§32 23 个系统字段名从规格文本抽取后与测试集空差集。独立扫描 12 类拒绝的 `field_path`/`detail`/`expected`/`observed`：§32 词条零命中；`op-zzz` / `mission-1` / `request-1` 不出现。`OPERATION_UNRESOLVED.observed` 是条数不是操作 id。

### 2.8 八个刁钻输入

| # | 输入 | 结果 |
|---|---|---|
| T1 | subject_key 形态合法但只属于另一请求的 planning_subjects | `SUBJECT_NOT_IN_REQUEST` `/subject_key` |
| T2 | 方法存在、状态 RETIRED | `METHOD_RETIRED` |
| T3 | 版本对、哈希不对 | `METHOD_STALE`（现有测试未钉，见 P1-3） |
| T4 | 修复目标实例属于兄弟对象 | `METHOD_RETIRED` `/payload/rejected_method_instance` |
| T5 | SHARE_ACTIVE 的 goal_ref = 本对象 task | **准入**（规格未禁，P2-5） |
| T6 | `enabled_decision_types=frozenset()` | **崩溃** `ContractError`（P1-1） |
| T7 | `visible_refs=()`：REFINE/WAIT 拒，NO_CHANGE 过 | 原版正确；空清单变异存活（P1-2） |
| T8 | 限额边界：`(1,1,True)` 过；rounds=0 → `BUDGET_INSUFFICIENT`；bound=0 → `PLANNING_BOUND_REACHED`；avail=False → `BUDGET_INSUFFICIENT`；WAIT 在耗尽时仍过 | 符合 §29/§30 起止型豁免 |

### 2.9 变异 ≥12：见 §4

### 2.10 ruff / 相关测试：通过（尾行见 §5）

---

## 3. 差集（脚本从规格文本抽取后与代码求差）

脚本：`/tmp/h1f-verify/extract_and_probe.py`（不随片提交）。

### 3.1 V2 §33 拒绝码 vs `PlanningDecisionRejectionCode`

```text
spec_n 36 enum_n 36
spec - enum []
enum - spec []
```

### 3.2 V2 §33 vs 准入模块实际 `REJECTION.*` 引用

```text
returned 28 codes
spec - returned ['DECISION_BLOCK_MISSING', 'DECISION_TYPE_UNKNOWN', 'MALFORMED_DECISION',
                 'MIXED_PROTOCOL_BLOCKS', 'MODEL_SET_SYSTEM_FIELD', 'MULTIPLE_DECISIONS',
                 'STRUCTURE_INVALID', 'UNKNOWN_FIELD']
returned - spec []
admission_cases - returned []
returned - admission_cases []
```

这 8 个是 codec/契约层码，准入收的是已解码信封，不应再产生它们。

### 3.3 V2 §32 系统字段 vs 测试 `SECTION_32_SYSTEM_FIELDS`

```text
spec_n 23 test_n 23
spec - test []
test - spec []
```

### 3.4 V2 §12 H1 列 vs `H1_DECISION_ENABLEMENT`

```text
matrix_keys == code_keys
['BIND_EXISTING_GOAL', 'DECLARE_BLOCKED', 'NO_CHANGE', 'PROPOSE_METHOD', 'REFINE',
 'REPAIR/PROPOSE_SUCCESSOR', 'REPAIR/REPLACE_METHOD', 'REQUEST_EVIDENCE',
 'REQUEST_HUMAN', 'WAIT']
matrix - code []
code - matrix []
```

D/A/X 七行 `executable=True`；D/R 三行 `admissible=False, executable=False`。与代码常量一致。

### 3.5 V2 §43 顺序（抽取）vs 实现

```text
spec: request identity → package/prompt binding → plan revision → subject
      → visible refs → phase enable → payload checks
      → method/applicability/evidence/auth/capability → operation gate
      → typed admitted command
impl: 同上，并在 phase enable 与 payload 之间插入 budget/bound，
      在 operation gate 之后插入 plan shape（P2-2）
```

### 3.6 V2 §39 / §40 字段名

规格抽取 `PlanningFeedbackV1` 六字段 + `PlanningRetryBudgetView` 五字段、`PlanningProblemDetailV1` 六字段，与 `feedback.to_json()` / 问题键集一致（测试 `test_the_feedback_carries_no_internal_fields` 钉死）。

---

## 4. 变异表

每个变异前 `cp` 到 `/tmp/h1f-verify/orig/decision_admission.py`，变异后跑 targeted，随后用副本恢复，`git status --short` 均为空。

| # | 变异（任务书要求） | 结果 | 尾行 |
|---|---|---|---|
| M1 | 调换两步顺序（phase enable ↔ binding revisions） | KILLED | `2 failed, 130 passed in 0.20s` |
| M2 | 去掉修订号检查 | KILLED | `8 failed, 124 passed in 0.21s` |
| M3 | 引用匹配改为只比 id | KILLED | `7 failed, 125 passed in 0.21s` |
| M4 | 去掉退役/拒绝生命周期检查 | KILLED | `6 failed, 126 passed in 0.21s` |
| M5 | 去掉适用性检查 | KILLED | `1 failed, 131 passed in 0.20s` |
| M6 | 未启用类型放行 | KILLED | `7 failed, 125 passed in 0.21s` |
| M7 | 通过时 canonical_hash 换成哑哈希 | KILLED | `1 failed, 131 passed in 0.20s` |
| M8 | 反馈 observed 泄漏内部操作 id | KILLED | `1 failed, 131 passed in 0.21s` |
| M9 | 多阶段错误混报进同一份反馈 | KILLED | `14 failed, 118 passed in 0.21s` |
| M10 | 修复目标不校验活动实例 | KILLED | `3 failed, 129 passed in 0.20s` |
| M11 | 空清单放行 | **SURVIVED** | `132 passed in 0.20s` |
| M12 | 对象键不校验 | KILLED | `2 failed, 130 passed in 0.20s` |
| M13 | 方法身份只比 version 不比 hash（附加） | **SURVIVED** | `132 passed in 0.21s` |
| M14 | 去掉 running-work 检查（附加） | KILLED | `3 failed, 129 passed in 0.21s` |

存活且行为可分辨：M11、M13（P1-2、P1-3）。无等价变异需要备案。

---

## 5. 命令尾行原文

```text
$ PYTHONPATH=src uv run --offline python -c "import agent_orchestrator, pathlib; ..."
__file__= /Users/taiwan/PROJECTS/SimplaHarness/simple-harness-sdk-h1f-verify-h1-f/src/agent_orchestrator/__init__.py
in_cwd= True

$ uv run --offline ruff check src/agent_orchestrator/planning/decision_admission.py tests/orchestrator/full_target/test_planning_decision_admission.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py -q -p no:cacheprovider
132 passed in 0.23s

$ grep -rn "_new_mode" src/agent_orchestrator | wc -l
      26

$ git status --short
（空）

$ git diff --name-status 0d89307
A	plans/llm-native-htn/H1/journal-F.md
A	src/agent_orchestrator/planning/decision_admission.py
A	tests/orchestrator/full_target/test_planning_decision_admission.py
```

变异电池恢复后 `git status --short` 仍空；`cmp /tmp/h1f-verify/orig/decision_admission.py src/.../decision_admission.py` 一致。

未跑 `tests/orchestrator/full_target` 全量（本片相关测试为上述 132 条）。实施者 journal 记过 `3582 passed, 2 skipped`；本轮未复测该数字。

---

## 6. 建议修复（不在本核验回合实施）

1. `_Stage.refuse`：`expected`/`observed` 若为 `""` 则传 `None`。
2. 补用例：空 `enabled_decision_types` → `DECISION_NOT_ENABLED_IN_PHASE`；空 `bindings` + 必填参数 → `PARAMETER_INVALID`（不得抛 `ContractError`）。
3. 补用例：`visible_refs=()` 且决定引用了四元组 → `REF_OUTSIDE_CONTEXT`；无引用的 NO_CHANGE 仍过。
4. 补用例：方法 version 相同、`content_hash` 不同 → `METHOD_STALE`，`expected`/`observed` 带上哈希。

---

# 复核 2（受检 HEAD `5804dc3` → `5804dc3`；区间无提交）

**范围：** 任务书要求只看上一轮 HEAD `5804dc3f4a7a58c48a866951d05259f5ac48c944` 到当前 HEAD 同哈希之间的改动。`git diff --stat 5804dc3` 为空；工作树除本报告外干净。**实施者未提交任何修复。**

`agent_orchestrator.__file__` 仍为本副本 `src/agent_orchestrator/__init__.py`。变异前 `cp` 到 `/tmp/h1f-verify/orig/`，结束后 `cmp` 一致。

## 结论

第 1 轮 3 条 P1 全部仍在：P1-1 空集合仍抛 `ContractError`；存活变异 R1-M11 / R1-M13 **均未被杀死**。新补 11 个变异中 7 个被杀死、4 个可分辨存活（WAIT `wait_for`、BIND 消费实例引用、修复实例只比 id、successor `old_task_ref` 均可从扫描中拿掉而 132 全绿）。无 P0。

**判定：修后可合（P0 无；第 1 轮 P1 未修，本轮新增 4 条测试缺口）。**

## 步骤 1：区间改动

```text
$ git rev-parse HEAD
5804dc3f4a7a58c48a866951d05259f5ac48c944
$ git diff --name-status 5804dc3
（空）
$ git status --short
?? plans/llm-native-htn/H1/reviews/verify-H1-F-2026-09-19.md
```

无源码、无测试、无 journal 变更。

## 步骤 2：第 1 轮存活变异重跑（均仍存活）

| 变异 | 原版行为 | 变异后 | targeted | 结果 |
|---|---|---|---|---|
| R1-M11 空 `visible_refs` 直接 return | REFINE+空清单 → `REF_OUTSIDE_CONTEXT` | `AdmittedPlanningDecision` | `132 passed in 0.20s` | **SURVIVED** |
| R1-M13 方法身份只比 version | version 同、hash 不同 → `METHOD_STALE` | `AdmittedPlanningDecision` | `132 passed in 0.21s` | **SURVIVED** |

P1-1 复测仍崩：空启用集 → `ContractError: problem.expected must not be blank`；空 `bindings={}` → `ContractError: problem.observed must not be blank`。

## 步骤 3：新变异

| # | 变异 | 结果 | 尾行 | 可分辨？ |
|---|---|---|---|---|
| N1 | 不扫描 `reason_refs` | KILLED | `3 failed, 129 passed in 0.20s` | — |
| N2 | 不扫描 WAIT `wait_for` | **SURVIVED** | `132 passed in 0.20s` | 是：WAIT+空清单 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N3 | 不扫描 BIND `consumer_method_instance_ref` | **SURVIVED** | `132 passed in 0.21s` | 是：隐藏消费实例 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N4 | 修复实例只按 `instance_id` 查找 | **SURVIVED** | `132 passed in 0.21s` | 是：同 id 错 hash 原 `METHOD_RETIRED` → 变异后准入 |
| N5 | 去掉 SHARE_ACTIVE 可共享目标检查 | KILLED | `1 failed, 131 passed in 0.21s` | — |
| N6 | 去掉 PARAMETER_INVALID | KILLED | `4 failed, 128 passed in 0.21s` | — |
| N7 | 去掉未注册谓词 EVIDENCE_REQUIRED | KILLED | `1 failed, 131 passed in 0.20s` | — |
| N8 | 去掉 CAPABILITY_MISSING | KILLED | `3 failed, 129 passed in 0.21s` | — |
| N9 | 去掉 AUTHORIZATION_REQUIRED | KILLED | `3 failed, 129 passed in 0.21s` | — |
| N10 | 预算只看轮次、忽略账户可用性 | KILLED | `1 failed, 131 passed in 0.21s` | — |
| N11 | 不扫描 successor `old_task_ref` | **SURVIVED** | `132 passed in 0.21s` | 是：隐藏 old_task 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |

存活且行为可分辨 = 测试缺口（P1），不是等价变异。

## 本轮问题（追加）

- **P1-2 / P1-3 未关闭**（R1-M11、R1-M13 仍存活）。
- **P1-1 未关闭**（空集合仍 `ContractError`）。
- **P1-4（新）：** `_decision_refs` 可不收录 WAIT `wait_for`，targeted 全绿。位置 `decision_admission.py:747-751`。
- **P1-5（新）：** 可不收录 BIND `consumer_method_instance_ref`，targeted 全绿。位置 `:741-743`。
- **P1-6（新）：** `_instance_for` 可改为只比 `instance_id`，同 id 错 hash 的修复目标被准入。位置 `:831-836`。
- **P1-7（新）：** 可不收录 successor `old_task_ref`，targeted 全绿。位置 `:737-739`。

## 命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/planning/decision_admission.py tests/orchestrator/full_target/test_planning_decision_admission.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py -q -p no:cacheprovider
132 passed in 0.20s
```

变异恢复后 `cmp` 与 `/tmp/h1f-verify/orig/decision_admission.py` 一致；`git status --short` 仅本报告未跟踪。未 commit、未 merge、未 push。

---

# 复核 3（受检 HEAD `5804dc3` → `5804dc3`；区间无提交）

**范围：** 上一轮 HEAD 与当前 HEAD 同为 `5804dc3f4a7a58c48a866951d05259f5ac48c944`。`git diff --name-status 5804dc3` 为空。**实施者仍未提交修复。**

`agent_orchestrator.__file__` 仍为本副本路径。变异前复制到 `/tmp/h1f-verify/orig/`，结束后 `cmp` 一致。

## 结论

复核 2 记录的 6 个存活变异 **全部仍存活**（R2-M11 / R2-M13 / R2-N2 / R2-N3 / R2-N4 / R2-N11）。P1-1 空集合仍抛 `ContractError`。新补 10 个变异：6 杀 / 4 可分辨存活（BIND `resolution_ref`、修复 `rejected_method_instance`、successor `obligation_ref` 可不进引用扫描；`requirements_revision` 失配检查可拿掉）。无 P0。

**判定：修后可合（P0 无；既有 P1 未修，本轮再增 4 条测试缺口）。**

## 步骤 1：区间改动

```text
$ git rev-parse HEAD
5804dc3f4a7a58c48a866951d05259f5ac48c944
$ git diff --name-status 5804dc3
（空）
$ git status --short
?? plans/llm-native-htn/H1/reviews/verify-H1-F-2026-09-19.md
```

## 步骤 2：复核 2 存活变异重跑（均仍存活）

| 变异 | targeted | 结果 |
|---|---|---|
| R2-M11 空 `visible_refs` 直接 return | `132 passed in 0.21s` | **SURVIVED** |
| R2-M13 方法身份只比 version | `132 passed in 0.20s` | **SURVIVED** |
| R2-N2 不扫描 WAIT `wait_for` | `132 passed in 0.21s` | **SURVIVED** |
| R2-N3 不扫描 BIND `consumer_method_instance_ref` | `132 passed in 0.20s` | **SURVIVED** |
| R2-N4 修复实例只按 id 查找 | `132 passed in 0.20s` | **SURVIVED** |
| R2-N11 不扫描 successor `old_task_ref` | `132 passed in 0.21s` | **SURVIVED** |

P1-1 复测：空启用集 → `ContractError: problem.expected must not be blank`；空 `bindings={}` → `ContractError: problem.observed must not be blank`。

## 步骤 3：新变异

| # | 变异 | 结果 | 尾行 | 可分辨？ |
|---|---|---|---|---|
| N12 | 不扫描 BIND `resolution_ref` | **SURVIVED** | `132 passed in 0.21s` | 是：隐藏 resolution 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N13 | 不扫描修复 `rejected_method_instance` | **SURVIVED** | `132 passed in 0.20s` | 是：隐藏该引用 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N14 | 不扫描 successor `obligation_ref` | **SURVIVED** | `132 passed in 0.20s` | 是：隐藏 obligation 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N15 | 去掉 `requirements_revision` 过期检查 | **SURVIVED** | `132 passed in 0.20s` | 是：`requirements_revision=12` 原 `REQUEST_BINDING_STALE` → 变异后准入 |
| N16 | 去掉 prompt_hash 包绑定检查 | KILLED | `1 failed, 131 passed in 0.20s` | — |
| N17 | 去掉 REFINEMENT_CYCLE | KILLED | `3 failed, 129 passed in 0.21s` | — |
| N18 | 去掉 DATA_UNBOUND | KILLED | `3 failed, 129 passed in 0.21s` | — |
| N19 | 去掉 COVERAGE_GAP | KILLED | `3 failed, 129 passed in 0.21s` | — |
| N20 | 去掉 METHOD_NOT_AUTHORIZED | KILLED | `3 failed, 129 passed in 0.20s` | — |
| N21 | 去掉 REUSE_ACCEPTED 当前决议检查 | KILLED | `3 failed, 129 passed in 0.20s` | — |

## 本轮问题（追加）

- **P1-1～P1-7 未关闭。**
- **P1-8（新）：** `_decision_refs` 可不收录 BIND `resolution_ref`。位置 `decision_admission.py:745-746`。
- **P1-9（新）：** 可不收录修复 `rejected_method_instance`。位置 `:734-736`。
- **P1-10（新）：** 可不收录 successor `obligation_ref`。位置 `:737-739`。
- **P1-11（新）：** `_check_binding_revisions` 可不比较 `requirements_revision`；测试只钉了 `plan_revision`。位置 `:685-690`。

## 命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/planning/decision_admission.py tests/orchestrator/full_target/test_planning_decision_admission.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py -q -p no:cacheprovider
132 passed in 0.20s
```

未 commit、未 merge、未 push。源码已从 `/tmp` 副本恢复。

---

# 复核 4（受检 HEAD `5804dc3` → `5804dc3`；区间无提交）

**范围：** 上一轮与当前 HEAD 同为 `5804dc3f4a7a58c48a866951d05259f5ac48c944`。`git diff --name-status 5804dc3` 为空。**实施者仍未提交修复。**

`agent_orchestrator.__file__` 仍为本副本路径。变异前复制到 `/tmp/h1f-verify/orig/`，结束后 `cmp` 一致。

## 结论

复核 3 记录的 10 个存活变异 **全部仍存活**。P1-1 空集合仍抛 `ContractError`。新补 8 个变异：5 杀 / 3 可分辨存活（`scope_epoch_digest` 失配检查可拿掉；BIND `goal_ref`、修复 `replacement_method_ref` 可不进引用扫描）。无 P0。

**判定：修后可合（P0 无；既有 P1 未修，本轮再增 3 条测试缺口）。**

## 步骤 1：区间改动

```text
$ git rev-parse HEAD
5804dc3f4a7a58c48a866951d05259f5ac48c944
$ git diff --name-status 5804dc3
（空）
$ git status --short
?? plans/llm-native-htn/H1/reviews/verify-H1-F-2026-09-19.md
```

## 步骤 2：复核 3 存活变异重跑（均仍存活）

| 变异 | targeted | 结果 |
|---|---|---|
| R3-M11 空清单放行 | `132 passed in 0.21s` | **SURVIVED** |
| R3-M13 方法身份只比 version | `132 passed in 0.20s` | **SURVIVED** |
| R3-N2 不扫描 WAIT `wait_for` | `132 passed in 0.20s` | **SURVIVED** |
| R3-N3 不扫描 BIND 消费实例 | `132 passed in 0.21s` | **SURVIVED** |
| R3-N4 修复实例只比 id | `132 passed in 0.20s` | **SURVIVED** |
| R3-N11 不扫描 successor `old_task_ref` | `132 passed in 0.21s` | **SURVIVED** |
| R3-N12 不扫描 BIND `resolution_ref` | `132 passed in 0.20s` | **SURVIVED** |
| R3-N13 不扫描 `rejected_method_instance` | `132 passed in 0.20s` | **SURVIVED** |
| R3-N14 不扫描 successor `obligation_ref` | `132 passed in 0.20s` | **SURVIVED** |
| R3-N15 去掉 `requirements_revision` 检查 | `132 passed in 0.21s` | **SURVIVED** |

P1-1 复测：空启用集 → `ContractError: problem.expected must not be blank`；空 `bindings={}` → `ContractError: problem.observed must not be blank`。

## 步骤 3：新变异

| # | 变异 | 结果 | 尾行 | 可分辨？ |
|---|---|---|---|---|
| N22 | 去掉 `scope_epoch_digest` 过期检查 | **SURVIVED** | `132 passed in 0.20s` | 是：digest 换成 HASH_A 原 `REQUEST_BINDING_STALE` → 变异后准入 |
| N23 | 不扫描 BIND `goal_ref` | **SURVIVED** | `132 passed in 0.21s` | 是：SHARE 隐藏 goal 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N24 | 不扫描 `replacement_method_ref` | **SURVIVED** | `132 passed in 0.21s` | 是：隐藏 replacement 原 `REF_OUTSIDE_CONTEXT` → 变异后准入 |
| N25 | 不扫描 REFINE `method_ref` | KILLED | `4 failed, 128 passed in 0.20s` | — |
| N26 | 去掉 ORDER_CYCLE | KILLED | `4 failed, 128 passed in 0.21s` | — |
| N27 | 去掉操作闸门 | KILLED | `4 failed, 128 passed in 0.20s` | — |
| N28 | 去掉 OBLIGATION_NOT_OPEN | KILLED | `4 failed, 128 passed in 0.21s` | — |
| N29 | 去掉 EVIDENCE_CONFLICT 前提映射 | KILLED | `3 failed, 129 passed in 0.20s` | — |

## 本轮问题（追加）

- **P1-1～P1-11 未关闭。**
- **P1-12（新）：** `_check_binding_revisions` 可不比较 `scope_epoch_digest`。位置 `decision_admission.py:691-696`。
- **P1-13（新）：** `_decision_refs` 可不收录 BIND `goal_ref`。位置 `:744`。
- **P1-14（新）：** 可不收录修复 `replacement_method_ref`。位置 `:736`。

## 命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/planning/decision_admission.py tests/orchestrator/full_target/test_planning_decision_admission.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py -q -p no:cacheprovider
132 passed in 0.22s
```

未 commit、未 merge、未 push。源码已从 `/tmp` 副本恢复。

---

# 续做（同一会话；报告回写）

磁盘上原路径一度缺失（`reviews/` 目录在 14:39 刷新后无本文件）。本小节不重跑已完成检查。从本会话 `updates.jsonl` 按第 1 轮 write + 复核 2/3/4 三次 search_replace 原样还原到 `plans/llm-native-htn/H1/reviews/verify-H1-F-2026-09-19.md`。

核对：HEAD 仍为 `5804dc3f4a7a58c48a866951d05259f5ac48c944`；`cmp /tmp/h1f-verify/orig/decision_admission.py src/agent_orchestrator/planning/decision_admission.py` 一致（变异均已从 /tmp 恢复）；`git status --short` 仅本报告未跟踪。未 commit、未 merge、未 push。判定不变。

---

# 复核 2（受检 HEAD `5804dc3` → `9a2fd1e`）

**范围：** `5804dc3f4a7a58c48a866951d05259f5ac48c944` → `9a2fd1e0cfadb59355b83dfbdd4f8e3e912e7395`。此前同哈希区间的「复核 2/3/4」是无提交空转，本小节才是实施修复后的复核。

`agent_orchestrator.__file__` 仍为本副本。变异前 `cp` 到 `/tmp/h1f-verify/orig/`，每个变异后用副本恢复，`git status --short` 仅本报告未跟踪。

## 结论

第 1 轮 3 条 P1 均已关闭：空集合不再抛 `ContractError`；报告所记 13 个存活变异全部被杀死。新补 8 个变异全部 KILLED。无新增 P0/P1。P2-1/P2-2/P2-5 实施者明确备案不改，仍不阻塞。

**判定：可合（P0 无；P1 无）。**

## 步骤 1：区间改动

```text
9a2fd1e docs(h1-f): journal for the review round (problem closure, mutation battery, measured tails)
1fd8329 fix(h1-f): refuse instead of crashing on empty problem sides; name both halves of a method identity
64a5775 test(h1-f): close the review's mutation holes (empty lists, hidden citations, same-id-wrong-hash instances)

 plans/llm-native-htn/H1/journal-F.md               | 117 +++++++-
 src/agent_orchestrator/planning/decision_admission.py |  17 +-
 tests/orchestrator/full_target/test_planning_decision_admission.py | 299 ++++++++++++++++++++-
 3 files changed, 413 insertions(+), 20 deletions(-)
```

白名单内。实现：空 `expected`/`observed` 改传 `None`；`METHOD_STALE` 回写 `version@hash`。测试补空启用集、空 bindings、空清单、身份哈希、各引用位隐藏、同 id 错 hash、`requirements_revision` / `scope_epoch_digest`。`ADMISSION_CODE_CASES` 幽灵用例名已改为真名。

## 步骤 2：第 1 轮及后续记录的存活变异（全部 KILLED）

| 变异 | 结果 | 尾行 |
|---|---|---|
| S-M11 空清单放行 | KILLED | `2 failed, 159 passed in 0.23s` |
| S-M13 方法身份只比 version | KILLED | `1 failed, 160 passed in 0.23s` |
| S-N2 不扫描 WAIT `wait_for` | KILLED | `3 failed, 158 passed in 0.24s` |
| S-N3 不扫描 BIND 消费实例 | KILLED | `2 failed, 159 passed in 0.24s` |
| S-N4 修复实例只比 id | KILLED | `2 failed, 159 passed in 0.24s` |
| S-N11 不扫描 successor `old_task_ref` | KILLED | `2 failed, 159 passed in 0.23s` |
| S-N12 不扫描 BIND `resolution_ref` | KILLED | `2 failed, 159 passed in 0.23s` |
| S-N13 不扫描 `rejected_method_instance` | KILLED | `2 failed, 159 passed in 0.22s` |
| S-N14 不扫描 successor `obligation_ref` | KILLED | `2 failed, 159 passed in 0.24s` |
| S-N15 去掉 `requirements_revision` | KILLED | `2 failed, 159 passed in 0.24s` |
| S-N22 去掉 `scope_epoch_digest` | KILLED | `2 failed, 159 passed in 0.26s` |
| S-N23 不扫描 BIND `goal_ref` | KILLED | `2 failed, 159 passed in 0.24s` |
| S-N24 不扫描 `replacement_method_ref` | KILLED | `2 failed, 159 passed in 0.24s` |

P1-1 复测：空启用集 → `DECISION_NOT_ENABLED_IN_PHASE`（不再崩溃）；空 `bindings={}` → `PARAMETER_INVALID`（不再崩溃）。

## 步骤 3：新变异（全部 KILLED）

| # | 变异 | 结果 | 尾行 |
|---|---|---|---|
| N30 | `_ref_key` 去掉 kind | KILLED | `5 failed, 156 passed in 0.23s` |
| N31 | `_ref_key` 去掉 semantic_revision | KILLED | `9 failed, 152 passed in 0.26s` |
| N32 | 去掉 plan_revision 过期检查 | KILLED | `9 failed, 152 passed in 0.26s` |
| N33 | 去掉 package_version 检查 | KILLED | `1 failed, 160 passed in 0.25s` |
| N34 | 去掉 METHOD_REJECTED | KILLED | `3 failed, 158 passed in 0.25s` |
| N35 | 去掉 repair_allowed | KILLED | `3 failed, 158 passed in 0.24s` |
| N36 | 去掉 UNKNOWN→EVIDENCE_REQUIRED | KILLED | `3 failed, 158 passed in 0.24s` |
| N37 | 空启用集 `expected` 再拼空串 | KILLED | `1 failed, 160 passed in 0.25s` |

无存活、无等价变异需备案。

## 命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/planning/decision_admission.py tests/orchestrator/full_target/test_planning_decision_admission.py
All checks passed!

$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py -q -p no:cacheprovider
161 passed in 0.25s
```

未 commit、未 merge、未 push。源码已从 `/tmp` 副本恢复。
