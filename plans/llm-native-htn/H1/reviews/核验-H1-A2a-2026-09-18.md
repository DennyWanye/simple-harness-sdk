# 独立核验报告：切片 H1-A2a（PlanningDecisionEnvelopeV1、全部载荷与封闭枚举）

- **核验日期：** 2026-09-18
- **核验对象（首轮）：** HEAD `8a609c9a8de803a68dc97b618695104f88441246`
  （`feat(h1-a2a): planning-decision envelope, payloads and closed enums`），基线 `b13e757b74aff9987eebf796ad1b01a3e27b4808`。
- **核验者：** 独立会话（不知实施者，只看代码、规格与可复现证据）。
- **规格来源：** V2 计划 `simpleharness-llm-native-htn-execution-plan-v2.zh-CN.md` 第 13、15、16、17、24–32、35 节；`LLM-native-HTN计划V2-裁定补遗-2026-09-18.zh-CN.md` 一 / 二 / 三节。
- **判定规则：** 有 P0 → 不可合；只有 P1 → 修后可合；只有 P2 或无 → 可合。

> 说明：本文件首轮正文由核验会话按其当日保存的证据（`/tmp/h1a2a/literal_diff*.out`、`strictness.out`、`canonical.out`、`mut/mutation.log`）重新落盘，因为本轮复核开始时该文件未出现在刷新后的核验副本内（副本 `plans/llm-native-htn/H1/reviews/` 当时只有 A1 报告）。第 2 轮复核结果见文末「## 复核 2」。

## 结论（首轮）

**未发现 P0。** 越界、逐字一致、严格性、canonical、A1 能力保持五项核对，除 1 条 P1 测试缺口外全部通过。

**P1（首轮）：** A2a 的 canonical 测试只断言“键序无关 / 数组序相关 / 64 位小写 hex”，没有任何断言把 `canonical_decision_json` 的值本身钉到规格 §15 的 `canonical_json(decision.to_json())` 或基准值；变异 M7（去 `sort_keys`）与 M12（`sha256(json + "x")`）在 145 条本片测试下全部存活。

**首轮判定：修后可合。**

## 一、核验环境与前置确认（首轮）

- 逻辑路径 `…/simple-harness-sdk-h1a2a-verify-h1-a2a` 与 `git rev-parse --show-toplevel` 的物理名 `…/simple-runtime-sdk-h1a2a-verify-h1-a2a` 为同一目录。
- `agent_orchestrator.__file__` 位于当前目录内。
- 全程 `uv run --offline`；未用 `git stash / checkout / reset --hard / rebase / push`；未 commit；未读密钥；工作树干净。

**越界核对：**

```
$ git diff --name-status b13e757 HEAD
A   plans/llm-native-htn/H1/journal-A2a.md
M   src/agent_orchestrator/contracts/planning_decisions.py
M   tests/orchestrator/full_target/test_planning_decision_contract.py
A   tests/orchestrator/full_target/test_planning_decision_envelope.py
```

`contracts/` 其余文件零改动；脚本（`ast` + `importlib`）确认 `contracts/` 11 个模块直接与传递依赖均不 import `planning` / `orchestrator` / `storage`：

```
contracts modules: ['assessments', 'evidence_state', 'fragments', 'htn', 'ids', 'models', 'obligations', 'planning_decisions', 'resolution', 'semantic_base', 'state_machines']
forbidden transitively imported: NONE
direct forbidden imports: NONE
```

## 二、逐项核对结果（首轮）

### 2. 逐字一致——通过（差集全部为空）

脚本 `/tmp/h1a2a/literal_diff3.py`、`literal_diff4.py`（权威）从规格抽取后与代码求差集：

```
### §13 envelope fields
spec(10) == code(10)；missing_in_code: []；extra_in_code: []

### §16 limits (name -> value) —— 11 项全部 OK
  MAX_PD_ALTERNATIVES=8  MAX_PD_ARGUMENTS=32  MAX_PD_ASSUMPTIONS=16  MAX_PD_BINDINGS=64
  MAX_PD_BLOCKERS=16     MAX_PD_HUMAN_OPTIONS=12  MAX_PD_RATIONALE_CHARS=4000
  MAX_PD_REASON_REFS=32  MAX_PD_REPLAN_TRIGGERS=16  MAX_PD_UNCERTAINTIES=16  MAX_PD_WAIT_REFS=32
missing_in_code: []
unlisted_extra_in_code: ['MAX_PD_EVIDENCE_QUESTIONS', 'MAX_PLANNING_REF_ID', 'MAX_SUBJECT_KEY_CHARS', 'MIN_PD_EVIDENCE_QUESTIONS']
```

（extra 四项有规格出处：补遗三 REQUEST_EVIDENCE 1–8；§13 subject_key maxLength 256；§17 id 边界，非越界。）

```
### §24-§30 top-level payload keys: spec vs codec required= —— 全部 missing_in_code=[] extra_in_code=[]
  §24 ['method_ref','bindings'] / §25 ['repair_kind','rejected_method_instance','replacement_method_ref','bindings']
  §26 ['repair_kind','old_task_ref','obligation_ref','goal_type_ref','bindings'] / §27 ['mode','consumer_method_instance_ref','step','goal_ref','resolution_ref']
  §28 ['blockers','resumable_if'] / §29 ['wait_for','reason'] / §30 ['reason']

  PlanningRefV1: spec=['kind','id','semantic_revision','content_hash'] missing=[] extra=[]
  VersionedTypeRefV1: spec=['id','version','content_hash'] missing=[] extra=[]
  BlockedItemV1: spec=['code','detail'] missing=[] extra=[]
```

封闭枚举（补遗二，含顺序）全部 OK：AssumptionRisk / UncertaintySeverity = LOW/MEDIUM/HIGH；AlternativeDisposition = CONSIDERED/REJECTED/DEFERRED；BlockerCode 6 值；ResumableIf 5 值；RepairKind = REPLACE_METHOD/PROPOSE_SUCCESSOR；BindExistingGoalMode = REUSE_ACCEPTED/SHARE_ACTIVE。

仅解码载荷（补遗三）全部一致：RequestEvidenceDecision {questions}、EvidenceQuestionV1 {predicate_key,arguments,purpose,blocking}、RequestHumanDecision {question,options,blocking}、HumanOptionV1 {key,label}、ProposeMethodDecision {method_proposal}。

引用种类 16 个逐值相等（含 `method_instance`）；代码中无 `fact`、无 `SELECT_METHOD`。

### 3. 严格性——通过

十个载荷的“未知字段 / 逐字段缺失 / 错类型 / bool 冒充 int”探针：**无一条意外放行**。跨字段规则全部拒绝：SHARE_ACTIVE 带 resolution_ref、REUSE_ACCEPTED 缺 resolution_ref、OTHER 无 detail、REQUEST_EVIDENCE 0 问与 9 问、REQUEST_HUMAN 13 选项。`decision_type` 与载荷不匹配 8 组全部拒绝。SELECT_METHOD / 未知 decision_type / kind=fact 全部拒绝。

**§32 例外（必须“不拒”）：** `bindings` / `arguments` 中出现 `mission_id` / `tenant_id` 键全部 `accepted=True`（不扫描 arbitrary domain parameter map）。

### 4. canonical——实现正确，但测试覆盖有缺口（首轮 P1）

独立实现（自写 `json.dumps(..., ensure_ascii=False, allow_nan=False, separators=(",", ":"), sort_keys=True)` + sha256）对照三组样例，json/hash 全一致：

```
  G1 REFINE: json_match=True hash_match=True   independent_sha256=68ae49297a167901094374076a90bdcf749646c7ae0cc5ff3ca8e150dc3fe2c5
  G2 WAIT+substructures: json_match=True hash_match=True   3c338952801abfe749ea792552205c79ad21747e5effe736242fe269f737147c
  G3 REQUEST_HUMAN: json_match=True hash_match=True        dc9eb3314476a1a649bc471a43ff8d9d521b3f1d9bb210d6b1c2c261f54efa45
  key_order_irrelevant=True  array_order_relevant=True
```

首轮缺口（已由复核 2 修复）：测试未钉住 canonical 值本身，M7 / M12 存活。

### 5. A1 已有能力未被破坏——通过

`compute_decision_id` 函数体零改动（仅 `__all__` 增项）；`PlanningRefV1` 未出现在 diff。行为复跑：`decision_id` 匹配 §35 公式、确定性、不同 raw 不同 id；`PlanningRefV1` 接受合法、拒绝 bool/"1"/0/大写 hash/fact。`H1_DECISION_ENABLEMENT` 未改（7 可执行 + 3 仅解码，符合 §12 H1 列）。

### 7. ruff 与尾行

```
$ PYTHONPATH=src uv run --offline ruff check <impl> <两个测试文件>
All checks passed!
```

首轮 full_target：`3105 passed, 2 skipped`；单文件 52 / 93 passed。

## 三、首轮变异表（12 个：10 KILLED / 2 SURVIVED）

| 编号 | 变异 | 结果 |
|---|---|---|
| M1 | 去掉 REFINE 载荷未知字段检查 | KILLED |
| M2 | 放宽 BlockerCode 为任意字符串 | KILLED |
| M3 | 去掉 reason_refs 限额检查 | KILLED |
| M4 | 去掉 reason_refs 四元组去重 | KILLED |
| M5 | 去掉 SHARE_ACTIVE 规则 | KILLED |
| M6 | 让 decision_type 与载荷不匹配也通过 | KILLED |
| **M7** | **改 canonical 排序（去 sort_keys）** | **SURVIVED** |
| M8 | 去掉 method_instance kind 校验 | KILLED |
| M9 | 去掉 OTHER detail 必填 | KILLED |
| M10 | 去掉 REQUEST_EVIDENCE 下界 | KILLED |
| M11 | canonical hash 改为 sha256("") | KILLED |
| **M12** | **canonical hash 改为 sha256(json + "x")** | **SURVIVED** |

## 六、未做项（首轮）

属 H1-A2b：JSON Schema 文件、黄金样例目录、`contracts/schemas/*.json` 打包、`test_planning_decision_json_schema.py`。属 H1-F/H1-H：三型仅解码的准入行为与 `PlanningDecisionEvaluated` 事件接线。

---

## 复核 2

- **复核日期：** 2026-09-18（同一核验会话续做）
- **上一轮核验副本 HEAD：** `8a609c9a8de803a68dc97b618695104f88441246`
- **当前核验副本 HEAD：** `a589bbbe399cb54f7da7955f22b58a96e920ed76`
- **本轮新增提交：** `a589bbb test(h1-a2a): pin canonical decision json and hash against an independent baseline`
- **判定规则：** 有 P0 → 不可合；只有 P1 → 修后可合；只有 P2 或无 → 可合。

### 复核 2 结论

- **上一轮 2 个存活变异（M7、M12）重跑后全部 KILLED**，缺口已补齐。
- 本轮另做 10 个新变异，**发现 3 个新存活变异（N2、N8、N9）**；三者在核验副本的**整个 `tests/orchestrator/full_target` 套件（3107 passed）下依然存活**。
- 未发现 P0；无越界改动。新增 3 条 P1（测试缺口）。

**复核 2 判定：修后可合。**

### 步骤 1：本轮改了什么、是否越界

```
$ git diff --name-status 8a609c9a8de803a68dc97b618695104f88441246 a589bbbe399cb54f7da7955f22b58a96e920ed76
M   plans/llm-native-htn/H1/journal-A2a.md
M   tests/orchestrator/full_target/test_planning_decision_envelope.py

$ git diff --stat 8a609c9a8de803a68dc97b618695104f88441246 a589bbbe399cb54f7da7955f22b58a96e920ed76 -- src/
（空）
```

- 改动只在 `journal-A2a.md` 与本片测试文件 `test_planning_decision_envelope.py`；**实现文件 `src/agent_orchestrator/contracts/planning_decisions.py` 零改动**（sha256 与首轮一致 `6d2bcc25…ed8b40`），在 A2a 允许改动集合内，**无越界**。
- 新增两个测试与常量（`tests/.../test_planning_decision_envelope.py:713-763` 附近）：

```python
CANONICAL_SAMPLE = {...}           # 固定、完整规格的样例
CANONICAL_SAMPLE_JSON = (...)      # 逐字节钉死的规范 JSON
CANONICAL_SAMPLE_HASH = "22111ad0105becc45cb57da4fded387b87557bd880ea1d1a3bda29249a49af72"

def test_canonical_decision_json_matches_an_independent_canonicalisation() -> None:
    envelope = PlanningDecisionEnvelopeV1.from_json(CANONICAL_SAMPLE)
    assert canonical_decision_json(envelope) == _independent_canonical_json(envelope.to_json())

def test_canonical_decision_json_and_hash_are_pinned_literals() -> None:
    envelope = PlanningDecisionEnvelopeV1.from_json(CANONICAL_SAMPLE)
    assert canonical_decision_json(envelope) == CANONICAL_SAMPLE_JSON
    assert canonical_decision_hash(envelope) == CANONICAL_SAMPLE_HASH
    assert hashlib.sha256(CANONICAL_SAMPLE_JSON.encode("utf-8")).hexdigest() == CANONICAL_SAMPLE_HASH
```

- **独立复核这些“钉死值”本身不是自证循环**（脚本 `/tmp/h1a2a/r2/verify_pins.py`）：`CANONICAL_SAMPLE_JSON` 等于标准库 `json.dumps(CANONICAL_SAMPLE, ensure_ascii=False, allow_nan=False, separators=(",", ":"), sort_keys=True)`；`CANONICAL_SAMPLE_HASH` 等于 `sha256(CANONICAL_SAMPLE_JSON)`；被测 `canonical_decision_json` / `canonical_decision_hash` 与该字面量相等。全部 `True`。
- 顺带把首轮 P2-2 的弱断言 `canonical_decision_hash(e1) != ""` 换成 `len(...) == 64`（`test_canonical_json_and_hash_ignore_object_key_order`）。

### 步骤 2：上一轮存活变异逐个重跑（全部 KILLED）

先红后绿：在新增测试存在的前提下，重跑首轮 M7 / M12。

| 编号 | 变异（同首轮） | 结果 | 测试尾行 / 首个失败 | 恢复 |
|---|---|---|---|---|
| R2-M7 | `canonical_decision_json` 去掉 `sort_keys`（改为不排序 `json.dumps`） | **KILLED** | 2 failed, 145 passed；`test_canonical_decision_json_matches_an_independent_canonicalisation` | restored=True clean=True |
| R2-M12 | `canonical_decision_hash` 改为 `sha256(canonical_decision_json + "x")` | **KILLED** | 1 failed, 146 passed；`test_canonical_decision_json_and_hash_are_pinned_literals` | restored=True clean=True |

原始代码上 targeted 全绿：`147 passed in 0.15s`（A1 文件 52 + A2a 文件 95）。

### 步骤 3：本轮新增变异（10 个：7 KILLED / 3 SURVIVED）

| 编号 | 新变异 | 结果 | 测试尾行 / 首个失败 | 恢复 |
|---|---|---|---|---|
| N1 | `canonical_decision_json` 改为 `indent=2`（多空白） | KILLED | 2 failed, 145 passed；`test_canonical_decision_json_matches_an_independent_canonicalisation` | restored=True clean=True |
| **N2** | **`canonical_decision_json` 改为 `ensure_ascii=True`（转义非 ASCII）** | **SURVIVED** | **147 passed（放行）** | restored=True clean=True |
| N3 | `canonical_decision_hash` 改为 md5 | KILLED | 3 failed, 144 passed；`test_canonical_decision_json_and_hash_are_pinned_literals` | restored=True clean=True |
| N4 | `canonical_decision_hash` 改为 sha1 | KILLED | 3 failed, 144 passed；同上 | restored=True clean=True |
| N5 | REQUEST_HUMAN 选项上限 12 → 13 | KILLED | 1 failed, 146 passed；`test_request_human_payload_negatives[payload0]` | restored=True clean=True |
| N6 | WAIT 的 `wait_for` 上限 32 → 33 | KILLED | 1 failed, 146 passed；`test_wait_payload_negatives[payload0]` | restored=True clean=True |
| N7 | DECLARE_BLOCKED 的 `blockers` 上限 16 → 17 | KILLED | 1 failed, 146 passed；`test_wait_payload_negatives[payload0]` | restored=True clean=True |
| **N8** | **放宽 `UncertaintySeverity` 为任意字符串（去掉 `enum_of`）** | **SURVIVED** | **147 passed（放行）** | restored=True clean=True |
| **N9** | **放宽 `AlternativeDisposition` 为任意字符串（去掉 `enum_of`）** | **SURVIVED** | **147 passed（放行）** | restored=True clean=True |
| N10 | 顶层增加 `from agent_orchestrator.orchestrator import ...`（分层违规探测） | KILLED | 2 errors；`test_planning_decision_contract.py`（导入即失败） | restored=True clean=True |

**全量套件交叉确认（脚本 `/tmp/h1a2a/r2/run_broad.py`）**：三个存活变异在 `tests/orchestrator/full_target` 整包下仍全部放行：

```
N8: rc=0 | 3107 passed, 2 skipped | restored=True
N9: rc=0 | 3107 passed, 2 skipped | restored=True
N2: rc=0 | 3107 passed, 2 skipped | restored=True
```

即：**N2、N8、N9 在核验副本的全部 3107 条测试下均无法被杀死。**

### 复核 2 逐条问题

#### P0 阻塞

无。

#### P1 必修

**P1-2（新）`UncertaintySeverity` 的封闭枚举只钉了成员表，未钉解码路径**

- **位置：** 实现 `src/agent_orchestrator/contracts/planning_decisions.py:825`（`PlanningUncertaintyV1.__post_init__` 的 `enum_of(UncertaintySeverity, ...)`）；测试 `tests/orchestrator/full_target/test_planning_decision_envelope.py:218`（`test_uncertainty_severity_members_are_pinned`，仅枚举成员清单）。
- **问题：** 现有测试只断言枚举类“有哪些成员”，不经过 `from_json`；把解码校验改成 `self.severity = self.severity`（任意字符串均放行）后，147 条本片测试与 3107 条全量测试**全部放行**。补遗二 §2 要求“不确定性严重度 = LOW/MEDIUM/HIGH 封闭枚举，原样生效”，即未知 `severity` 必须在解码时被拒。
- **复现（变异 N8）：** 将 `uncertainties` 的 `severity` 校验改为直接赋值，运行两个测试文件 → `147 passed`；全量套件 → `3107 passed, 2 skipped`。此时 `{"statement":"u","severity":"BOGUS","affects":[]}` 被接受。修复前应先在测试中加如下红测：

```python
def test_uncertainty_severity_rejects_unknown_value() -> None:
    raw = _envelope("REFINE", REFINE_PAYLOAD,
                    uncertainties=[{"statement": "u", "severity": "BOGUS", "affects": []}])
    with pytest.raises(ContractError):
        PlanningDecisionEnvelopeV1.from_json(raw)
```

**P1-3（新）`AlternativeDisposition` 的封闭枚举同样只有成员表测试**

- **位置：** 实现 `planning_decisions.py:878`（`AlternativeSummaryV1.__post_init__` 的 `enum_of(AlternativeDisposition, ...)`）；测试 `test_planning_decision_envelope.py:226`（`test_alternative_disposition_members_are_pinned`）。
- **问题：** 同 P1-2 的形态。变异 N9 把 `enum_of(...)` 换成直接赋值后，本片 147 条与全量 3107 条**全部放行**；`{"method_ref":null,"label":"l","disposition":"BOGUS","reason":"r"}` 被接受。补遗二 §2 要求备选处置为 CONSIDERED/REJECTED/DEFERRED 封闭枚举，未知值必须拒绝。
- **复现（变异 N9）：** 同上。建议先加：

```python
def test_alternative_disposition_rejects_unknown_value() -> None:
    raw = _envelope("REFINE", REFINE_PAYLOAD,
                    alternatives=[{"method_ref": None, "label": "l",
                                   "disposition": "BOGUS", "reason": "r"}])
    with pytest.raises(ContractError):
        PlanningDecisionEnvelopeV1.from_json(raw)
```

**P1-4（新）canonical 的“非 ASCII 不转义”未被钉住（新样例本身只含 ASCII）**

- **位置：** 新增测试的固定样例 `CANONICAL_SAMPLE`（`test_planning_decision_envelope.py`，`rationale="pick the registered method"`，全 ASCII）；实现 `planning_decisions.py:1734`。
- **问题：** 首轮 P1 的修复用了一个**纯 ASCII** 样例，因此 `ensure_ascii=True` 的变异（N2）不被任何断言触及。规格 §15 要求 `canonical_decision_json == canonical_json(decision.to_json())`，而共享 `simple_harness.contracts.canonical_json`（`src/simple_harness/contracts/json.py:56`）明确 `ensure_ascii=False`；V2 §13 的示例 `rationale` 本身就是中文。变异 N2 下，含中文的决策 canonical JSON 与规格 `canonical_json` 分叉（`canonical == spec canonical_json for CJK: False`），但 147 / 3107 条测试全部放行。
- **复现（变异 N2）：** 把 `canonical_decision_json` 换成 `ensure_ascii=True` 的 `json.dumps(..., sort_keys=True)`，lib 内部 `cj(e) != canonical_json(e.to_json())` 对任意含非 ASCII 的决策成立，而 targeted 与全量套件均 `passed`。建议给固定样例加入非 ASCII 文本（例如把 `rationale` 改为规格示例中的中文），并保留 `canonical_decision_json(envelope) == _independent_canonical_json(envelope.to_json())`（`_independent_canonical_json` 已用 `ensure_ascii=False`）与字面量钉死断言；随后同步更新 `CANONICAL_SAMPLE_JSON` / `CANONICAL_SAMPLE_HASH`。

#### P2 建议

- **P2-4（新）：** `test_wait_payload_negatives[payload0]` 同时被 N6 / N7 命中，说明这两个限额反例都落在同一参数化用例上；建议未来把 WAIT `wait_for` 上限与 DECLARE_BLOCKED `blockers` 上限的断言做的失败信息更可区分（仅影响诊断体验，不影响判定）。
- 首轮 P2-1（`decision_payload_hash` 命名 vs `canonical_decision_hash`）、P2-3（journal 缺口记录）仍适用；P2-2 已在本轮修复（弱断言换成 `len == 64`）。

### 复核 2：命令尾行原文

```
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_contract.py tests/orchestrator/full_target/test_planning_decision_envelope.py -q -p no:cacheprovider
........................................................................ [ 97%]
...                                                                      [100%]
147 passed in 0.15s
```

```
$ PYTHONPATH=src uv run --offline ruff check src/agent_orchestrator/contracts/planning_decisions.py tests/orchestrator/full_target/test_planning_decision_contract.py tests/orchestrator/full_target/test_planning_decision_envelope.py
All checks passed!
ruff exit=0
```

```
（全量套件，作为存活变异的交叉确认）
N8: 3107 passed, 2 skipped (0:02:12)
N9: 3107 passed, 2 skipped (0:02:04)
N2: 3107 passed, 2 skipped (0:02:05)
```

### 复核 2：恢复与工作树状态

- 每个变异应用前，实现文件均备份到 `/tmp/h1a2a/r2/orig_impl.py`（测试文件备份 `/tmp/h1a2a/r2/orig_test.py`），变异后从副本覆盖恢复，逐一 `git status --short` 确认为空。
- 本轮全部 12 个变异 `restored=True clean=True`；结束后实现文件 sha256 仍为 `6d2bcc250217968099f717caa6e5928e2a8aba12c0899d85b58577d2e9ed8b40`（与首轮相同）。
- 本轮未用 `git stash / checkout / reset --hard / rebase / push`，未 merge，未 push，未 commit，未读密钥文件；工作树结束时仅本报告为未跟踪新增。

### 复核 2 判定

未发现 P0；新增 3 条 P1（封闭枚举解码路径 2 条 + canonical 非 ASCII 1 条），均可在本片允许改动集合内以“测试先行”补齐。按判定规则为 **修后可合**。

---

## 复核 3

- **复核日期：** 2026-09-19（同一核验会话续做）
- **上一轮核验副本 HEAD：** `a589bbbe399cb54f7da7955f22b58a96e920ed76`
- **当前核验副本 HEAD：** `72d440c8eab631c1ac9cafa55c8e0749ba63a3c3`
- **本轮新增提交：** `72d440c test(h1-a2a): pin closed-enum decode paths and non-ascii canonical json`
- **判定规则：** 有 P0 → 不可合；只有 P1 → 修后可合；只有 P2 或无 → 可合。

### 复核 3 结论

- **上一轮 3 个存活变异（N2、N8、N9）重跑后全部 KILLED**；另外补做的 `AssumptionRisk` 与 `ReplanTriggerHintV1.suggested_decision` 解码路径变异也 KILLED。
- 本轮另做新变异，**发现 1 个新存活变异（`AssumptionV1.required_for` 去掉 `_decision_type` 校验，记为 Q3）**；该变异在本片 151 条与**全量 `tests/orchestrator/full_target`（3111 passed）下均存活**。
- 未发现 P0；无越界改动。新增 1 条 P1（测试缺口）。
- 另有两个探针（P6、P8）在尝试“弱化”检查后仍被拒——经逐条判定属**探针无效**（未真正去掉校验），不计为缺口，已在下方说明。

**复核 3 判定：修后可合。**

### 步骤 1：本轮改了什么、是否越界

```
$ git diff --name-status a589bbbe399cb54f7da7955f22b58a96e920ed76 72d440c8eab631c1ac9cafa55c8e0749ba63a3c3
M   plans/llm-native-htn/H1/journal-A2a.md
M   tests/orchestrator/full_target/test_planning_decision_envelope.py

$ git diff --stat a589bbbe399cb54f7da7955f22b58a96e920ed76 HEAD -- src/
（空）
```

- 改动只在 `journal-A2a.md` 与本片测试文件；**实现文件 `src/agent_orchestrator/contracts/planning_decisions.py` 零改动**（sha256 仍为 `6d2bcc25…ed8b40`），在 A2a 允许改动集合内，**无越界**。
- 新增内容（`tests/orchestrator/full_target/test_planning_decision_envelope.py`）：
  - 顶部新增 `from simple_harness.contracts import canonical_json`（`:53`）。
  - 四个“解码路径封闭性”反例：`test_uncertainty_severity_decode_path_is_closed`（`:270`）、`test_alternative_disposition_decode_path_is_closed`（`:282`）、`test_assumption_risk_decode_path_is_closed`（`:294`）、`test_replan_suggested_decision_decode_path_is_closed`（`:312`）。
  - `CANONICAL_SAMPLE.rationale` 由英文改为 CJK 文本「选择已注册且当前可适用的方法。」，同步更新 `CANONICAL_SAMPLE_JSON` 与 `CANONICAL_SAMPLE_HASH`（`:797`，值为 `18bb6817f7117b20de4c9da5d9f9411f542cef5d37188f89adcd3e2ac8d5751f`）。
  - `test_canonical_decision_json_matches_an_independent_canonicalisation` 增加 `canonical_decision_json(envelope) == canonical_json(envelope.to_json())`（共享合同层 canonical，不转义非 ASCII）。
- **独立复核这些钉死值非自证循环**（脚本 `/tmp/h1a2a/r3/verify_pins.py`）：样例含非 ASCII；`CANONICAL_SAMPLE_JSON` 等于标准库 `json.dumps(..., ensure_ascii=False, allow_nan=False, separators=(",", ":"), sort_keys=True)`，也等于共享 `simple_harness.contracts.canonical_json`；`CANONICAL_SAMPLE_HASH` 等于 `sha256(CANONICAL_SAMPLE_JSON)`；被测 `canonical_decision_json` / `canonical_decision_hash` 与该字面量相等。全部 `True`。
- 顺带修复了上一轮 P1-2 / P1-3（`UncertaintySeverity`、`AlternativeDisposition` 解码路径）与 P1-4（非 ASCII canonical）。

### 步骤 2：上一轮存活变异逐个重跑（全部 KILLED）

| 编号 | 变异（同上一轮） | 结果 | 测试尾行 / 首个失败 | 恢复 |
|---|---|---|---|---|
| R3-N2 | `canonical_decision_json` 改为 `ensure_ascii=True` | **KILLED** | 2 failed, 149 passed；`test_canonical_decision_json_matches_an_independent_canonicalisation` | restored=True clean=True |
| R3-N8 | `UncertaintySeverity` 去掉 `enum_of` | **KILLED** | 1 failed, 150 passed；`test_uncertainty_severity_decode_path_is_closed` | restored=True clean=True |
| R3-N9 | `AlternativeDisposition` 去掉 `enum_of` | **KILLED** | 1 failed, 150 passed；`test_alternative_disposition_decode_path_is_closed` | restored=True clean=True |

原始代码 targeted 全绿：`151 passed in 0.15s`（分文件计数：A1 文件 `52 passed` + A2a 文件 `99 passed` = 151；A1 文件本轮未改）。

### 步骤 3：本轮新增变异

**新增且 KILLED（6 个）**

| 编号 | 新变异 | 结果 | 首个失败 |
|---|---|---|---|
| P1 | `AssumptionRisk` 去掉 `enum_of` | KILLED（1 failed, 150 passed） | `test_assumption_risk_decode_path_is_closed` |
| P2 | `ReplanTriggerHintV1.suggested_decision` 去掉 `enum_of` | KILLED（1 failed, 150 passed） | `test_replan_suggested_decision_decode_path_is_closed` |
| P4 | `BlockerCode` 去掉 `enum_of` | KILLED（2 failed, 149 passed） | `test_declare_blocked_payload_negatives` |
| P5 | `ResumableIf` 去掉 `enum_of` | KILLED（1 failed, 150 passed） | `test_declare_blocked_payload_negatives` |
| P7 | canonical 改为其他序列化（降序/反转） | KILLED（2 failed, 149 passed） | `test_canonical_decision_json_matches_an_independent_canonicalisation` |
| Q8 | REFINE 解码静默丢弃未知字段 | KILLED（3 failed, 148 passed） | `test_payload_shape_must_match_decision_type`、`test_refine_payload_negatives` |
| Q8b | 顶层信封静默丢弃未知字段 | KILLED（3 failed, 148 passed） | `test_payload_shape_must_match_decision_type`、`test_refine_payload_negatives` |

**新增且 SURVIVED（1 个真缺口 + 2 个无效探针）**

| 编号 | 新变异 / 探针 | 结果 | 说明 |
|---|---|---|---|
| **Q3** | **`AssumptionV1.required_for` 去掉 `_decision_type` 校验（`sequence_of(..., _decision_type, ...)` → `tuple(self.required_for)`）** | **SURVIVED** | **真缺口（P1-5）**：`required_for=["NOT_A_DECISION_TYPE"]` 被接受并原样往返 |
| P3 | 同上（首次写法，`tuple(self.required_for)`） | SURVIVED | 与 Q3 同一变异（早期编号），已并入 Q3 判定 |
| P6 | `WaitDecision.wait_for` 试图放宽但保留了 `_planning_ref` 校验 | SURVIVED | **探针无效**：改后 `wait_for="x"` 仍被拒（`accepted_wait_for_str: False`），未真正去掉校验，不计缺口 |
| P8 | 试图在 `_decode_payload` 里剔除 `evil_` 前缀键 | SURVIVED | **探针无效**：`totally_unknown`/`evil_x` 仍被拒（`fields_of` 仍生效），未真正放开未知字段 |

**Q3 全量套件交叉确认**（脚本 `/tmp/h1a2a/r3/run_broad_q3.py`）：

```
Q3 full suite: 3111 passed, 2 skipped in 130.86s | rc=0
```

即 **Q3 在核验副本全部 3111 条测试下无法被杀死。**

### 复核 3 逐条问题

#### P0 阻塞

无。

#### P1 必修

**P1-5（新）`AssumptionV1.required_for` 的成员封闭性只钉了“能写出九个决定类型名”，未钉住解码时对任意字符串的拒绝**

- **位置：** 实现 `src/agent_orchestrator/contracts/planning_decisions.py:753`（`AssumptionV1`）与 `:765-774`（`required_for` 的 `sequence_of(..., _decision_type, limit=len(PlanningDecisionType))`）；测试 `tests/orchestrator/full_target/test_planning_decision_envelope.py:298-307`（`_assumptions()` 仅用 `["REFINE"]` 合法值）与 `:440-449`（限额用例同样只用合法值）。
- **问题：** 补遗二 §2 规定“假设的 required_for 取九个决定类型名”，即该元组在解码时必须逐项校验为 `PlanningDecisionType`。现有测试只喂合法值，从不喂未知值；把 `sequence_of(..., _decision_type, ...)` 换成 `tuple(self.required_for)`（去掉成员校验与长度上限）后，本片 151 条与全量 3111 条测试**全部放行**，且未知值被原样保留：
  `{"key":"a","statement":"s","required_for":["NOT_A_DECISION_TYPE"],"risk":"LOW","suggested_predicate_key":null}` → 接受，`to_json()` 回读为 `['NOT_A_DECISION_TYPE']`。
- **复现（变异 Q3）：** 将上述 `sequence_of(...)` 调用替换为 `tuple(self.required_for)`，运行两个测试文件 → `151 passed`；全量套件 → `3111 passed, 2 skipped`。修复前应先在测试中加红测：

```python
def test_assumption_required_for_decode_path_is_closed() -> None:
    raw = _envelope(
        "REFINE",
        REFINE_PAYLOAD,
        assumptions=[
            {
                "key": "a",
                "statement": "s",
                "required_for": ["NOT_A_DECISION_TYPE"],
                "risk": "LOW",
                "suggested_predicate_key": None,
            }
        ],
    )
    with pytest.raises(ContractError):
        PlanningDecisionEnvelopeV1.from_json(raw)
```

  并建议顺带覆盖 `required_for` 为空列表是否允许（补遗二未规定下界，需按语义裁定后钉死）。

#### P2 建议

- **P2-5（新）：** 第 2 轮 P2-4（WAIT 与 DECLARE_BLOCKED 两个限额反例落在同一参数化用例、失败信息不可区分）本轮仍未处理；本轮 N6/N7 的失败确实同落在 `test_wait_payload_negatives[payload0]`，建议后续把两处限额断言拆成独立、可区分的用例。
- 首轮 P2-1（任务书草案用 `decision_payload_hash`，本片用 `canonical_decision_hash`）仍属 H1-C/H1-H 接线片命名统一事项。
- 首轮 P2-3（journal 缺口记录）已在本轮 journal 追加段体现。

### 复核 3：命令尾行原文

```
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_contract.py tests/orchestrator/full_target/test_planning_decision_envelope.py -q -p no:cacheprovider
........................................................................ [ 95%]
.......                                                                  [100%]
151 passed in 0.10s
```

```
$ PYTHONPATH=src uv run --offline ruff check src/agent_orchestrator/contracts/planning_decisions.py tests/orchestrator/full_target/test_planning_decision_contract.py tests/orchestrator/full_target/test_planning_decision_envelope.py
All checks passed!
ruff exit=0
```

```
（全量套件，作为 Q3 存活变异的交叉确认）
Q3 full suite: 3111 passed, 2 skipped in 130.86s (0:02:10) | rc=0
```

### 复核 3：恢复与工作树状态

- 每个变异应用前，实现文件均备份到 `/tmp/h1a2a/r3/orig_impl.py`（测试文件备份 `/tmp/h1a2a/r3/orig_test.py`），变异后从副本覆盖恢复，逐一 `git status --short` 确认为空。
- 本轮全部变异 `restored=True clean=True`；结束后实现文件 sha256 仍为 `6d2bcc250217968099f717caa6e5928e2a8aba12c0899d85b58577d2e9ed8b40`（与首轮相同，实现零改动）。
- 本轮未用 `git stash / checkout / reset --hard / rebase / push`，未 merge，未 push，未 commit，未读密钥文件；工作树结束时仅本报告为未跟踪新增。

### 复核 3 判定

未发现 P0；新增 1 条 P1（`AssumptionV1.required_for` 解码路径成员校验），可在本片允许改动集合内以“测试先行”补齐；两个 SURVIVED 探针（P6、P8）经判定为探针无效，不构成缺口。按判定规则为 **修后可合**。


---

## 复核 4（终轮，2026-09-19 01:30）——核验员最终回复原文

> 说明：终轮核验员在分离副本里续写了本报告，但流水线保存未提交文件时没处理中文文件名（已修，Host d388130c 之后的提交），副本随清理丢失。以下为核验员终轮最终回复原文，结论行在末尾。

## 结果

**本轮（复核 4）范围：** HEAD 已从 `72d440c8` 前进到 `7920d1150862c71e10820ec3587507ce23ad1d26`（新增提交 `7920d11 test(h1-a2a): pin assumption required_for decode path`），故按“上一轮 HEAD → 当前 HEAD”只审这一段。

**越界检查：**
- `git diff --name-status 72d440c8 7920d11` → 仅 `plans/llm-native-htn/H1/journal-A2a.md` 与 `tests/orchestrator/full_target/test_planning_decision_envelope.py`。
- `git diff --stat 72d440c8 7920d11 -- src/` 为空：**实现零改动**（sha256 仍 `6d2bcc25…ed8b40`）。无越界、无 merge、无 push。
- 新增测试 `test_assumption_required_for_decode_path_is_closed`（`:312`）即复核 3 的 P1-5 建议红测，属纯测试补强。

**上一轮存活变异重跑（1/1 KILLED）：**
- R4-Q3（`AssumptionV1.required_for` 去掉 `_decision_type` 校验）→ **KILLED**（1 failed, 151 passed；`test_assumption_required_for_decode_path_is_closed`）。复核 2/3 的 6 条 P1 至此全部闭环。

**本轮新增变异（8 个：6 KILLED / 2 SURVIVED）：**
- KILLED：R4-Q3b（只放宽成员校验保留 `sequence_of`）、R7（PlanningRefV1 语义修订下界 1→0）、R8（VersionedTypeRefV1.version 下界 1→0）、R9（subject_key 256→257）、R10（rationale 4000→4001）、R11（content_ref content_hash 放宽为任意字符串）。
- **SURVIVED：R5（required_for 上限 9→10）、R6（加 `minimum=0`，与默认值等价）**——经逐条判定均为**弱探针/无效探针**，不构成缺口：R5 只让「重复九个类型」通过（补遗二未要求去重），R6 行为与默认完全等价。已在报告中给出 P2-6 建议（请规格方明确 required_for 是否需去重/非空）。

**判定：** 无 P0、无新增 P1 → **可合**。

**文件变更（未 commit）：**
- `plans/llm-native-htn/H1/reviews/核验-H1-A2a-2026-09-18.md`（首轮 + 复核 2 + 复核 3 + 复核 4，39 437 字节，无 U+FFFD，代码围栏配平）。该报告在本轮刷新中再次被清掉，已按原任务书路径重建；未新建其它报告。
- 未改动任何源码或测试；本轮 9 个变异均从 `/tmp/h1a2a/r4/orig_impl.py` 恢复，逐个 `git status --short` 确认为空。

**测试运行（pass/fail）：**
- 两个目标测试文件：**152 passed**（A1 52 + A2a 100）。
- 全量 `tests/orchestrator/full_target`（原始树）：**3112 passed, 2 skipped**（跳过为既有环境项：`SH_PANDA_PARSER` / `--run-real-provider`）。
- `ruff check`（实现 + 两测试文件）：**All checks passed!**

**未做项：** 属 H1-A2b 的 JSON Schema、黄金样例、打包、`test_planning_decision_json_schema.py`；属 H1-F/H1-H 的仅解码类型准入与 `PlanningDecisionEvaluated` 事件接线；P2-1（`decision_payload_hash` 命名统一）、P2-4（限额反例诊断粒度）、P2-6（required_for 是否去重/非空）留待后续裁定。

VERDICT: 可合