# 独立核验报告：切片 H1-A1（PlanningDecision 合同核心）

- **核验日期：** 2026-09-18
- **核验对象：** 当前目录（SDK 分离副本）HEAD `7bcbd1b` 相对基线 `e1684b7`（`docs(h0): realign the baseline package to the V2 plan layout`）的全部改动
- **被核验提交：**
  - `e971a07 feat(h1-a1): planning-decision contract core (enums, refs, request binding, feedback, decision id)`
  - `7bcbd1b test(h1-a1): pin wire identity constants and §17 ref boundaries`（针对上一轮核验的 P1 补测）
- **核验员：** 独立会话（不知实施者，只看代码与证据）
- **规格来源：** V2 计划 `simpleharness-llm-native-htn-execution-plan-v2.zh-CN.md` 第 5、11、12、16、17、33–36、39、40 节；`H1-A-B-C任务书草案-2026-09-18.zh-CN.md`；`H1-V2对照源码冲突检查-2026-09-18.zh-CN.md`
- **判定规则：** 有 P0 → 不可合；只有 P1 → 修后可合；只有 P2 或无 → 可合。

## 结论

**未发现 P0。** 检查项 1–5 全部通过；上一轮判为 P1 的 4 个测试缺口，在 `7bcbd1b` 中已补测并复验为 KILLED（原 7 个存活变异现全部 KILLED）。

**但仍存在一组 P1 测试缺口：** 本片自称“钉死规格”，实测在**反馈/问题明细（§39/§40）字段的闭集与类型校验**上，多个“放宽校验”的实现变异不会被任何用例检出。这些变异会造成真实行为回退（非法 `status`、非法拒绝码、非法 `field_path`/`expected` 等被接受），且被测代码路径是模型输入入口，属安全相关。需补测后放行。

因此判定为 **修后可合**。（另附 P2 建议若干。）

---

## 一、核验环境与前置确认

- 逻辑路径 `…/simple-harness-sdk-h1a1-verify-h1-a1` 与物理路径 `…/simple-runtime-sdk-h1a1-verify-h1-a1` 为同一目录（本机 `PWD`/`::getcwd` 已解析为物理名）；`git rev-parse --git-dir` = `…/simple-runtime-sdk/.git/worktrees/simple-runtime-sdk-h1a1-verify-h1-a1`，即 SDK 分离副本 worktree。
- 网络受限（`pypi.org` 不可达），所有 `uv run` 均加 `--offline`；venv 由本目录 `.venv` 提供。
- **模块路径确认：** `agent_orchestrator` 与 `agent_orchestrator.contracts.planning_decisions` 均位于当前目录内：
  `OK: …/simple-runtime-sdk-h1a1-verify-h1-a1/src/agent_orchestrator/contracts/planning_decisions.py`
- 全程未用 `git stash / checkout / reset --hard / rebase / push`，未 commit，未读密钥文件；工作树结束时干净。

## 二、逐项核对结果

### 1. 越界（改动文件范围）——通过

`git diff --name-status e1684b7 HEAD`：

```text
A	plans/llm-native-htn/H1/journal.md
M	plans/llm-native-htn/journal.md
A	src/agent_orchestrator/contracts/planning_decisions.py
A	tests/orchestrator/full_target/test_planning_decision_contract.py
```

- 代码只新增 `src/agent_orchestrator/contracts/planning_decisions.py`；测试只新增 `tests/orchestrator/full_target/test_planning_decision_contract.py`；文档只在 `plans/llm-native-htn/` 下。**通过。**
- `contracts/` 既有文件零改动（`__init__.py`、`semantic_base.py`、`htn.py`、`models.py`、`ids.py` 的 `git diff --name-only e1684b7 HEAD -- …` 输出为空）。**通过。**
- `7bcbd1b`（补测）只改测试与 journal，**未改任何 `src/`**。**通过。**
- 无删除、无 mode 变更（`git diff --summary` 仅三条 create）。

### 2. 逐字一致（脚本抽取 V2 文本 → 与代码求差集）——通过

脚本 `/tmp/h1a1b/diff_vs_code.py`（章节切块 + 正则抽取，再与枚举成员求对称差集）。**差集打印（原文）：**

```text
[PlanningDecisionType] V2=9 CODE=9
   差集 名称 V2-CODE = []  CODE-V2 = []
   差集 取值 V2-CODE = []  CODE-V2 = []
   同名字不同值 = []
   并集差(名称) = []  并集差(值) = []
   顺序一致 = True

[PlanningRefKind] V2=15 CODE=15
   差集 名称 V2-CODE = []  CODE-V2 = []
   差集 取值 V2-CODE = []  CODE-V2 = []
   同名字不同值 = []
   并集差(名称) = []  并集差(值) = []
   顺序一致 = True

[PlanningDecisionRejectionCode] V2=36 CODE=36
   差集 名称 V2-CODE = []  CODE-V2 = []
   差集 取值 V2-CODE = []  CODE-V2 = []
   同名字不同值 = []
   并集差(名称) = []  并集差(值) = []
   顺序一致 = True

[PlanningDecisionStatus] V2=8 CODE=8
   差集 名称 V2-CODE = []  CODE-V2 = []
   差集 取值 V2-CODE = []  CODE-V2 = []
   同名字不同值 = []
   并集差(名称) = []  并集差(值) = []
   顺序一致 = True

[§16 limits] V2=11 CODE=11
   差集 V2-CODE = []  CODE-V2 = []
   值不同 = {}
   并集差 = []

[forbidden in enum members] {'SELECT_METHOD': False, 'method_instance': False, 'fact': False, 'REUSE_RESULT': False, 'RETIRE_METHOD_USE': False, 'SHARE_ACTIVE': False, 'REUSE_ACCEPTED': False, 'bind_shared_goal': False}
```

§11 决定类型 9/9、§17 引用种类 15/15、§33 拒绝码 36/36、§36 状态 8/8、§16 限额 11/11，**名称与取值不多不少、顺序一致**；`SELECT_METHOD`/`method_instance`/`fact` 均不存在于枚举。**通过。**
（另核 §12 H1 列：V2 表 10 行 ↔ `H1_DECISION_ENABLEMENT` 10 项，一一对应。**通过。**）

### 3. `compute_decision_id`（独立 hashlib 实现比对）——通过

```text
codec constant == spec: True
'req-0001'                 ord=0          expected=pd-6ddace2ec77d6efa64fde25e got=pd-6ddace2ec77d6efa64fde25e match=True prefix=True len=27
'req-0002'                 ord=1          expected=pd-93f54a08b6f60973e1c1f6d4 got=pd-93f54a08b6f60973e1c1f6d4 match=True prefix=True len=27
'req/with:punctuation-42'  ord=7          expected=pd-0941c8cbf47ad7726e4d6bfb got=pd-0941c8cbf47ad7726e4d6bfb match=True prefix=True len=27
'req-abcdef'               ord=123456789  expected=pd-2bcb04f9e785b9539c6bea3c got=pd-2bcb04f9e785b9539c6bea3c match=True prefix=True len=27
'R'                        ord=0          expected=pd-684baacafc7ca578bc4c3732 got=pd-684baacafc7ca578bc4c3732 match=True prefix=True len=27
ALL_MATCH: True
```

5 组输入全一致；分隔符 `\x1f`、codec 常量 `planning-decision-codec-v1`、前缀 `pd-`、长度 27 全部符合 §35。拒绝项：`bool`/`负数`/`float`/`str` ordinal、大写与短 hash、空 request_id、非字符串 request_id 全部 `ContractError`。**通过。**

### 4. dataclass 严格性、往返、§34 字段——通过（含 2 项规格许可项）

- **往返：** 五个类型 `from_json(to_json(x)) == x` 全部 `True`。**通过。**
- **未知字段：** 五个类型逐一插入 `unexpected_authority_override` 全部被拒。**通过。**
- **缺字段：** 逐字段 pop 后仅 `PlanningProblemDetailV1.expected/observed` 可缺（V2 §40 明写 `= None` 默认值，属**规格允许**）；其余全部被拒，`PlanningRequestBinding` 14 字段无一漏检。**通过。**
- **错类型：** `semantic_revision=True/"1"/0`、`package_version=True/0`、`created_at=True/NaN`、`planning_rounds_remaining=True`、`detail=123`、`previous_decision_id=123`、`changed_refs="x"` 等全部被拒（bool 不冒充 int）。**通过。**
- **`content_hash`：** 大写/63/65/非 hex/空串/换行/None/整数 全部被拒。**通过。**
- **§34 字段：** V2 §34 的 13 字段全部存在且**顺序一致**，末尾按 BL-7/I5 追加 `intent_id`（`order == §34 + ["intent_id"]` 为 `True`）。**通过。**
- 备注：`feedback.rejection_codes=["PARAMETER_INVALID"]`（JSON 数组）被接受——这是 `sequence_of` 对 `list`→`tuple` 的正规行为（wire 上本来就是数组），不是缺陷。

### 5. 复用与导入——通过

- 复用 `from .models import ContractError` 与 `semantic_base` 的 `enum_of/fields_of/flag/hash_hex/identifier/index/optional_index/sequence_of/text`；**未另造**错误类型或边界校验器（`grep "class .*Error"` 无结果）。
- 7 个新类型/函数在本模块外**无同名定义**（`grep` 全 `src/` 仅本模块）。
- 无循环导入：单独导入子模块与导入 `agent_orchestrator.contracts` 包均成功。

### 6. 变异测试（存活 = 测试缺口）

方法：每次变异前把原文件复制到 `/tmp/h1a1b/orig/`；变异时追加长度不同的标记行（避免同尺寸源码的 `.pyc` 失效问题）并设 `PYTHONDONTWRITEBYTECODE=1`；跑该测试文件；**用副本覆盖恢复**并核对 sha256 与 `git status --short`（恢复后干净、两文件 sha256 与备份一致）。

**(a) 清单指定的 4 个必做变异：**

| # | 变异 | 结果 | 触发失败用例 |
|---|---|---|---|
| M1 | 枚举值 `REFINE = "REFINE"` → `"REFINE_V2"` | **KILLED** | `test_planning_decision_type_members_are_pinned` |
| M2 | 去掉未知字段检查（permissive `fields_of`） | **KILLED** | `test_unknown_field_is_rejected[*]`（5 条） |
| M3 | `decision_id` 分隔符 `\x1f` → `\|` | **KILLED** | `test_decision_id_matches_the_independent_literal` |
| M4 | 放宽 `content_hash` 校验（任意字符串） | **KILLED** | `test_planning_ref_illegal_inputs_are_rejected` |

**(b) 上一轮 7 个存活变异（本轮复验 —— 修复已生效，全部 KILLED）：**

| # | 变异 | 上轮 | 本轮 | 触发失败用例 |
|---|---|---|---|---|
| Y1 | `PLANNING_DECISION_SCHEMA_VERSION = 1` → `2` | SURVIVED | **KILLED** | `test_wire_identity_constants_are_pinned` |
| Y2 | `PLANNING_DECISION_V1` → `…-v2` | SURVIVED | **KILLED** | `test_wire_identity_constants_are_pinned` |
| Y3 | `LEGACY_PLANNING_PROTOCOL` → `…-v2` | SURVIVED | **KILLED** | `test_wire_identity_constants_are_pinned` |
| Y6 | `planning_ref.semantic_revision` 下界 `1` → `0` | SURVIVED | **KILLED** | `test_planning_ref_illegal_inputs_are_rejected[value2]` |
| Y7 | `binding.package_version` 下界 `1` → `0` | SURVIVED | **KILLED** | `test_request_binding_package_version_lower_bound_is_pinned` |
| X5 | `MAX_PLANNING_REF_ID` 256 → 255（及 300/512 加宽） | SURVIVED | **KILLED** | `test_max_planning_ref_id_is_pinned_with_boundaries` |
| Y12 | `PlanningRefV1.content_hash` 加默认值 | SURVIVED | **KILLED** | `test_planning_ref_all_fields_are_required_by_construction` |

**(c) 本轮扩展变异（核验员加做）——结果分布：**

| # | 变异 | 结果 | 备注 |
|---|---|---|---|
| X1 | `PlanningRefKind` 增 `method_instance` | KILLED | 枚举钉死 |
| X2 | `PlanningDecisionType` 增 `SELECT_METHOD` | KILLED | 枚举钉死 |
| X3 | `PlanningDecisionStatus.COMMITTED` → `"DONE"` | KILLED | 枚举钉死 |
| X4 / P12 / P13 / S5 | 各限额常量 ±1 | KILLED | §16 限额钉死 |
| X6 | codec 常量后缀改 v2 | KILLED | 2 条用例命中 |
| X7 | `PROPOSE_METHOD` 改 executable | KILLED | §12 钉死 |
| X8 / Y10 / Q8 | `to_json` 大写 hash / 丢 content_hash / 丢 kind | KILLED | 往返钉死 |
| Y4 | `AssumptionRisk` 增 `CRITICAL` | KILLED | 枚举钉死 |
| Y8 / Y9 | `decision_id` 截断 24→23 / 前缀 `pd-`→`pdv-` | KILLED | 字面量钉死 |
| Y11 / P6 | 增删 `H1_DECISION_ENABLEMENT` 条目 | KILLED | §12 钉死 |
| P1 / P2 / S1 | `MAX_PLANNING_REF_ID` 加宽 300/512/1024 | KILLED | 边界双向钉死 |
| P3 | `package_version` 下界 1 → 2 | KILLED | 上界侧钉死 |
| P7 / P8 / S2 / S3 / S4 | 删除/改名各枚举成员 | KILLED | 成员表钉死 |
| P11 | `decision_id` 编码 utf-8 → utf-16 | KILLED | 字面量钉死 |
| P9 | hash 允许 63–64 位 | KILLED | 负例钉死 |
| P17 | `binding.request_id` 长度上限 512 → 64 | KILLED | 见下方说明 |
| Q3 / Q4 | `problem.subject_ref` / `feedback.budgets` 收裸 dict | KILLED | 往返钉死 |
| **P15** | `feedback.status` 由 `enum_of` 改 `str()` | **SURVIVED** | 见 P1-A |
| **Q1** | `feedback.rejection_codes` 元素由 `enum_of` 改 `str()` | **SURVIVED** | 见 P1-B |
| **Q2** | `problem.code` 由 `enum_of` 改 `str()` | **SURVIVED** | 见 P1-C |
| **P14** | `problem.field_path` 由 `identifier` 改 `str()` | **SURVIVED** | 见 P1-D |
| **Q5** | 去掉 `problem.expected/observed` 的 `_optional_text` | **SURVIVED** | 见 P1-E |
| **Q6** | 去掉 `binding.intent_id` 的 `identifier` | **SURVIVED** | 见 P2-1 |
| **Q7** | 去掉 `binding.prompt_hash` 的 `_hash` | **SURVIVED** | 见 P2-2 |
| **P4** | `binding.base_plan_revision` 下界 0 → 1 | **SURVIVED** | 见 P2-3 |
| **P5** | `PlanningRetryBudgetView` 四个计数下界 0 → 1 | **SURVIVED** | 见 P2-4 |
| **Q10** | `PlanningFeedbackV1.to_json` 的 `str(status)` 改裸值 | **SURVIVED** | 见 P2-5（等价变异） |
| **P10** | `decision_id` 的 `str(ordinal)` 改 `repr(ordinal)` | **SURVIVED** | 见 P2-6（等价变异） |
| **Q9** | `budget.repeated_failure…` 的 None 强制为 0 | 结果异常 | 已随 P2-4 一并说明 |

---

## 三、逐条问题

### P0 阻塞

无。

### P1 必修

以下 5 条同属一个根因：**测试只覆盖了“字段存在/类型为基本类型”，没有覆盖 §39/§40 的“闭集枚举”与“字符串字段的类型与取值”语义**，因此把校验放宽到 `str()`/直通不会被任何用例检出。每条均可独立复现（把实现改成表中所列、跑指定测试文件，45 条全绿）。

**P1-A：`feedback.status` 的 §36 八值闭集未钉死（存活变异 P15）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:572`（`enum_of(PlanningDecisionStatus, self.status, "feedback.status")`）
- 规格：任务书 C4 与 §36 明写 `status` 只允许 `PlanningDecisionStatus` 八值。
- 复现：
  ```bash
  python3 - <<'EOF'
  p='src/agent_orchestrator/contracts/planning_decisions.py'
  s=open(p).read()
  s=s.replace('self, "status", enum_of(PlanningDecisionStatus, self.status, "feedback.status")',
              'self, "status", str(self.status)')+"\n# m\n"
  open(p,'w').write(s)
  EOF
  PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_contract.py -p no:cacheprovider -q
  # 实际：45 passed  → 未被检出
  ```
- 行为回退实测（变异后）：`PlanningFeedbackV1(..., status="GARBAGE")` 被**接受**（原实现拒绝）。
- 影响：`status` 是 admission 反馈与持久状态之间的关键字段，非法状态静默入库。

**P1-B：`feedback.rejection_codes` 元素的闭集未钉死（存活变异 Q1）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:575-585`（`sequence_of(..., lambda entry, where: enum_of(PlanningDecisionRejectionCode, entry, where), limit=len(...))`，`enum_of` 在 `:580`）
- 复现：把该 `lambda` 改成 `lambda entry, where: str(entry)`，测试文件仍 **45 passed**。
- 行为回退实测：`rejection_codes=("NOPE",)` 被**接受**（原实现 `ContractError`）。
- 影响：§33 “完整拒绝码”闭集失效，任意字符串可进入反馈。

**P1-C：`problem.code` 的闭集未钉死（存活变异 Q2）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:445-447`（`enum_of(PlanningDecisionRejectionCode, self.code, "problem.code")`，`enum_of` 在 `:446`）
- 复现：改为 `str(self.code)`，测试文件仍 **45 passed**。
- 行为回退实测：`PlanningProblemDetailV1(code="NOPE", ...)` 被**接受**。
- 影响：§33 闭集在问题明细侧同样失效，非法 code 可随反馈/明细持久化。

**P1-D：`problem.field_path` 的字符串约束未钉死（存活变异 P14）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:453-459`（`identifier(self.field_path, "problem.field_path")`，在 `:458`）
- 规格：V2 §40 `field_path: str | None`。
- 复现：改为 `str(self.field_path)`，测试文件仍 **45 passed**。
- 行为回退实测：`field_path=123` 被**接受**并存成 `'123'`（类型语义被静默改变）。
- 影响：`field_path` 从 `str | None` 退化为“任意值转字符串”，路径锚点可被静默篡改。

**P1-E：`problem.expected/observed` 的类型约束未钉死（存活变异 Q5）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:461-462`（`_optional_text(self.expected, ...)` / `_optional_text(self.observed, ...)`）
- 规格：V2 §40 `expected: str | None = None`、`observed: str | None = None`。
- 复现：改为直接赋值 `self.expected` / `self.observed`，测试文件仍 **45 passed**。
- 行为回退实测：`expected=123`、`observed=[1,2]` 被**接受**。
- 影响：§40 明定的 `str | None` 退化为任意值直通，问题明细的机器可读性被破坏。

### P2 建议

**P2-1：`binding.intent_id` 的校验未钉死（存活变异 Q6）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:383`（`identifier(self.intent_id, "binding.intent_id")`）
- 现象：去掉校验后 `intent_id=123` 被接受，测试仍 **45 passed**。
- 说明：`intent_id` 是 BL-7/I5 的追加字段，取值合法性由本模块唯一负责，建议补负例。

**P2-2：`binding.prompt_hash` 的 hash 校验未钉死（存活变异 Q7）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:379`（`_hash(self.prompt_hash, "binding.prompt_hash")`）
- 现象：去掉校验后 `prompt_hash="not-a-hash"` 被接受，测试仍 **45 passed**（同类 `package_hash` 等亦集中在一条未覆盖路径上）。
- 说明：与 §5 “旧字节/旧 hash 解释保持”同族的完整性约束，建议补一条非 64-hex 负例。

**P2-3：`binding.base_plan_revision` 下界未钉死（存活变异 P4）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:353-355`（`index(self.base_plan_revision, ..., minimum=0)`，在 `:354`）
- 现象：改成 `minimum=1` 后测试文件仍 **45 passed**（语义上会拒绝合法的 revision 0）。
- 说明：§34/§36 用 `INTEGER NOT NULL` 未明写非负，实现取 `>=0` 合理；建议补 0 接受 / -1 拒绝的边界断言。

**P2-4：`PlanningRetryBudgetView` 计数下界未钉死（存活变异 P5）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:515-519`（`index(getattr(self, name), name, minimum=0)`，在 `:518`）
- 现象：改成 `minimum=1` 后测试文件仍 **45 passed**（会拒绝合法的 0，例如 `same_request_format_retries_remaining=0`）。
- 说明：§39 未规定下界；这是“剩余量可以为 0”的合理实现，建议补 0 接受 / -1 拒绝断言。

**P2-5：`PlanningFeedbackV1.to_json` 的 `str(status)` 为等价写法（存活变异 Q10，非缺口）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:609`
- 说明：`PlanningDecisionStatus` 是 `StrEnum`，其成员 `==` 同值字符串且被 `to_json` 用于 dict 比较时等价，故该变异在语义上**确实等价**；不构成测试缺口。

**P2-6：`decision_id` 的 `str(ordinal)` 在本片输入域内为等价写法（存活变异 P10，非缺口）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:660`
- 说明：配合 `index(..., minimum=0)` 把 ordinal 限为 int，则 `str(int) == repr(int)` 对所有合法值成立，故该变异等价；不构成测试缺口。（若未来 ordinal 允许大整数以外类型，此结论需重估。）

**范围观察（不计为问题）**：本片为 H1-A 子片，未交付 `PlanningDecisionEnvelopeV1`、`contracts/schemas/planning-decision-v1.schema.json`、`schemas/__init__.py`、黄金 fixtures、`test_planning_decision_json_schema.py`，也未在 `contracts/__init__.py` 挂载模块；实现者已在 `plans/llm-native-htn/H1/journal.md` 逐条说明（BL-1…BL-6 待裁定、不发明 wire format），且核验清单未将这些列为本次检查项，故不阻塞。

---

## 四、命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/contracts/planning_decisions.py tests/orchestrator/full_target/test_planning_decision_contract.py
All checks passed!
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_contract.py -p no:cacheprovider -q
45 passed in 0.07s
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3005 passed, 2 skipped in 131.20s (0:02:11)
$ PYTHONPATH=src uv run --offline python -c "import agent_orchestrator.contracts.planning_decisions as m; print('OK:', m.__file__)"
OK: /Users/taiwan/PROJECTS/SimplaHarness/simple-runtime-sdk-h1a1-verify-h1-a1/src/agent_orchestrator/contracts/planning_decisions.py
```

（`full_target` 全量跑为回归参考：本片未引入新失败。2 skipped 为 `test_panda_backend.py` 与 `test_real_provider_hierarchical_smoke.py` 的环境性跳过，与本片无关。）

## 五、变异恢复与工作树状态

- 每次变异前备份到 `/tmp/h1a1b/orig/`，变异后覆盖恢复；恢复后 sha256 与备份一致：
  - `planning_decisions.py` = `b6290525b835d129a75fa976bfd47af5052b3b1f2b28ead87e859da5c145b38d`
  - `test_planning_decision_contract.py` = `e34bcd292ecbcaab7b6d903e59d75fca647197931d7f41afc37e8f4c4c0f8d74`
- 全部变异循环结束后 `git status --short -uall` 输出为空（除本报告新增外干净）。
- 核验脚本落点：`/tmp/h1a1b/`（`diff_vs_code.py`、`verify_decision_id.py`、`check_dataclasses.py`、`mutate.py`、`mutate_probe.py`、`mutate_probe2.py`、`probe_p1.py`、`probe_survivors.py`、`conf_b.py`、`conf_c.py`、`cmd_tails.txt`、`orig/` 备份）。

---

## 复核 3（同一核验员会话续做，2026-09-18）

- **复核对象：** 当前目录 HEAD `3f0aadd`（上一轮核验 HEAD 为 `7bcbd1b`）
- **本轮新增提交：**

```text
$ git log --oneline e1684b7..HEAD
3f0aadd docs(h1-a1): note legacy timing-flake observation in journal
c779e80 test(h1-a1): pin closed-set validation of feedback and problem-detail fields
7bcbd1b test(h1-a1): pin wire identity constants and §17 ref boundaries
e971a07 feat(h1-a1): planning-decision contract core (enums, refs, request binding, feedback, decision id)

$ git diff 7bcbd1b HEAD --stat
 plans/llm-native-htn/H1/journal.md                 |  21 +++++
 .../full_target/test_planning_decision_contract.py | 101 +++++++++++++++++++++
 2 files changed, 122 insertions(+)
```

### 复核 3 结论

**只有 P2，无 P0/P1 ⇒ 可合。**

- 本轮改动**未越界**：只动 `tests/orchestrator/full_target/test_planning_decision_contract.py` 与 `plans/llm-native-htn/H1/journal.md`；**`src/` 零改动**（`planning_decisions.py` sha256 与上一轮完全一致），`contracts/` 既有文件在全 `e1684b7..HEAD` 范围内仍零改动。
- **上一轮 7 个存活变异复核全部 KILLED**（补测生效）。
- 新做 15 个变异（12 + 3），其中 8 KILLED、7 SURVIVED；7 个存活者**均已实测为非等价**（行为确有放宽），但同属"边界/兄弟字段加固"而非规格明文要求项 ⇒ 记为 P2。

---

### 复核 3 步骤 1：本轮改了什么、是否越界

- `git diff --name-status 7bcbd1b HEAD`：仅 `M plans/llm-native-htn/H1/journal.md`、`M tests/orchestrator/full_target/test_planning_decision_contract.py`。
- `git diff --name-only 7bcbd1b HEAD -- src/` 输出为空 ⇒ **本轮 `src/` 零改动**；被测实现未变，故本轮所有变异结果都与上一轮可直接比较。
- `git diff --name-only e1684b7 HEAD -- …/contracts/__init__.py …/semantic_base.py …/htn.py …/models.py …/ids.py` 输出为空 ⇒ **`contracts/` 既有文件零改动**。
- `git diff --summary e1684b7 HEAD` 仅三条 `create mode`（无删除、无 mode 变更）。
- 内容对应关系：新增 7 个用例（测试文件 `:378-472`）逐条针对上一轮 7 个 P1/P2 存活变异：

```text
378:def test_feedback_status_closed_set_is_enforced()
392:def test_feedback_rejection_codes_element_closed_set_is_enforced()
405:def test_problem_detail_code_closed_set_is_enforced()
416:def test_problem_detail_text_fields_reject_wrong_types()
435:def test_request_binding_intent_id_and_prompt_hash_are_strict()
449:def test_request_binding_base_plan_revision_lower_bound_is_pinned()
459:def test_retry_budget_counters_lower_bound_is_pinned()
```

**无越界。通过。**

### 复核 3 步骤 2：上一轮 7 个存活变异逐个重跑（全部 KILLED）

方法：变异前备份至 `/tmp/h1a1c/orig/`；变异追加长度不同的标记行并设 `PYTHONDONTWRITEBYTECODE=1`；**从 /tmp 副本覆盖恢复**；每轮核对 sha256 与 `git status --short`。

| # | 变异 | 上轮 | 本轮 | 触发失败用例 |
|---|---|---|---|---|
| R2-1 | `feedback.status`：`enum_of` → `str()` | SURVIVED | **KILLED** | `test_feedback_status_closed_set_is_enforced` |
| R2-2 | `feedback.rejection_codes` 元素：`enum_of` → `str()` | SURVIVED | **KILLED** | `test_feedback_rejection_codes_element_closed_set_is_enforced` |
| R2-3 | `problem.code`：`enum_of` → `str()` | SURVIVED | **KILLED** | `test_problem_detail_code_closed_set_is_enforced` |
| R2-4 | `problem.field_path`：`identifier` → `str()` | SURVIVED | **KILLED** | `test_problem_detail_text_fields_reject_wrong_types` |
| R2-5 | `problem.expected/observed`：`_optional_text` 移除 | SURVIVED | **KILLED** | `test_problem_detail_text_fields_reject_wrong_types` |
| R2-6 | `binding.intent_id`：`identifier` 移除 | SURVIVED | **KILLED** | `test_request_binding_intent_id_and_prompt_hash_are_strict` |
| R2-7 | `binding.prompt_hash`：`_hash` 移除 | SURVIVED | **KILLED** | `test_request_binding_intent_id_and_prompt_hash_are_strict` |

（另复核上一轮两条 P2：`binding.base_plan_revision` 下界 `0→1` KILLED、`PlanningRetryBudgetView` 计数下界 `0→1` KILLED，分别由 `test_request_binding_base_plan_revision_lower_bound_is_pinned`、`test_retry_budget_counters_lower_bound_is_pinned` 捕获。）

**上一轮全部存活变异已清零。通过。**

### 复核 3 步骤 3：本轮新增变异（12 + 3 = 15 个）

**(3a) 针对本轮新增校验与测试的 12 个新变异：**

| # | 变异 | 结果 | 触发失败用例 |
|---|---|---|---|
| N1 | `problem.detail`：`text()` → `str()` | KILLED | `test_wrong_type_is_rejected[problem_detail-detail-123]` |
| N2 | `problem.subject_ref` 检查移除 | KILLED | `test_dataclass_round_trips_through_json[problem_detail]` |
| N3 | `feedback.problems` 元素检查移除 | KILLED | `test_dataclass_round_trips_through_json[feedback]` |
| N4 | `feedback.changed_refs` 元素检查移除 | KILLED | `test_dataclass_round_trips_through_json[feedback]` |
| **N5** | `feedback.rejection_codes` 条数上限 `len(enum)` → `10_000` | **SURVIVED** | — 见 P2-A |
| **N6** | `repeated_failure_…` 校验退化（丢下界） | **SURVIVED** | — 见 P2-B |
| N7 | `feedback.previous_decision_id`：`identifier` → `str()` | KILLED | `test_wrong_type_is_rejected[feedback-previous_decision_id-123]` |
| N8 | `PlanningRefV1.id` 上限退回默认 `MAX_ID=512` | KILLED | `test_max_planning_ref_id_is_pinned_with_boundaries` |
| **N9** | `binding.package_hash`：`_hash` 移除 | **SURVIVED** | — 见 P2-C |
| **N10** | `binding.visible_refs_digest`：`_hash` 移除 | **SURVIVED** | — 见 P2-C |
| **N11** | `repeated_failure_…` 边界直通（负值可入） | **SURVIVED** | — 见 P2-B |
| N12 | `PlanningRefKind`：`enum_of` → 直通 | KILLED | `test_planning_ref_illegal_inputs_are_rejected[value0]` |

**(3b) 补充的 3 个兄弟字段变异：**

| # | 变异 | 结果 | 备注 |
|---|---|---|---|
| **D1** | `binding.scope_epoch_digest`：`_hash` 移除 | **SURVIVED** | 见 P2-C |
| **D2** | `binding.subject_bindings_hash`：`_hash` 移除 | **SURVIVED** | 见 P2-C |
| **D3** | `binding.requirements_revision` 下界 `0 → 1` | **SURVIVED** | 见 P2-D |

**非等价性证据（原实现拒绝、变异实现接受，逐条实测）：**

```text
under N5  : feedback.rejection_codes 37 items        -> ACCEPTED   (原实现 ContractError)
under N6  : budget.repeated_failure=-1               -> ACCEPTED   (原实现 ContractError)
under N11 : budget.repeated_failure=-1               -> ACCEPTED   (原实现 ContractError)
under N9  : binding.package_hash='not-a-hash'        -> ACCEPTED   (原实现 ContractError)
under N10 : binding.visible_refs_digest='zzz'        -> ACCEPTED   (原实现 ContractError)
under D1  : binding.scope_epoch_digest='zzz'         -> ACCEPTED   (原实现 ContractError)
under D2  : binding.subject_bindings_hash='zzz'      -> ACCEPTED   (原实现 ContractError)
under D3  : binding.requirements_revision=0          -> REJECTED   (原实现接受；下界被抬到 1 会误拒)
```

### 复核 3 逐条问题

#### P0 阻塞

无。

#### P1 必修

无。（上一轮 P1 已全部闭环 KILLED。）

#### P2 建议

**P2-A：`feedback.rejection_codes` 条数上限未被钉死（存活变异 N5）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:581`（`limit=len(PlanningDecisionRejectionCode)`）
- 现象：上限改为 `10_000` 后目标测试文件仍 **52 passed**；实测 37 个元素被接受（原实现拒绝）。
- 规格：V2 §39 未规定条数上限（该上限由实现按枚举基数推导），故非规格要求项。
- 建议：补一条"37 个元素应拒绝"的用例，或给该上限命名常量后钉死。

**P2-B：`PlanningRetryBudgetView.repeated_failure_before_escalation_remaining` 下界未被钉死（存活变异 N6/N11）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:523-527`（`optional_index(self.repeated_failure_before_escalation_remaining, ..., minimum=0)`，`minimum=0` 在 `:526`）
- 现象：改为直通或去掉下界后，`repeated_failure_before_escalation_remaining=-1` 被接受，测试仍 **52 passed**。
- 规格：V2 §39 仅写 `int | None`，未规定下界；该下界属实现推理。
- 建议：补一条 `-1` 拒绝 / `0` 接受 / `None` 接受的边界断言（本轮新用例已覆盖同族的前四个计数，唯独漏了这个可空字段）。

**P2-C：`PlanningRequestBinding` 的 4 个 digest/hash 字段未被钉死（存活变异 N9/N10/D1/D2）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:350`（`package_hash`）、`:364`（`scope_epoch_digest`）、`:369`（`subject_bindings_hash`）、`:374`（`visible_refs_digest`）
- 现象：逐个移除 `_hash` 后，`package_hash='not-a-hash'` / `visible_refs_digest='zzz'` / `scope_epoch_digest='zzz'` / `subject_bindings_hash='zzz'` 均被接受，测试仍 **52 passed**。
- 规格：任务书 R2（`:547`）明文要求 **`package_hash` / `prompt_hash` 必须是 64 hex，否则拒**；`prompt_hash` 本轮已补负例（KILLED），**`package_hash` 与其 3 个兄弟 digest 字段仍无负例**。
- 建议：补一条参数化负例，覆盖 `package_hash` / `scope_epoch_digest` / `subject_bindings_hash` / `visible_refs_digest` 的非 64-hex 输入（`prompt_hash` 可一并纳入同一参数表）。

**P2-D：`binding.requirements_revision` 下界未被钉死（存活变异 D3）**
- 位置：`src/agent_orchestrator/contracts/planning_decisions.py:356-360`（`index(self.requirements_revision, ..., minimum=0)`，在 `:359`）
- 现象：下界改为 `1` 后测试仍 **52 passed**（会误拒合法的 `requirements_revision=0`）。
- 规格：§34/§36 用 `INTEGER NOT NULL` 未规定非负；实现取 `>=0` 合理。
- 建议：补 0 接受 / -1 拒绝断言（与 `base_plan_revision` 本轮已补的边界断言同形）。

**说明（不计为问题）**：本轮新增用例 `test_problem_detail_text_fields_reject_wrong_types` 覆盖了 `field_path`/`expected`/`observed` 三类错类型，但 `expected` 与 `observed` 未分别覆盖"非 str 的 list/None 组合"等更强组合；因核心语义（非 str 拒绝）已被覆盖，不再单列。

### 复核 3：命令尾行原文

```text
$ uv run --offline ruff check src/agent_orchestrator/contracts/planning_decisions.py tests/orchestrator/full_target/test_planning_decision_contract.py
All checks passed!
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_contract.py -p no:cacheprovider -q
52 passed in 0.06s
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target -p no:cacheprovider -q
3012 passed, 2 skipped in 127.95s (0:02:07)
$ PYTHONPATH=src uv run --offline python -c "import agent_orchestrator.contracts.planning_decisions as m; print('OK:', m.__file__)"
OK: /Users/taiwan/PROJECTS/SimplaHarness/simple-runtime-sdk-h1a1-verify-h1-a1/src/agent_orchestrator/contracts/planning_decisions.py
```

（`full_target` 较上一轮 3005 → 3012，增量 +7 恰为本轮新增 7 个用例；2 skipped 仍为环境性跳过。）

### 复核 3：恢复与工作树状态

- 变异前备份 `/tmp/h1a1c/orig/`，每轮覆盖恢复；全部循环结束后
  `planning_decisions.py` sha256 = `b6290525b835d129a75fa976bfd47af5052b3b1f2b28ead87e859da5c145b38d`（与备份、与 HEAD 一致），
  `test_planning_decision_contract.py` sha256 = `02e0ae4714ec230be75e3f5c3ca88171a99a4870291d9a8c39f25e5d875a8020`（与备份一致）。
- `git status --short -uall` 仅显示本报告为未跟踪文件；`git diff --stat HEAD` 为空（被跟踪文件未被核验动作改动）。
- 本轮未使用 `git stash / checkout / reset --hard / rebase / push`，未 commit，未读密钥文件。
- 核验脚本落点：`/tmp/h1a1c/`（`mutate.py`、`mutate_new.py`、`mutate_digest.py`、`mutate_p2.py`、`probe_new.py`、`orig/` 备份）。

### 复核 3 判定

- 步骤 1：未越界、`contracts/` 既有文件零改动、本轮 `src/` 零改动 —— **通过**
- 步骤 2：上一轮 7 个存活变异全部 KILLED —— **通过**
- 步骤 3：15 个新变异中 8 KILLED、7 SURVIVED；存活者均为**非规格强制**的边界/兄弟字段加固缺口 —— 记为 **P2**
- 步骤 4：ruff `All checks passed!`；目标测试 `52 passed`；`full_target` `3012 passed, 2 skipped`

**只有 P2、无 P0/P1 ⇒ 可合。**

VERDICT: 可合
