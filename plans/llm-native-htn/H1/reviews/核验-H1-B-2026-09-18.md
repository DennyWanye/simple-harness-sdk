# 独立核验 H1-B（数据库迁移 19 与决定存储）

> 说明：核验员写了 395 行完整报告（第 1 轮正文 + 复核 2），放在分离副本里未提交；流水线保存未提交文件时没处理中文文件名（已修），副本随清理丢失。现存的是核验员两轮的最终回复原文与确定性检查报告 `../gates/h1-b-gate-full.json`。第 1 轮结论：修后可合（1 条 P1：NOT NULL 未被测试钉住；2 条 P2）；第 2 轮结论：可合。

## 复核 2（终轮，2026-09-19 01:11）——核验员最终回复原文

The 复核 2 work is complete. Summary of this continuation:

**Re-checked state:** worktree clean; the untracked H1-B report had been wiped between turns (only `核验-H1-A1-2026-09-18.md` remained in `reviews/`), so I restored the round-1 body and appended the requested `## 复核 2` section at the specified path — no new report file.

**Verification completed (not redone):** the round-1 survivors, the new mutations, the fix-scope diff, the tests-first evidence, the frozen checksum cross-check, and all gate suites.

## 结果

**Files changed (working tree, report only — nothing else):**
- `plans/llm-native-htn/H1/reviews/核验-H1-B-2026-09-18.md` (new, untracked, not committed) — round-1 body + appended `## 复核 2` section (395 lines). No source/test file was modified in this round; every mutation was restored from `/tmp` and verified clean (`git status --short` empty of tracked changes; mutated files matched `HEAD:<path>` blob hashes).

**Scope reviewed (`7141b48` → `f951c46`, 3 files):** `journal-B.md`, `planning_decision_store.py`, `test_planning_decision_store.py`. `contracts/` = 0 changes; `commit_service`/`event_handler`/`hierarchical_dispatch`/`policy_snapshot` untouched; `planning_decision_schema.py` + `schema.py` byte-unchanged between rounds.

**Round-1 findings — all fixed:**
- **P1-1** (NOT NULL not pinned): 4/4 surviving mutants now **KILLED** (intent_id, status, prompt_version, raw_output_hash NOT NULL removals); new `PROTOCOL_SCHEMA` pins name+type+NOT NULL+PK with **no column coverage lost** (6/14/12 identical).
- **P2-1** (empty value blanked `detail`/`rejection_codes`): fixed with `COALESCE`; probe confirms `detail blanked? False`; new tests fail on the pre-fix store (**1 failed → red/green proven**).
- **P2-2** (no frozen checksum for 19): `MIGRATION_19_CHECKSUM` added; independent `sha256(DDL)` == `schema.checksum()` == constant.

**New mutants:** 12/12 **KILLED** (unconditional-overwrite revert, PK removals ×3, type change, NOT NULL removal, STRICT removal, checksum tamper ×2, over-retention, pin weakened, empty-list guard removal). One documented boundary: editing DDL *and* hand-editing the frozen constant survives — expected golden-value semantics (N7 shows DDL-only edits go red).

**Tests run (this round):**
- `test_planning_decision_store.py` + `test_planning_decision_request_binding.py`: **35 passed**
- `test_htn_store.py`: **93 passed**
- full_target: **3047 passed, 2 skipped** (+3 vs round-1)
- legacy mode: **560 passed, 13 skipped**
- sentinel `test_hierarchical_event_flow.py`: **108 passed** (`self._new_mode(mission)` count = 19, file unchanged)
- ruff: **All checks passed!**

**Left undone / notes:** No commit, push, or merge (per instructions) — the report remains an untracked working-tree file, and I flagged in it that the round-1 report was missing from the copy and should be committed to avoid loss again.

VERDICT: 可合