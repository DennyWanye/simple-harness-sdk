# 独立核验报告：切片 H1-T（两种决定降为只解码）

**核验对象：** 切片 H1-T，按 2026-09-19 15:20 追加裁定把 `BIND_EXISTING_GOAL` 与 `REPAIR/PROPOSE_SUCCESSOR` 降为只解码。
**基准：** `e8fbafc`（`docs(h1): archive full-gate report run on merged main (F)`）
**被核验 HEAD：** `b0accdf`（`c4142fc` 功能提交 → `b0accdf` journal 收尾）
**工作目录：** `/Users/taiwan/PROJECTS/SimplaHarness/simple-harness-sdk-h1t-verify-h1-t`
（SDK 分离副本；detached HEAD；`agent_orchestrator.__file__` = 本目录 `src/agent_orchestrator/__init__.py`，`PYTHONPATH=src uv run --offline python -c "import agent_orchestrator; print(agent_orchestrator.__file__)"` 已确认）。
**核验员立场：** 只看代码与证据；不 commit、不读密钥文件、未运行 `git stash/checkout/reset/push`。变异前把原文件复制到 `/tmp/h1t-verify/orig/`，变异后用副本恢复，并以 `git status --short` 确认干净。
**权威规格：** 裁定补遗末尾「追加裁定（2026-09-19 15:20）」；冲突时裁定优先于 V2 第 12、44 节本阶段表述。既有拒绝码以 `PlanningDecisionRejectionCode` 封闭枚举为准。

> 第 1 轮（受检 HEAD `b0accdf`）。

---

## 结论

相对 `e8fbafc` 共 14 个路径，全部落在本片白名单（合同启用表、提示词 v8、冻结指纹、三个 `.expect.json`、既有测试、本日志）。生产代码只动两处：`H1_DECISION_ENABLEMENT` 两行 `_EXECUTABLE` → `_DECODE_ONLY`，以及 v8 删除 `BIND_EXISTING_GOAL` 诱导段。准入实现、编解码器、Schema、`valid/` 样例、存储层、编译器均为 0 字节改动。

10 种类型启用矩阵只有 `REPAIR/PROPOSE_SUCCESSOR` 与 `BIND_EXISTING_GOAL` 从可执行变为只解码；其余 8 行前后一致。双违规（引用不在清单 + 类型未启用）仍先报 `REF_OUTSIDE_CONTEXT`。三种合法样例解码、往返、写入 `planning_decisions` 均成功，默认准入仍拒 `DECISION_NOT_ENABLED_IN_PHASE`。v8 正文检索不到这两种类型及其载荷字段名；指纹已更新且与文本一致；H0 的 29 条其余模板 sha256 逐一相等。8 个规定变异全部被杀死。

裁定口称 `DECISION_TYPE_NOT_ENABLED`，封闭枚举无此成员；实现沿用 H1-F 已落地、三种原仅解码类型共用的 `DECISION_NOT_ENABLED_IN_PHASE`。这是与既有码表对齐，不记问题。

**无 P0、无 P1。判定：可合。**

---

## 1. 逐条问题（P0/P1/P2）

无 P0、无 P1。

### P2-1 — 提示词覆盖测试函数名仍写「十二个要点」

- 位置：`tests/orchestrator/full_target/test_planning_decision_prompt_v8.py:124`（`test_v8_text_covers_every_one_of_the_twelve_points`）；`POINT_PHRASES` 现为 11 条（`:109-111`）。
- 事实：本片删掉 §41 点 9「共享用 BIND_EXISTING_GOAL」后，函数名与注释口径未改。行为断言仍遍历 `POINT_PHRASES`，不构成漏检。
- 复现：读函数名与 `len(POINT_PHRASES)`；不阻塞。

### P2-2 — Schema 元测试把三个下游码从「夹具必覆盖」里豁免

- 位置：`tests/orchestrator/full_target/test_planning_decision_json_schema.py` 新增 `ADMISSION_CODES_WITHOUT_H1F_FIXTURE = {OBLIGATION_NOT_OPEN, REFINEMENT_CYCLE, REUSE_NOT_ALLOWED}`。
- 事实：三个 `.expect.json` 的首个拒绝码改为阶段启用码后，这条元测试不再要求夹具 `expected_code` 钉住那三个下游码。它们仍由准入单元测试在显式重新启用对应行时覆盖（见 §2.6），故不是覆盖空洞，只是「每个拒绝码都有一份 `.expect.json`」这条更强不变量被本片正当削弱。
- 复现：`git diff e8fbafc -- tests/orchestrator/full_target/test_planning_decision_json_schema.py`。不阻塞。

---

## 2. 逐项核验

### 2.1 越界：通过

白名单（实施日志与裁定）：合同启用状态、既有测试、提示词 v8、提示词冻结指纹、三个受影响的 `.expect.json`、本日志。格式文件与 `valid/` 样例不得改。

`git diff --name-status e8fbafc`：

```text
A	plans/llm-native-htn/H1/journal-T.md
M	plans/llm-native-htn/H1/prompt-v8.md
M	src/agent_orchestrator/contracts/planning_decisions.py
M	src/agent_orchestrator/runtime/role_templates.py
M	tests/orchestrator/full_target/fixtures/planning_decision_v1/invalid/obligation-not-open.expect.json
M	tests/orchestrator/full_target/fixtures/planning_decision_v1/invalid/refinement-cycle.expect.json
M	tests/orchestrator/full_target/fixtures/planning_decision_v1/invalid/reuse-not-allowed.expect.json
M	tests/orchestrator/full_target/test_output_port_claims.py
M	tests/orchestrator/full_target/test_planning_decision_admission.py
M	tests/orchestrator/full_target/test_planning_decision_codec.py
M	tests/orchestrator/full_target/test_planning_decision_contract.py
M	tests/orchestrator/full_target/test_planning_decision_json_schema.py
M	tests/orchestrator/full_target/test_planning_decision_package_v4.py
M	tests/orchestrator/full_target/test_planning_decision_prompt_v8.py
```

白名单外路径为空。`decision_admission.py` / `decision_codec.py` / `contracts/schemas/` / `valid/*.json` / `storage/` / `hierarchical_dispatch.py` 相对基准 0 改动。

生产最小性：

| 文件 | numstat | 内容 |
|---|---|---|
| `planning_decisions.py` | 3/3 | 启用表两行 + 一处计数注释 |
| `role_templates.py` | 0/2 | 删除 BIND 诱导两行，无顺手重构 |

测试侧把泄漏扫描从「每码取一份夹具」改成「遍历全部 H1-F 夹具」，是因为三个 `.expect.json` 现在共用 `DECISION_NOT_ENABLED_IN_PHASE`，原 `next(...)` 会丢掉后两份；属必要连带，不是无关重构。

### 2.2 启用矩阵：通过（只有那两种变了）

脚本从基准与当前 `H1_DECISION_ENABLEMENT` 源码抽取 `_EXECUTABLE` / `_DECODE_ONLY`，并与运行时 `enablement.executable` 交叉核对。请求包启用清单运行时为 `['DECLARE_BLOCKED', 'NO_CHANGE', 'REFINE', 'REPAIR/REPLACE_METHOD', 'WAIT']`。

| 类型 | 改动前 | 改动后 | 是否变化 |
|---|---|---|---|
| REFINE | 可执行 | 可执行 | 否 |
| REPAIR/REPLACE_METHOD | 可执行 | 可执行 | 否 |
| REPAIR/PROPOSE_SUCCESSOR | 可执行 | 只解码 | **是** |
| BIND_EXISTING_GOAL | 可执行 | 只解码 | **是** |
| DECLARE_BLOCKED | 可执行 | 可执行 | 否 |
| WAIT | 可执行 | 可执行 | 否 |
| NO_CHANGE | 可执行 | 可执行 | 否 |
| REQUEST_EVIDENCE | 只解码 | 只解码 | 否 |
| REQUEST_HUMAN | 只解码 | 只解码 | 否 |
| PROPOSE_METHOD | 只解码 | 只解码 | 否 |

运行时三旗（两种降级行，`planning_decisions.py:266-267`）：`decodable=True admissible=False executable=False`，与原三种仅解码类型同一 `_DECODE_ONLY`。键集合 10 项，前后相等。

### 2.3 准入顺序未变：通过

`admit_planning_decision` 阶段元组（`decision_admission.py:1215-1220`）相对基准 0 改动：visible refs 仍在 phase enablement 之前。

核验员自构「引用不在清单 + 类型未启用」：

| 输入 | 默认 H1 启用集（两种已不在清单） | 显式重新启用该类型 |
|---|---|---|
| `bind-existing-goal-reuse` 改 `goal_ref.content_hash` | `REF_OUTSIDE_CONTEXT` | `REF_OUTSIDE_CONTEXT` |
| `bind-existing-goal-share` 改 `goal_ref.content_hash` | `REF_OUTSIDE_CONTEXT` | `REF_OUTSIDE_CONTEXT` |
| `repair-propose-successor` 改 `old_task_ref.semantic_revision` | `REF_OUTSIDE_CONTEXT` | `REF_OUTSIDE_CONTEXT` |
| 对照：原仅解码 `request-human` + 隐藏 `reason_refs` | `REF_OUTSIDE_CONTEXT` | — |

与改动前一致：引用阶段仍先到。若把阶段启用前移，同一输入会改报 `DECISION_NOT_ENABLED_IN_PHASE`（见变异 M8，已杀死）。

### 2.4 解码未受影响：通过

`valid/` 三份合法样例未改。核验员走 codec → 往返 → `PlanningDecisionStore.record_planning_decision(status=DECODED)`：

| 夹具 | parse | 往返 `to_json`/再 decode | 存储 status/type | 默认准入 |
|---|---|---|---|---|
| `repair-propose-successor` | `REPAIR` | True | `DECODED` / `REPAIR` | `DECISION_NOT_ENABLED_IN_PHASE` |
| `bind-existing-goal-reuse` | `BIND_EXISTING_GOAL` | True | `DECODED` / `BIND_EXISTING_GOAL` | `DECISION_NOT_ENABLED_IN_PHASE` |
| `bind-existing-goal-share` | `BIND_EXISTING_GOAL` | True | `DECODED` / `BIND_EXISTING_GOAL` | `DECISION_NOT_ENABLED_IN_PHASE` |

canonical_hash 与行内哈希一致。裁定「解码、合法样例、存储不变；只是不进入执行」成立。

### 2.5 提示词 v8 / 指纹 / 其余模板：通过

对 `PLANNER_HIERARCHICAL_V8.instructions` 做子串检索（含示例 JSON 内取值）：

| 检索字样 | 命中 |
|---|---|
| `BIND_EXISTING_GOAL` | 0 |
| `PROPOSE_SUCCESSOR` | 0 |
| `REPAIR/PROPOSE_SUCCESSOR` | 0 |
| `goal_ref` | 0 |
| `resolution_ref` | 0 |
| `BindExistingGoal` / `propose_successor` / `绑定已有` / `后继任务` | 0 |

- 正文长度 2459；UTF-8 sha256 = `90c8b9f0551b98e299f1c11f90930f2b77b46e83397caaaa0f99617381c93e1c`。
- 冻结表 `FROZEN_PROMPT_DIGESTS["PLANNER_HIERARCHICAL_V8"]` 已从基准 `dba73c4f…e85ada` 更新为上述摘要。
- `plans/llm-native-htn/H1/prompt-v8.md` 围栏（`re.search(r"```text\n(.*?)```\n", …, re.S)`）与 `instructions` 逐字节相等。
- H0 `prompt-digests.json` 的 29 条（不含本片才引入的 v8）与当前 `TEMPLATE_VERSIONS` 逐一 EQUAL：`planner-v3/v4`、`planner-hierarchical-v1`…`v7`、`method-synthesizer-v1`…`v7`、`worker-v2/v3`、`worker-hierarchical-v1`…`v4`、`worker-appworld-v3`、`worker-appworld-hierarchical-v1`、`root-reviewer-v1/v2/v3`、`critic-v2/v3`。mismatch=[]，missing_live=[]。

v8 仍写「本阶段不能请求取证：REQUEST_EVIDENCE（以及 REQUEST_HUMAN、PROPOSE_METHOD）」；裁定只要求删除对两种降级类型的诱导段，未要求把它们写进禁止句。剩余 REPAIR 段只教 `REPLACE_METHOD`，并写「不要用别的 repair_kind 代替」，不点名 `PROPOSE_SUCCESSOR`。

### 2.6 反例 `.expect.json`：通过（三份均有必要，新码正确）

相对基准只改了三份 `.expect.json` 的 `expected_code` 字段；对应 `.json` 本体未改。另有 `structure-share-active-with-resolution` 也是 `BIND_EXISTING_GOAL`，但其 `expected_stage=codec` / `STRUCTURE_INVALID`，编解码先于准入，正确未改。

| 文件 | 夹具类型 | 原期望 | 新期望 | 默认准入（核验员实测） | 重新启用该行后 |
|---|---|---|---|---|---|
| `invalid/obligation-not-open.expect.json` | REPAIR / PROPOSE_SUCCESSOR | `OBLIGATION_NOT_OPEN` | `DECISION_NOT_ENABLED_IN_PHASE` | 同新期望 | `OBLIGATION_NOT_OPEN` |
| `invalid/refinement-cycle.expect.json` | REPAIR / PROPOSE_SUCCESSOR | `REFINEMENT_CYCLE` | `DECISION_NOT_ENABLED_IN_PHASE` | 同新期望 | `REFINEMENT_CYCLE` |
| `invalid/reuse-not-allowed.expect.json` | BIND_EXISTING_GOAL | `REUSE_NOT_ALLOWED` | `DECISION_NOT_ENABLED_IN_PHASE` | 同新期望 | `REUSE_NOT_ALLOWED` |

必要：阶段启用（§43 第 6 步）先于载荷/结构检查。不改 expect 则 H1-F 夹具遍历会红。下游三码仍由 `test_a_successor_on_a_closed_obligation_is_refused` / `test_a_successor_that_would_refine_itself_is_refused` / `test_a_resolution_that_is_not_current_cannot_be_reused` 在 `enabled_decision_types=H1_ENABLED | {该行}` 下钉住。新拒绝码与既有仅解码类型同一码，正确。

### 2.7 变异 ≥8：8/8 杀死

定向集：admission + codec + contract + json_schema + package_v4 + prompt_v8 + output_port_claims；`-p no:cacheprovider`。每轮结束后用 `/tmp/h1t-verify/orig/` 副本恢复，未用 checkout/stash/reset。终态 `git status --short` 为空。

| ID | 变异 | 结果 | 定向集尾行 | 杀死点 |
|---|---|---|---|---|
| M1 | 只降一种：`BIND_EXISTING_GOAL` 改回 `_EXECUTABLE` | KILLED | `6 failed, 583 passed, 3 skipped in 1.30s` | 包启用清单仍含 BIND；enablement 表断言 |
| M2 | 把别的也降了：`REFINE` 改为 `_DECODE_ONLY` | KILLED | `49 failed, 540 passed, 3 skipped in 1.52s` | 可执行夹具准入、enablement 表 |
| M3 | 准入放行：阶段闸门对两种降级类型直接 `return` | KILLED | `9 failed, 580 passed, 3 skipped in 1.35s` | `test_every_decode_only_valid_fixture_is_phase_refused` 收到 `AdmittedPlanningDecision` |
| M4 | 解码也一并拒绝：codec 对两种类型抛 `DECISION_NOT_ENABLED_IN_PHASE` | KILLED | `3 failed, 586 passed, 3 skipped in 1.17s` | `test_demoted_operations_still_decode_and_round_trip` 三夹具 |
| M5 | 提示词残留示例：把 BIND 段写回，并同步指纹与 md | KILLED | `1 failed, 588 passed, 3 skipped in 1.23s` | `test_v8_does_not_induce_demoted_operations` |
| M6 | 启用清单仍含它们：`_enabled_decision_types` 并入两行 | KILLED | `1 failed, 588 passed, 3 skipped in 1.17s` | `test_the_decision_package_states_the_protocol_and_its_enabled_types` |
| M7 | 指纹不更新：v8 正文插入「（本阶段）」但不改冻结表 | KILLED | `3 failed, 586 passed, 3 skipped in 1.17s` | `test_a_shipped_prompt_keeps_its_bytes[PLANNER_HIERARCHICAL_V8]` 摘要 `90c8b9f0…` ≠ `b45b79a0…` |
| M8 | 顺序前移：阶段启用与 visible refs 对调 | KILLED | `10 failed, 579 passed, 3 skipped in 1.22s` | 隐藏引用的降级夹具从 `REF_OUTSIDE_CONTEXT` 变成 `DECISION_NOT_ENABLED_IN_PHASE` |

### 2.8 ruff 与测试尾行

ruff：

```text
$ uv run --offline ruff check src/agent_orchestrator/contracts/planning_decisions.py src/agent_orchestrator/runtime/role_templates.py tests/orchestrator/full_target/test_planning_decision_admission.py tests/orchestrator/full_target/test_planning_decision_codec.py tests/orchestrator/full_target/test_planning_decision_contract.py tests/orchestrator/full_target/test_planning_decision_json_schema.py tests/orchestrator/full_target/test_planning_decision_package_v4.py tests/orchestrator/full_target/test_planning_decision_prompt_v8.py tests/orchestrator/full_target/test_output_port_claims.py
All checks passed!
```

定向测试（含 store，核验员亲跑）：

```text
$ PYTHONPATH=src uv run --offline pytest tests/orchestrator/full_target/test_planning_decision_admission.py tests/orchestrator/full_target/test_planning_decision_codec.py tests/orchestrator/full_target/test_planning_decision_contract.py tests/orchestrator/full_target/test_planning_decision_json_schema.py tests/orchestrator/full_target/test_planning_decision_package_v4.py tests/orchestrator/full_target/test_planning_decision_prompt_v8.py tests/orchestrator/full_target/test_output_port_claims.py tests/orchestrator/full_target/test_planning_decision_store.py -q -p no:cacheprovider
=========================== short test summary info ============================
SKIPPED [3] tests/orchestrator/full_target/test_planning_decision_admission.py:1248: codec-level refusal is covered by the codec test
620 passed, 3 skipped in 1.53s
```

导入路径确认：

```text
$ PYTHONPATH=src uv run --offline python -c "import agent_orchestrator; print(agent_orchestrator.__file__)"
/Users/taiwan/PROJECTS/SimplaHarness/simple-harness-sdk-h1t-verify-h1-t/src/agent_orchestrator/__init__.py
```

本核验员未重跑实施者日志中的全量 `tests/orchestrator/full_target`（声称 `3643 passed, 5 skipped`）与旧模式 560 条；生产未改准入/编译/存储实现，定向集已覆盖本片全部改动文件。未跑不等于那些命令失败。

---

## 3. 身份与范围备忘

- 工作树终态干净；核验过程未 commit。
- 裁定「V2 第 12 节启用矩阵、第 44 节映射表中这两行本阶段不生效」由启用表 + 包派生清单共同落实；适配层/编译器本片未动，符合「本阶段不换编译器」。
- `admissible` 旗标在生产路径上仍无读取点（准入读的是调用方传入的 `enabled_decision_types`，由 `executable` 派生）。这是 H1-A 起的既有形状，三种原仅解码类型同样如此，不记本片问题。
