# Simple Harness SDK

An embeddable, durable agent runtime extracted from Simple Harness.

This repository is in its v0.1 foundation phase. The only public symbol in the
foundation package is `simple_harness.__version__`; runtime factories are added
by the later implementation slices.

